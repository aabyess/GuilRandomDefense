# 조합식 수정 계획 (2026-09-02)

`RECIPE_AUDIT.md`에서 어긋난다고 확인된 **9줄**을 고치는 데 필요한 변경을 정확히 적어둔 것이다.
**아직 아무것도 고치지 않았다.** 사장님 결정이 나오면 이 목록대로 실행한다.

---

## 실행 방식 — 왜 생성기를 다시 돌리면 안 되나

조합식은 두 단계로 만들어졌다:
1. `Tools/generate_recipe_assets.py` — 이름만 보고 `기본_*` 임시 유닛을 참조하는 에셋을 찍어낸다
2. `Tools/repoint_upper_recipes.py` — 그 참조를 실제 등급 유닛으로 갈아끼운다

**생성기를 다시 돌리면 안 된다.** `generate_recipe_assets.py`는 매번 `new_guid()`로 GUID를 새로 뽑아서,
재실행하면 로스터·조합식의 모든 GUID가 바뀌고 `MainGachaTable`·씬 참조가 전부 깨진다.

→ **`.asset` 파일을 직접 고치고, `Tools/recipes_data.py`는 기록을 맞추기 위해 같이 고친다.**
   (`recipes_data.py`만 고치면 아무 효과가 없다. 반대로 `.asset`만 고치면 나중에 누가 전사를 다시 볼 때 되돌아간다.)

`.asset`의 재료 한 칸은 이 다섯 줄이다:
```yaml
  - kind: 0
    unit: {fileID: 11400000, guid: <유닛 GUID>, type: 2}
    item: {fileID: 0}
    wildcardGrade: 0
    count: 1
```
**조합식 표가 재료를 적힌 순서대로 그리므로 순서도 맞춰야 한다.**

---

## 1군 — 감사에서 확정된 9건 (바로 실행 가능)

전부 **이미 있는 유닛**을 가리키면 된다. **새로 만들 유닛도, 새 조합식도 없다.**

### ① `초월_조성진_AD` — 재료 이름 교체

| | |
|---|---|
| 자리 | `ingredients[1]` |
| 지금 | `특별함_조성진` `e347c6d9f02b49d7a23ce06074d70d6c` |
| 바꿀 것 | **`히든_최경범`** `3023d6bd4c884c029ff7db83b5b4204c` |
| 근거 | 이미지 2번째 재료가 `최경범`, 색 `(139,185,205)` = 하늘색 = 히든 |

`recipes_data.py` TRANSCENDENT: `["이동엽","조성진",…]` → `["이동엽","최경범",…]`

### ② `초월_유재헌_ADAP` — 재료 추가 + 순서 교정

이미지: `김민규 / 홍인창 / 유재헌 / 이은엽` (4개). 지금은 3개이고 순서도 다르다.

| 자리 | 바꿀 것 |
|---|---|
| `[0]` | `제한_김민규` 유지 (⚠️ 2군 참고) |
| `[1]` | **`전설적인_홍인창`** `dc83909b5a354e04ae67f0adb1eff38d` ← 지금 `[2]`에 있는 것을 앞으로 |
| `[2]` | **`희귀함_유재헌`** `538ce66f02f848b59b5889d327d43a6c` ← 지금 `[1]`에 있는 것을 뒤로 |
| `[3]` | **`희귀함_이은엽` 새로 추가** `96c3c0bae3bd48dda79b86235864f3c0` |

근거: 이은엽 색 `(99,39,95)` = 짙은 보라 = 희귀함 → `희귀함_이은엽`이 이미 있다.

`recipes_data.py`: `["김민규","유재헌","홍인창"]` → `["김민규","홍인창","유재헌","이은엽"]`

### ③ `히든_황정기` — 재료 **삭제** (재료 5개 → 4개)

⚠️ **이 건만 재료를 지운다.** 이미지의 첫 재료는 `최상호 상호파의두뇌` **한 덩어리**다 —
사이에 구분자 `/`가 없고 글자 색도 같다. 재측정에서 그 덩어리의 가로폭이 **106px**로
다른 재료(약 36px)의 세 배였다. 이 표는 `최상호 오타쿠의길`처럼 "이름 + 별칭"을 붙여 쓴다.

| | |
|---|---|
| 지울 것 | `ingredients[0]` **`전설적인_최상호`** `492454a484cc44079a1d4e9fd7a9216f` — 다섯 줄 통째로 삭제 |
| 남길 것 | `ingredients[1]` `희귀함_최상호_윤식파의두뇌` `b9cfc0bc8c444dfca71b42cd2141e8f7` — **그대로 두면 된다** |
| 근거 | 그 덩어리 색 `(95,35,93)` = 짙은 보라 = 희귀함. `윤식파의두뇌`가 희귀함 최상호이므로 일치 |

**지금 상태는 같은 인물을 두 번 넣은 셈**이다(전설적인 최상호 + 희귀함 최상호).

`recipes_data.py` HIDDEN_PERSON: `["최상호","윤식파의두뇌","배병규","황정기","노태현"]` → `["윤식파의두뇌","배병규","황정기","노태현"]`

### ④ `불멸_신지우` — 자원 비용 추가 (재료는 그대로)

| | |
|---|---|
| 지금 | `resourceCosts: []` |
| 바꿀 것 | `resourceCosts:` → `  - type: 0` / `    amount: 10` (type 0 = Wood) |
| 근거 | 이미지 `+ 나무 10개`. 불멸 8종 중 `박은석`만 무료고 나머지 7종 전부 나무 10 |

`recipes_data.py` IMMORTAL 신지우 튜플의 4번째 값 `0` → `10`

### ⑤ `불멸_이승우` — 재료 추가

| | |
|---|---|
| 넣을 자리 | `ingredients[1]` (이승우와 신문철 사이) |
| 넣을 것 | **`전설적인_이시원`** `dcdf6dbbdddc4d4db39e63a4c6aba922` |
| 근거 | 이미지 `이승우 / 이시원 / 신문철`, 세 재료 모두 빨강. 이시원 `(157,48,51)` = 전설적인 |

`recipes_data.py` IMMORTAL: `["이승우","신문철"]` → `["이승우","이시원","신문철"]`

### ⑥ `영원_이지원` — 재료 추가

| | |
|---|---|
| 넣을 자리 | `ingredients[1]` (김정래와 이지원 사이) |
| 넣을 것 | **`전설적인_이시원`** `dcdf6dbbdddc4d4db39e63a4c6aba922` |
| 근거 | 이미지 `김정래 / 이시원 / 이지원 / …`. **이시원과 이지원이 같은 줄에 나란히** 있고 색도 다르다 |

`recipes_data.py` ETERNAL: `["김정래","이지원",…]` → `["김정래","이시원","이지원",…]`

### ⑦ `영원_문필환` — 재료 추가

| | |
|---|---|
| 넣을 자리 | `ingredients[3]` (김태영 뒤, 마지막 박은석 앞) |
| 넣을 것 | **`특별함_양재모`** `cbe77abb750c4daf806cf7ee4c78c724` |
| 근거 | 이미지 `… / 김태영 / 양재모 / 박은석`. 양재모 `(210,170,101)` = 금색 = 특별함 |

⚠️ **이 줄은 박은석 문제와 얽혀 있다.** 지금 `ingredients[0]`이 `전설적인_박은석 count: 2`인데,
이미지에서 두 박은석의 **색이 다르다**(첫 번째 = 연한 핑크, 마지막 = 남색). 남색의 정체가 확정되면
그때 `count: 2`를 둘로 쪼개야 한다. **양재모 추가만 먼저 해도 되고, 박은석까지 같이 정리해도 된다 — 사장님 결정 대기.**

`recipes_data.py` ETERNAL: `["박은석","노태현","김태영","박은석"]` → `["박은석","노태현","김태영","양재모","박은석"]`

### ⑧ `영원_윤현모` — 재료 추가

| | |
|---|---|
| 넣을 자리 | `ingredients[0]` (**맨 앞**) |
| 넣을 것 | **`전설적인_이승우`** `c66783a2ce0c4659bdbe2af5f528ae89` |
| 근거 | 이미지 `이승우 / 박민석 / …`. 이승우 `(158,55,60)` = 전설적인 |

`recipes_data.py` ETERNAL: `["박민석",…]` → `["이승우","박민석",…]`

### ⑨ `제한_이유범` — 재료 이름 교체

| | |
|---|---|
| 자리 | `ingredients[0]` |
| 지금 | `특별함_이지원` `7f11e9f5e5ea4f66a0d11f3193bdfaca` |
| 바꿀 것 | **`전설적인_이시원`** `dcdf6dbbdddc4d4db39e63a4c6aba922` |
| 근거 | 이미지 1번째가 `이시원`, 색 `(156,51,57)` = 전설적인 |

이 하나로 `RECIPE_AUDIT.md` §F의 `특별함_이지원 → 전설적인` 항목도 같이 해소된다(등급 문제가 아니라 이름 문제였다).

`recipes_data.py` LIMITED: `["이지원","양문호",…]` → `["이시원","양문호",…]`

---

## 2군 — 같은 줄에서 부수적으로 드러난 등급 어긋남 (별도 판단 필요)

위 9줄을 재측정하면서 **주 결함이 아닌 등급 불일치**가 같이 보였다. 1군과 성격이 달라 분리했다 —
`RECIPE_AUDIT.md` §F와 같은 종류이고, `repoint_upper_recipes.py`의 선호 순서 휴리스틱이 원인으로 보인다.

### 대상 에셋이 이미 있어 바로 바꿀 수 있는 것

| 조합식 | 자리 | 지금 | 이미지 색 | 바꿀 후보 | GUID |
|---|---|---|---|---|---|
| `초월_조성진_AD` | `[2]` 구주호 | 전설적인 | 희귀함 | `희귀함_구주호` | `65e17b1f09e449dfbd05d14dffa49ddc` |
| `초월_유재헌_ADAP` | `[0]` 김민규 | 제한됨 | 전설적인 | `전설적인_김민규` | `689a12bcaf5f41f38f879d56be838a5d` |
| `히든_황정기` | 황정기 | 히든 | 특별함 | `특별함_황정기` | `c8cc05c122bc4c108a15f0429d37b4eb` |
| `히든_황정기` | 노태현 | 전설적인 | 특별함 | `특별함_노태현` | `18237fd4c0c64ed9a1ca9bb1a6660e98` |
| `제한_이유범` | `[3]` 박기찬 | 희귀함 | 특별함 | `특별함_박기찬` | `a4e10dda3f8442a8b5640ecdf09d99ee` |
| `영원_이지원` | 노태현 | 전설적인 | 특별함 | `특별함_노태현` | `18237fd4c0c64ed9a1ca9bb1a6660e98` |

> `히든_황정기`의 결과 유닛 자체가 `히든_황정기`인데 재료로도 `히든_황정기`가 들어간다.
> 이미지 색은 특별함이므로 `특별함_황정기`가 맞아 보인다 — **자기 자신을 재료로 삼는 순환도 같이 풀린다.**

### 대상 에셋이 없어서 못 바꾸는 것 (새로 만들어야 함 → 사장님 판단)

| 조합식 | 재료 | 지금 | 이미지 색 | 문제 |
|---|---|---|---|---|
| `영원_윤현모` | 장태영 | 희귀함 | **특별함** | `특별함_장태영` 없음 (`희귀함_장태영`만 있음) |
| `영원_윤현모` | 상붕카 | 안흔함 | **하늘색=히든** | `히든_상붕카` 없음. `RECIPE_AUDIT.md` §5의 "상붕카 등급" 미해결 건과 같은 문제 |
| `영원_이지원` | 이지원 | 특별함 | **희귀함** | `희귀함_이지원` 없음 |
| `영원_이지원` | 강재규 | 희귀함 | **초록** | 초록은 안흔함·흔함 **둘 다**를 뜻해서 색만으로는 못 고른다. 둘 다 에셋은 있음 |

---

## 3군 — 이번 목록에 **넣지 않은** 것

- **타입(AD/AP) 불일치 4건** (`초월_임장혁`·`초월_이태훈`·`초월_배성령`·`영원_문필환`) —
  에셋 파일명에 타입이 들어 있어(`초월_임장혁_AD`) 고치려면 **파일명·`.meta`·이를 참조하는 모든 GUID**까지
  건드려야 한다. 재료 수정과 성격이 완전히 다르므로 별도 작업으로 잡아야 한다.
- **`영원_문필환`의 박은석 `count: 2` 분리** — 남색의 정체가 확정돼야 한다(§⑦ 참고).
- **`RECIPE_AUDIT.md` §F의 나머지 등급 어긋남** — 위 9줄 밖의 줄들이라 여기 없다.

---

## 실행 후 확인할 것

- `python3 Tools/check_meta.py` (새 파일이 없으므로 개수만 그대로면 된다)
- `bash Tools/compile_check.sh` (코드 변경은 없지만 규칙대로 돌린다)
- 수정한 조합식이 참조하는 GUID가 전부 로스터에서 해결되는지 재확인
- `python3 Tools/audit_combine_table.py`를 다시 돌려 어긋남이 줄었는지 확인
