# 도움소 (2026-08-30 사용자 설명)

## 무엇인가

**각 플레이어의 레인 안에 있는 상점형 건물.** 클릭하면 광역 스킬을 쓸 수 있고,
스킬은 **마나**를 소모한다.

| 항목 | 내용 |
|---|---|
| 위치 | 플레이어별 레인 안 (4개, 플레이어당 1개) |
| 조작 | 건물을 클릭 → 스킬 목록 |
| 비용 | 마나 |
| 효과 | 자기 필드에 버프를 주거나 적에게 광역 피해 |

## 알려진 스킬 이름

불비 · 버스터콜 · 지진 · 해일 (그 외 추가 예정)

> 각 스킬의 피해량·범위·쿨다운·마나 비용은 **미정**. 사용자 확정 필요.

## 마나 수급

**자원 칸(뽑기 섬)의 남쪽 포탈**에서 위습을 넣어 회복한다.
원작 공식: `20 + (현재 라운드 × 1.5)`

## 현재 구현 상태

| 요소 | 상태 |
|---|---|
| `ResourceType.Mana` | ✅ 추가됨 (enum 맨 뒤 — 앞에 끼우면 저장된 값이 밀린다) |
| 마나 지급 포탈 | ✅ 자원 칸 남쪽, `20 + 라운드×2` |
| 도움소 건물 | ❌ 없음 |
| 스킬 시스템 | ❌ 없음 (`SkillData` 에셋도 0개) |
| 광역 피해 | ❌ 없음 — 지금 공격은 `UnitAttacker`의 단일 대상뿐 |

## 만들 때 필요한 것

1. **레인마다 건물 배치** — `LaneMarker`가 이미 레인 위치를 알고 있으므로 그걸 쓴다.
2. **클릭 → UI** — 하단 HUD의 명령 카드 12칸이 이 용도로 비어 있다.
3. **광역 피해** — 범위 안의 `EnemyDummy`를 모두 때리는 코드가 필요하다.
   지금은 단일 대상 공격만 있다.
4. **스킬 데이터** — `SkillData`에 마나 비용·범위·피해·쿨다운 필드가 있어야 한다.

## 미확정

- 스킬이 플레이어별인가, 팀 공유인가
- 도움소가 파괴될 수 있는가

---

## 원작 조사 (2026-09-01, 리서치담당)

> 표기: **[원작]** = 원문 직접 확인(URL 있음) / **[추정]** = 근거는 있으나 원문 미확인 / **[미기재]** = 못 찾음
> ⚠️ 신뢰도 유의: 아래 스킬 수치 대부분은 나무위키 자체가 아니라, 검색엔진이 다른 블로그·게시글을 요약해 준 결과다.
> 원문 페이지를 직접 열어 확인하려 했으나 두 곳 모두 접근 실패했다 (`ehvk123.egloos.com`은 SSL 핸드셰이크 실패,
> `ordsearch.net`은 403 차단). 그래서 수치는 전부 **[추정]**으로 표시한다 — 틀렸을 가능성을 염두에 두고 사용할 것.

### 사용자가 알려준 스킬명과 실제로 찾은 스킬명이 다르다

사용자 설명의 "불비·버스터콜·지진·해일" 중 **버스터콜과 지진(어쓰퀘이크)만 원문에서 확인**했다.
"불비"·"해일"이라는 정확한 이름은 나무위키·디시·검색 어디에서도 찾지 못했다. 가장 가까운 후보는
"폭우"(비 관련 광역기)와 "해적선충돌"(바다 관련 광역기)이지만, 이름 자체가 다르므로 동일 스킬이라고 단정할 수 없다.
→ 원작 버전(시즌1/시즌2/원랜디R 등)에 따라 스킬명이 개편됐을 가능성이 있다. **사용자 확인 필요.**

### 스킬 목록 (찾은 것)

| 스킬명 | 마나 비용 | 효과 | 근거 |
|---|---|---|---|
| 버스터콜 | 800 (검색엔진 요약) | 강력한 광역 피해 ("무시무시한 스킬") | [추정] — 원문 직접 미확인, egloos 블로그 "원랜디 도움소 잘 활용하기" 요약. 별도로 나무위키에 "버스터콜: 쿨타임/필요마나 30% 감소"(항법 적용 시) 문구는 직접 확인 [원작] (https://namu.wiki/w/원피스%20랜덤%20디펜스) — 이건 기본 수치가 아니라 항법 버프 수치 |
| 지진 (어쓰퀘이크) | 미기재 | 광역 피해. 항법 적용 시 "데미지 100만 증가" | [원작] 존재 자체와 항법 보너스 수치는 나무위키 원문 확인 (https://namu.wiki/w/원피스%20랜덤%20디펜스, "항법: 최고의 도움" 섹션). 기본 데미지 수치는 [미기재] |
| 해루석 | 미기재 | 지속 효과(구속?). 항법 적용 시 "지속시간 및 초당 추가 마법데미지 20% 증가" | [원작] 존재는 나무위키 원문 확인 (동일 섹션). 기본 수치는 [미기재]. ⚠️ 사용자가 말한 "해일"과 다른 스킬일 가능성 높음 |
| 폭우 | 15 (검색엔진 요약) | "적은 범위형" 광역 피해 | [추정] — egloos 블로그 요약, 원문 미확인 |
| 해적선충돌 | 75 (검색엔진 요약) | 해적선 낙하 피해 + 일정 시간 스턴 | [추정] — egloos 블로그 요약, 원문 미확인 |
| 출항이다! | 100 전후로 추정, 10초 지속 | 버프 계열로 추정(정확한 효과 불명) | [추정] — 검색엔진 요약 자체가 "15퍼 공업인가?"처럼 불확실하게 서술함. 신뢰도 낮음 |
| 흡수 | 30 (과거 50에서 하향) | 마나 회복 | [추정] — egloos 블로그 요약, 원문 미확인 |
| 마나포션 | 미기재 | 사용 시 마나 회복 (도움소에 "추가"되는 스킬이라는 서술) | [추정] — 검색엔진 요약, ordsearch.net 원문 접근 실패(403)로 미확인 |
| 연금술 | 마나 소모 | 희귀 등급 이하 유닛을 분해. 잘못된 대상에 쓰면 마나 환불("등가교환" 컨셉) | [추정] — 검색엔진 요약, ordsearch.net 원문 접근 실패(403)로 미확인 |

**쿨다운·범위**: 모든 스킬에서 **[미기재]** — 어떤 자료에서도 구체 수치를 찾지 못했다.

### 마나 상한

두 가지 다른 개념이 섞여서 언급된다 — 혼동 주의:
1. **도움소 자체의 마나 총량 상한**: "항법: 도움소 잠그기"라는 페널티형 항법을 선택하면 "도움소 최대 마나 100으로 감소"된다는 문장이 나무위키에 원문으로 있다 [원작] (https://namu.wiki/w/원피스%20랜덤%20디펜스). "감소"라는 표현으로 볼 때 **기본 상한은 100보다 크다** — 정확한 기본값은 [미기재].
2. **위습 1회로 채울 수 있는 마나량 상한**: 검색 결과 "원랜디R에서 위습 1개로 회복 가능한 마나의 양이 최대 100으로 고정(버프)"이라는 서술이 있다 [추정] — ordsearch.net 원문 미확인, 검색엔진 요약만 확보.

### 마나 회복 수단

자원 포탈(`20 + 라운드×1.5`, 기존 조사) 외에 도움소 안에 있는 것으로 추정되는 **흡수**·**마나포션** 스킬 자체가 마나를 회복시켜 준다는 서술을 찾았다 [추정, 원문 미확인]. 마나가 자원 포탈로만 채워지는 게 아니라 도움소 스킬로도 순환된다는 뜻인데, 이게 "도움소 스킬을 쓰려고 마나를 쓰는데 동시에 마나를 버는" 구조인지는 [미기재] — 앞뒤가 안 맞아 보이는 부분이라 원문 확인 전엔 신뢰하지 않는 게 낫다.

### 도움소 파괴 여부

여러 검색어로 시도했으나 **[미기재]**. "도움소가 공격받는다"거나 "파괴된다"는 서술은 어디서도 나오지 않았다. 오히려 도움소는 항법(패시브 버프 선택지)과 스킬 소모 UI에 가까운 서술만 나와서, **건물이 아니라 UI/메뉴에 가까운 시스템일 가능성**도 있다. 확실치 않으므로 판단은 사용자에게 넘긴다.

### 스킬이 개인용인가 팀 공유인가

**[미기재]**. 도움소가 "레인 안에 하나씩" 있다는 건 우리 쪽 기존 설명(SUPPORT_SHOP.md 상단)이고, 원작에서 이게 플레이어 개인 소유인지 4인 파티 공유 자원(마나 포함)인지 명시한 문장은 찾지 못했다. "마리당 100씩 차기 때문에 60라부터 다 모아두는게 아니면 마나가 부족하다"는 QnA성 문장을 하나 찾았으나(디시 갤러리) [추정, 신뢰도 낮음] 개인/팀 여부와는 무관한 내용이었다.

### 확인 못한 항목 정리 (`[미기재]`)

- 각 스킬의 정확한 범위·쿨다운 (전부)
- "불비"·"해일"이라는 정확한 스킬명 자체 (원작에 없거나 버전별 개칭 가능성)
- 지진(어쓰퀘이크)·해루석의 기본(비버프) 마나 비용·피해량
- 도움소 자체 마나 총량의 기본 상한값 (100은 "감소된" 값)
- 도움소가 파괴 가능한 건물인지, UI에 가까운 시스템인지
- 스킬/마나가 플레이어 개인 소유인지 4인 팀 공유인지


---

# 사용자 확정 (2026-09-01)

| 항목 | 확정 내용 |
|---|---|
| 스킬 구성 | **원작을 따른다** — 불비·해일은 원작에 없으므로 버린다 |
| 소유 | **플레이어 개인용.** 공유하지 않는다 |
| 효과 범위 | 스킬은 **맵 전체 어디에나** 쓸 수 있다. 자기 레인에 한정되지 않는다 |
| 파괴 | **불가능** |
| 수치 | 미정 — 조사 중 |

---

# 원작 맵 파일 직접 확인 (2026-09-04, 구현담당2)

> `Tools/w3x`로 `ORD11.089.w3x`의 `war3map.w3u`(유닛)·`war3map.w3a`(능력)·`war3map.j`(스크립트)를
> 직접 파싱했다. **웹 조사(위 2026-09-01 절)는 전부 [추정]이었는데, 이번엔 오브젝트 데이터 원본
> 수치를 그대로 읽었다 — 여기 있는 값이 위 웹조사 값보다 신뢰도가 훨씬 높다.**

## ⚠️ 뒤집힌 것부터 — "불비는 원작에 없다"는 확정이 틀렸다

**`war3map.w3u`에서 도움소 건물(`h08A`)의 능력 목록에 "9도움소 불비"(`A0IB`)가 실제로 있다.**
2026-09-01 확정 당시엔 "불비·해일이라는 이름을 어디서도 못 찾았다"는 게 근거였는데, 그건 **웹 자료가
부족했던 것뿐**이었다 — 맵 파일엔 있다. **사장님께 재확인이 필요하다**(아래 "새로 찾은 스킬" 참고).

## 도움소 건물 실체 확인

`h08A`("도움소") 유닛의 능력 12개: `A0QT(지진) A0JR(버스터콜) A0P2(낙뢰) AOeq(미상)
A0IE(독약) A0ID(해루석) A0IC(출항) A0IB(불비) A0B1(폭우) A0IA(선택위습제조) A0I9(흡수) Avul(미상)`.
`AOeq`/`Avul`은 커스텀 능력 테이블에 없다 — Blizzard 기본 능력을 안 고치고 그대로 쓴 것으로 보이고,
기본 게임 데이터가 없어서 정체를 못 밝혔다(유틸리티성 버프일 가능성 — 예: 무적).

## 스킬별 원작 수치 (전부 `war3map.w3a`에서 직접 읽음)

| 스킬 | 마나(Lv1→Lv2) | 쿨다운(Lv1→Lv2) | 지속 | 효과 | 우리 구현과 다른 점 |
|---|---|---|---|---|---|
| 버스터콜 | 500→333 | 100→66 | 1초 | **700만 고정 데미지** | 마나·쿨다운 다름(우리 800/90). Lv2는 항법 버프로 보임(마나·쿨다운 둘 다 ~33%↓, 어제 "30%감소" 웹조사와 일치) |
| 폭우 | 25(고정) | 5 | 3초 | **1웨이브 1600뎀, 총 3웨이브**(연속 타격) | 마나(15→25)·쿨다운(8→5) 다름. **원작은 다단 히트인데 우리는 즉발 1회** — 구조가 다르다 |
| **지진** | 200 | 40 | 3초 | **⚠️ 순수 스턴, 데미지 0** — "600범위 3초스턴(저항50%무시)" | **우리는 데미지 스킬(damageBase 60)로 만들었는데 원작엔 데미지가 아예 없다.** 아래 "적용" 참고 |
| 해루석 | 700→600 | 300(둘다) | 5→6초 | **첫 타격 대상 현재HP의 7%** + 초당 250만→300만 고정 DoT | 마나(120)·쿨다운(50)·지속(4초) 전부 원작과 크게 다르다(원작이 훨씬 무겁다). **7%는 %라서 옮길 수 있는데 우리 스키마엔 "%피해" 필드가 없다** |
| **출항이다** | 150 | 135 | 10초(일치) | **맵 전체 아군 "공격력" +50%** | **우리는 "공격속도"(1.3배)를 올리는데, 원작은 "공격력"이다 — 완전히 다른 스탯.** 우리 스키마에 공격력 배율 필드가 없다 |
| **흡수** | 100 | 4 | — | **⚠️ 대상 유닛을 한 방에 제거(즉사), 보스·스토리 유닛 제외** | **우리는 "피해+맞은 수만큼 마나 환급"으로 만들었는데, 원작은 즉사기다.** 완전히 다른 스킬 |
| 해적선충돌 | — | — | — | **[미기재]** | 도움소 능력 12개 어디에도 "해적선"·"충돌"·"낙하" 계열 이름이 없다 — **이 이름 자체가 이 버전 도움소엔 없을 수 있다** |
| 연금술 | — | — | — | **[미기재]** | `war3map.j`·`war3map.wts` 전체에 "연금술"·"등가교환" 문자열이 0건이다 |
| 마나포션 | — | — | — | **[미기재]** | 같은 방식으로 "마나포션" 문자열도 0건이다 |

> ⚠️ **에셋에 들어간 값은 전부 Lv1 기준이다.** 위 표의 "Lv1→Lv2"는 원작이 항법(패시브
> 강화 선택지)으로 스킬을 업그레이드하는 체계인데, 우리 게임엔 항법 시스템 자체가 없어서
> 지금은 Lv1 값만 옮겼다(마나·쿨다운·지속시간·데미지 전부). **나중에 항법 Lv2 개념을
> 구현하면 여기 적힌 모든 스킬의 값이 한 번 더 올라가야 한다** — 예를 들어 해루석의
> DoT를 지금 250만이 아니라 300만으로 고치는 식으로 개별 스킬만 손대면 안 되고,
> 항법 시스템 자체가 생길 때 이 표 전체를 다시 훑어야 한다.

## ③ 마나 회복 — 확정, 우리 공식과 다르다

**[파일 확인]** `Trig_Random_Mana_Actions`: 랜덤위습(`e0IX`)을 "Random_Mana" 구역에 넣으면
`도움소 마나 += 20 + 1.5 × udg_Level`. **SUPPORT_SHOP.md 상단에 이미 적혀 있던 "20 + 라운드×1.5"가
정확히 맞았다** — `udg_Level`이 라운드를 뜻하는지는 변수명만으로 100% 확정은 아니지만
(`set udg_Level=...`가 다른 트리거 값을 그대로 받아쓰는 코드라 직접 라운드 변수인지는 못 봤다),
공식 자체는 원문 그대로다.

**우리 현재 구현(`MapGenerator.cs`의 `ResourcePortal(..., ResourceType.Mana, 20, 2, 100f)`)은
`20 + 라운드×2`다 — 원작(`20 + 라운드×1.5`)과 다르다.** 포탈 방식(위습을 넣어야 회복)과 기본값(20)은
정확히 일치한다 — **배율만 2→1.5로 고치면 원작과 완전히 같아진다.**

## ④ 원작에 있는데 우리에게 없는 스킬 — 4개 발견

| 이름 | 마나(Lv) | 쿨다운(Lv) | 효과 |
|---|---|---|---|
| **선택위습제조** | 80 | 60 | 흔함 **선택** 위습 1기 제조. **최대 3번까지만**(횟수 제한 있는 스킬 — 우리 스키마엔 "총 사용 횟수 상한" 개념이 없다). 특성포인트 1 소모(`Rhse` 기술 요구) |
| **불비** | 210 | 60 | 1웨이브 5만뎀, 맞으면 초당 2.8만뎀 6초 지속, 총 4웨이브 — **폭우의 상위 호환처럼 보임**(둘 다 "웨이브" 다단히트 구조) |
| **낙뢰** | 100 | 17 | 단일 대상 22500뎀 + 2초 스턴(저항무시) — 광역이 아니라 **단일 대상 지정형**, 우리 9종 중 단일 대상 데미지 스킬이 없다 |
| **독약(Lv1→Lv2)** | 620 | 170(둘다) | 폭발 20만→30만 + 2차 20만→30만 + 방어력 20→25 감소, 5→7.5초 지속 |

## 우리 스키마(`SupportSkillData.cs`)가 못 담는 것 — 코드 변경 필요, 이번에 안 건드림

1. **% 기반 피해**(해루석의 "대상 HP 7%") — `damageBase`/`damagePerRound`는 고정값 공식뿐이다.
2. **공격력 배율 버프**(출항이다) — `buffAttackSpeedMultiplier`만 있고 공격력 배율 필드가 없다.
3. **순수 CC(무피해 스턴)** — `Damage` 이펙트가 "duration>0이면 스턴"을 이미 지원해서
   **데미지를 0으로 두면 코드 변경 없이 표현 가능**하다(아래 "적용한 것" 참고).
4. **즉사 이펙트**(흡수) — `SupportSkillEffect`에 즉사 케이스가 없다.
5. **레벨(Lv1→Lv2) 체계** — 우리 에셋은 스킬당 값이 하나뿐이라 원작의 "항법/업그레이드로 Lv2"
   개념을 못 담는다. 지금은 전부 Lv1 값을 기준으로 옮겼다.
6. **사용 횟수 상한**(선택위습제조의 "최대 3번") — 쿨다운 개념만 있고 총량 제한이 없다.

## 이번에 에셋에 반영한 것 / 안 한 것

아래 "적용" 항목만 `Assets/Data/SupportSkills/*.asset`에 직접 반영했다(별도 커밋). **피해량·범위는
전부 그대로 뒀다**(고정 수치라 옮기면 안 된다는 지시 그대로) — 위 표에서 효과 자체가 다르다고 표시한
스킬(지진·출항이다·흡수·해루석)은 **마나·쿨다운·지속시간만** 옮기고 **이펙트 자체는 안 건드렸다**
(그건 `SupportSkillEffect` enum에 새 케이스가 필요한 코드 변경이라 이번 범위 밖으로 판단).

**지진만 예외로, 데미지를 0으로 낮춰서 "순수 스턴"에 가깝게 만들었다** — 우리 `Damage` 이펙트가
이미 "duration>0이면 스턴"을 지원해서 **코드 변경 없이** 원작 동작(피해 없는 3초 스턴)을 그대로
표현할 수 있었기 때문이다. 이건 순수 수치 이상의 변경이라 커밋 메시지에 따로 적는다.

# range(`radius`) 값 — 원작에 없어 발명한 것 (2026-09-05, 구현담당2)

도움소 스킬의 `radius` 필드 6개는 **전부 원작에 대응 근거가 없다.** 원작 능력 데이터
(`war3map.w3a`)에 범위(`aare`) 필드 자체가 없거나(위 "스킬별 원작 수치" 표, 전 스킬 공통
[미기재]) 우리가 직접 못 읽은 값이라, 아래는 전부 **밸런스 감으로 지어낸 수치**다 —
피해량·마나·쿨다운처럼 원작 원문과 대조해서 고칠 수 있는 값이 아니라, **사장님이 실제로
플레이해보고 잡아야 하는 값**이라는 뜻이다.

| 스킬 | `radius` | 근거 |
|---|---|---|
| 버스터콜 | 25 | 없음 — 발명값 |
| 폭우 | 10 | 없음 — 발명값 |
| 지진 | 15 | 없음 — 발명값 |
| 해루석 | 12 | 없음 — 발명값 |
| 불비 | 20 | 없음 — 발명값 |
| 독약 | 12 | 없음 — 발명값(`c3fd1ea` 커밋 메시지에 이미 명시돼 있던 것) |

`출항이다`는 `mapWide=1`이라 `radius`가 애초에 안 쓰이고, `흡수`는 단일 대상 지정형이라
범위 자체가 없다(둘 다 표에서 제외).
