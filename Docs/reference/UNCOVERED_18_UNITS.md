# 커버리지 갭 18기 — 전부 풀렸다. 밀도 최종값

조사: 리서치담당 / 2026-09-06
대상: 「평타 트리거가 `udg_HashAttack`에 등록됐는데 우리 표엔 한 행도 없던」 19기 중
`H08Z`(샹크스, 019 특성에서 이미 해결)를 뺀 **18기**

전체 표: `UNCOVERED_18_UNITS.csv`

## 결과 — **17기에서 피해를 찾았다. 새 스킬 36개**

| 등급 | 새 스킬 |
|---|---|
| 초월함 | 15 |
| 전설적인 | 6 |
| 불멸의 | 5 |
| 특별함 | 4 |
| 제한됨 | 3 |
| 희귀함 | 2 |
| 변화된 | 1 |

**`H091`(밀짚모자 해적단 의사·쵸파) 1기만 진짜로 0이다.** `Trig_Chopa_Attack`은
`MANA == 150.00`에서 `IssueImmediateOrderById("metamorphosis")` — **변신만 한다.**
피해는 변신 후 `H093`(슈퍼 탱크) 쪽에 있다. 정당한 0행이다.

## 큰 것들

| 유닛 | 등급 | 값 |
|---|---|---|
| `H0B3` 루피-블랙맘바 | 초월함 | 킹코브라 **3,500,000** × (1 + 0.01×이동속도×0.10) 배수 1.00~1.50 MAGIC+UNIVERSAL · 단일 **1,875,000** · 블랙맘바 **112,500** ×(1+0.01×이동속도×0.30) 배수 1.00~2.00 HERO+NORMAL ×2 · 파이선 **350,000** ×(1+0.01×이동속도×0.40) · `e0L3` firebolt |
| `H08U` 시라호시 | 초월함 | **3,350,000 + (최대체력−현재체력)×0.06** · 오더 소환 5종(`e04S` frostarmor · `e04V`/`e0BD`/`e0KA` stomp · `e0PF` stampede) |
| `h0BI` 부유물1 | 불멸의 | `UnitDamagePointLoc` **30,000 + 받은피해×0.75** (400범위) · **250,000** (400범위) |
| `h086` 시노부(제한됨-소환) | 제한됨 | **300,000** · 최대체력×0.01 · 최대체력×0.04×(0.20+0.05×`A11S`레벨) |
| `h085` 시노부(전설-소환) | 전설적인 | **200,000** · 최대체력×0.01 배수 0.33 · 최대체력×0.02×(0.20+0.05×`A11S`레벨) |
| `H093` 슈퍼 탱크 | 초월함 | 받은피해 그대로(`GetEventDamage()`) CHAOS+NORMAL · **250,000** MAGIC+UNIVERSAL |
| `h08H` 삭풍 | 불멸의 | (**25,000** + 최대체력×0.02) × (0.20+0.05×`A11S`레벨) · (**15,000** + 최대체력×0.012) × 동일 |
| `H09C` 우솝 10톤해머 | 초월함 | **최대체력 × 0.035** 배수 1.00~1.75 PIERCE+NORMAL |
| `h07F` 시키 전설돌떨구기 | 변화된 | **105,000** CHAOS+NORMAL |
| `h07R` 시키의 함대 | 불멸의 | `UnitDamagePointLoc(400범위, 받은피해 그대로)` — ⚠️ **평타 200,000은 025 특성(`A0T4`)에서 이미 셌다. 중복 계상 금지** |
| `h012` 스쿼드 | 특별함 | `UnitDamageTargetBJ` **5,000** · **3,000** · `e0J1` magicleash |
| `h014` 봉쿠레 | 특별함 | `1/8` → `h063` 소환. **오더 없이 스스로 공격한다**(플레이어 소유·이동 522·사거리 800). 평타 **475** 쿨 0.30, HP 2/재생 −1 → 약 2초 생존 |
| `h01Z` 마젤란 | 희귀함 | `1/10` → `e058` 소환. `A09V`(base **`Apxf`** = 자동 발사 패시브) **1,250 피해 · 범위 1,000 · 쿨 0.09**, 약 3초 생존 |
| `h027` 슈가 | 희귀함 | `h06O` **selfdestruct** (장난감희귀함) |
| `h037` 슈가 | 전설적인 | `h06P` **selfdestruct** (장난감) |
| `h0AH` 킹 | 전설적인 | `e0NK` **acidbomb** |
| `h02W` 드래곤 | 전설적인 | `1/10` → `e007` **stomp** |

## 이번에 고친 내 도구 버그 3건

이 조사를 처음 돌렸을 때 4기가 「피해 0」으로 나왔는데, 전부 **내 추출기의 갭**이었다.

| 갭 | 증상 | 고침 |
|---|---|---|
| **함수 접두사 경계 없음** | `Trig_Legend3`이 `Trig_Legend30`·`Trig_Legend33`까지 먹어 드래곤에 시노부·토키·킹·모리아 피해가 딸려 들어왔다 | 접미사를 `^(Actions|Conditions|Func\w*)$`로 앵커 |
| **피해 함수를 `RRD`만 봄** | `h07R`·`h0BI`가 `UnitDamagePointLoc`을 쓰는데 0으로 나왔다 | `UnitDamageTarget(BJ)?` · `UnitDamagePoint(Loc)?` 추가 |
| **소환 함수를 3종만 봄** | `h02W`가 `CreateUnitAtLoc`을 쓰는데 0으로 나왔다 | `CreateUnitAtLoc`·`CreateUnitAtLocSaveLast` 추가. 또 `ForGroup(..., function XXX)` 콜백을 안 따라가 `h07F`가 0이었다 → 콜백 이름 추적 추가 |

**「0건」이 나오면 먼저 내 그물을 의심한다** — 오늘만 네 번째다.

---

# ★ 원작 스킬 밀도 — 최종값 (18기 반영)

분모 정의는 그대로: **309기** = `h*`/`H*` · 등급 라벨 보유 · `Aloc` 없음.
분자: 기존 3표 + `DUMMY_CHANNEL_MISSING`(79행) + **`UNCOVERED_18_UNITS`(36개)**.
「겹치는 능력 대표화」 기준(한 스킬 = 한 이름의 능력/트리거).

| | 18기 제외(직전 보고) | **★ 18기 포함(최종)** |
|---|---|---|
| 스킬 보유 유닛 | 159 / 309 (51.5%) | **176 / 309 (57.0%)** |
| 총 스킬 | 394 | **430** |
| **보유 유닛당 평균** | 2.48 | **2.44** |
| **전체 로스터 평균** | 1.28 | **1.39** |

> 📌 **보유당 평균이 2.48 → 2.44로 거의 안 움직였다.** 새로 푼 18기의 밀도(36/17 ≈ 2.1)가
> 기존 평균과 비슷했다는 뜻이고, **「보유 유닛당 2.4~2.5」가 안정적인 값**이라는 근거가 된다.
> 반면 전체 로스터 평균은 1.28 → 1.39로 **8.6% 올랐다** — 그물이 넓어지면 계속 오르는 값이라 하한 성격이 강하다.

## 등급별 (최종)

| 등급 | 로스터 | 보유 | 스킬 | 보유당 | **등급평균** |
|---|---|---|---|---|---|
| 흔함 | 32 | 0 | 0 | 0.00 | **0.00** |
| 안흔함 | 14 | 2 | 4 | 2.00 | 0.29 |
| 특별함 | 33 | 11 | 18 | 1.64 | 0.55 |
| 희귀함 | 46 | 32 | 50 | 1.56 | 1.09 |
| 전설적인 | 43 | 36 | 72 | 2.00 | **1.67** |
| 변화된 | 9 | 7 | 13 | 1.86 | 1.44 |
| 제한됨 | 12 | 11 | 29 | 2.64 | **2.42** |
| 초월함 | 64 | 34 | 103 | 3.03 | **1.61** |
| 영원한 | 9 | 9 | 40 | 4.44 | **4.44** |
| 불멸의 | 18 | 15 | 54 | 3.60 | **3.00** |
| 랜덤전용 | 29 | 19 | 47 | 2.47 | 1.62 |

**등급평균 기준 「심한 다섯」: 영원한 4.44 · 불멸의 3.00 · 제한됨 2.42 · 전설적인 1.67 · 랜덤전용 1.62**
(직전 보고에서 초월함 1.38 → **1.61**로 올라 랜덤전용과 거의 붙었다. 5위 자리가 바뀔 수 있으니 확정 전에 봐주시길.)

## 남은 불확실성 — 이제 훨씬 작다

`udg_HashAttack`에 등록됐는데 우리 표에 0행인 유닛은 **이제 없다**(`H091`은 정당한 0).
남은 경로는 **`uabi`의 w3a 필드만으로 효과를 내는 유닛**과 **아직 안 판 채널**이다.
전자는 기존 표가 덮고 있고, 후자는 오늘 다섯 개(1채널·2채널·절대쿨·`HashAttack`·불리언)를 다 열었다.
**「430개·176기」를 02번의 분모로 써도 된다고 본다.**

---

# 🔴 정정 — 79행 중 **43행은 피해 행이 아니었다** (같은 날, 뒤이어 발견)

③ 「추정 20」 정밀화를 하다가 더 큰 것이 나왔다.
**79행 전체에 「오더가 실제로 내려가는가」를 검사**했더니:

| | 행 |
|---|---|
| **작동** (오더 있음 33 + 패시브/오라라 오더 불필요 3) | **36** |
| **작동 안 함 — 순수 시각 이펙트** | **43** |

## 근거 — 표본 5건을 코드로 직접 확인했다

더미는 오더 없이 생성되고, **피해는 바로 옆의 `RRD`/`ForGroupBJ(…RRD…)`가 낸다.**

| 더미 | 코드 |
|---|---|
| `e056` (Z, `Ucs1`=1,000,000) | `CreateNUnitsAtLoc('e056',…)` ×2 → 오더 없음 → **`RRD(…, 1000000.00, 1.00,1.00, NORMAL,UNIVERSAL)`** |
| `e0FU`·`e0FR` (핸콕 애로우, `Ucs1`=400,000) | 생성 → `SetUnitTimeScale` → **`RRD(…, 250000.00, 1.00,1.5, …)`** |
| `e0GP` (로우 라디오, `Htc1`=350,000) | 생성 → `SetUnitTimeScale(1.70)` → **`ForGroupBJ(…, Trig_Law_skill_5_Func006Func005A)`** (안에 RRD) |
| `e09I` (나미, `Hbz2`=500,000) | 생성 → **`ForGroupBJ(…, Trig_Nami_Skill_4_Func018A)`** = RRD 350,000 |
| `e0PP` (미호크 먼지, `Ucs1`=750,000) | 생성 → **`s__TrigVariables_Flush`** — 아무 오더도 없다. 순수 연출 |

「작동」쪽도 표본을 봤다: `e03T`·`e0IW` 둘 다 **`IssueImmediateOrderById(…, "thunderclap")`**가 명시돼 있다.

## 뜻 — **이 43행을 넣으면 이중 계상이다**

그 피해는 **이미 `ORIGINAL_UNLISTED_SKILL_EFFECTS.csv`의 RRD 행으로 세어져 있다.**
더미의 `w3a` 필드값(`Ucs1`/`Htc1`/`Hbz2`…)은 **한 번도 시전되지 않는 죽은 값**이다.

내가 앞서 「더미 표 77기가 빠져 있다」고 한 것은 **표에 없던 것은 맞지만, 그중 43행은 애초에 넣으면 안 되는 행**이었다.
**「빠져 있다」와 「넣어야 한다」는 다른 말이다.**

가장 위험했던 것: PM이 지목한 큰 값들이 대부분 이쪽이다 —
`e056`·`e0GH` Z **1,000,000** · 미호크 먼지 **750,000** ×3 · 루치 육왕권 **600,000** ×4 ·
핸콕 애로우 **400,000** ×5 · 아오키지 아이스볼 **500,000** ×2 · 로우 라디오 **350,000** ×3.
**「1,000,000짜리가 매 타로 도는 게 가장 큰 위험」이라 하셨는데, 실은 아예 안 돕니다.**

## 게이트 확신도도 다시 냈다

| | 행 |
|---|---|
| **확정** | **36** (작동하는 행 전부) |
| 해당없음 — 실제로 안 돈다 | 43 |
| 추정 | **0** |

→ **「추정 20」은 0이 됐다.** 20행 중 19행은 로컬 조건이 구체적이라(확률/마나/체력/버프/스테이지) 확정으로 올렸고,
나머지는 실제로 안 도는 행으로 재분류됐다.

## ★★ 밀도 최종 재계산 (43행 제외)

| | 값 |
|---|---|
| 스킬 보유 유닛 | **176 / 309 (57.0%)** |
| 총 스킬 | **412** |
| **보유 유닛당 평균** | **2.34** |
| **전체 로스터 평균** | **1.33** |

세 판의 추이 — **보유당 평균이 2.48 → 2.44 → 2.34로 계속 좁은 범위에 머문다.**
그물을 넓혀도(18기 +36) 좁혀도(연출 −43) 값이 안 흔들린다. **「보유 유닛당 2.3~2.5」로 써도 된다.**

| 등급 | 로스터 | 보유 | 스킬 | 보유당 | **등급평균** |
|---|---|---|---|---|---|
| 흔함 | 32 | 0 | 0 | 0.00 | 0.00 |
| 안흔함 | 14 | 2 | 4 | 2.00 | 0.29 |
| 특별함 | 33 | 11 | 18 | 1.64 | 0.55 |
| 희귀함 | 46 | 32 | 50 | 1.56 | 1.09 |
| 전설적인 | 43 | 36 | 72 | 2.00 | **1.67** |
| 변화된 | 9 | 7 | 11 | 1.57 | 1.22 |
| 제한됨 | 12 | 11 | 29 | 2.64 | **2.42** |
| 초월함 | 64 | 34 | 89 | 2.62 | 1.39 |
| 영원한 | 9 | 9 | 39 | 4.33 | **4.33** |
| 불멸의 | 18 | 15 | 53 | 3.53 | **2.94** |
| 랜덤전용 | 29 | 19 | 47 | 2.47 | **1.62** |

**「심한 다섯」: 영원한 4.33 · 불멸의 2.94 · 제한됨 2.42 · 전설적인 1.67 · 랜덤전용 1.62**
(초월함은 1.39로 6위 — 43행 제외로 다시 내려갔다. 5위 자리는 랜덤전용 1.62 vs 초월함 1.39로 벌어졌다.)


---

# ★★★ 밀도 최종 확정판 (2026-09-06, 오더 재검증 + `Nbr1`/`e055` 정정 반영)

분자에서 뺀 것: `DUMMY_CHANNEL_MISSING` 79행 중 **작동 31행만 채택**(미작동 44 · 피해아님 4 제외),
`DUMMY_CHANNEL_GATES` 52행에서 **`e055`(가반 공증 750,000 — 공격력 증가라 피해 아님) 1행 제외**.

| | 값 |
|---|---|
| 스킬 보유 유닛 | **176 / 309 (57.0%)** |
| 총 스킬 | **406** |
| **▶ 보유 유닛당 평균** | **2.31** |
| **▶ 전체 로스터 평균** | **1.31** |

**네 판의 추이: 2.48 → 2.44 → 2.34 → 2.31.** 그물을 넓히든(18기 +36) 좁히든(연출 −48) 값이 안 흔들린다.
**「보유 유닛당 2.3」으로 확정해 써도 된다.**

| 등급 | 로스터 | 보유 | 스킬 | 보유당 | **등급평균** |
|---|---|---|---|---|---|
| 흔함 | 32 | 0 | 0 | 0.00 | 0.00 |
| 안흔함 | 14 | 2 | 4 | 2.00 | 0.29 |
| 특별함 | 33 | 11 | 18 | 1.64 | 0.55 |
| 희귀함 | 46 | 32 | 50 | 1.56 | 1.09 |
| 전설적인 | 43 | 36 | 71 | 1.97 | **1.65** |
| 변화된 | 9 | 7 | 11 | 1.57 | 1.22 |
| 제한됨 | 12 | 11 | 29 | 2.64 | **2.42** |
| 초월함 | 64 | 34 | 85 | 2.50 | 1.33 |
| 영원한 | 9 | 9 | 39 | 4.33 | **4.33** |
| 불멸의 | 18 | 15 | 52 | 3.47 | **2.89** |
| 랜덤전용 | 29 | 19 | 47 | 2.47 | **1.62** |

> 📌 이 표는 **「원작」 한쪽 열**로만 쓴다. 우리와의 **차이**로 「심하게 어긋난 등급」을 내는 건
> 우리 쪽을 같은 규칙(한 스킬 = 한 능력)으로 다시 센 뒤에야 가능하고, 그건 구현담당1 몫이다.
