# 남은 헐거움의 출처 — **「매 타 발동」 29개가 발동률의 74%다** (2026-09-06, 리서치담당)

> **한 줄 결론**: 제한됨·영원한·불멸의 세 등급 스킬 83개 중 **「매 타(`triggerChance ≥ 1.0`)」 18개가 발동률 총합의 74%**를 만든다.
> 다섯 등급으로 넓히면 **29개**이고, 그중 **13개는 원작도 「게이트 없음」이라 정당**하다. **남은 16개가 구멍**이다.
>
> 표: `EVERY_HIT_SKILLS.csv`

---

## ① 유형별 기여 — 게이지형은 사실상 안 나간다

제한됨·영원한·불멸의 스킬 **83개**:

| 유형 | 개수 | 발동률 총합 | 비중 |
|---|---|---|---|
| **매 타(1.0)** | **18** | **27.41** | **74%** |
| 확률형 | 46 | 9.82 | 26% |
| 게이지형 | 19 | 0.20 | **0.5%** |

**게이지형 19개가 다 합쳐 0.2**다. 임계값이 100~150이라 초당 0.01 수준이다.
**헐거움은 확률형에서 오지 않는다 — 매 타 18개가 거의 전부다.**

## ② 🟢 그중 13개는 **정당하다**

파일명에 **`355f5d39`** 접미사가 붙은 14개를 열어보니 `skillName`이 전부 이렇다:

```
쿠치키 뱌쿠야 - 랜덤전용[제한됨] — (게이트 없음)
토트 무지카 봉인된 악보의 괴물 — (게이트 없음)
마르코 환수종'불사조' - 전설적인 — (게이트 없음)
킹 삼재해:화재 - 제한됨 — (게이트 없음)
…
```

**`355f5d39`는 「(게이트 없음)」의 해시**다. **원작이 무조건 발동이므로 우리 `1.0`이 맞다.**
(내 확정표에 `(게이트 없음)` 35행이 있다 — 그 묶음이다.)

여기에 이미 확인된 **홍인창·김영원 꼴**을 더해 **13개가 정당**이다.

→ **「`triggerChance=1.0`이면 무조건 틀렸다」가 아니라는 게 또 확인됐다.** 29개 중 13개(45%)가 맞는 값이다.

## ③ 🔴 남은 16개 — **여기가 구멍이다**

| 등급 | 유닛 | 발동률 | raw | 파일 |
|---|---|---|---|---|
| 불멸의 | 정준영 | **4.167** | 1,250,002 | `원작024_h04D` |
| 전설적인 | 정준영 | 2.632 | 150,000 | `더미채널_전설적인_정준영_2` |
| 영원한 | 윤현모 | 1.724 | 1,270,000 | `더미채널_영원_윤현모_1` |
| 불멸의 | 정윤식 | 1.724 | 92,500 | `원작능력_불멸_정윤식` |
| 불멸의 | 신지우 | 1.667 ×3 | 4,500,012 / 2,250,004 / 70,000 | `카이도_…_skill18` · `…_buster` · `더미채널_…_1` |
| 제한됨 | 이충민 | 1.639 | 1,062,504 | `원작능력_제한_이충민` |
| 초월함 | 강주혁_AP | 1.589 | 350,000 | `절대쿨_초월_강주혁_AP` |
| 제한됨 | 김민규 | 1.492 | 150,006 | `원작능력_제한_김민규` |
| 불멸의 | 고도현 | 1.370 | 5,349,993 | `원작능력_불멸_고도현` |
| 제한됨 | 강보명 | 1.266 | 1,102,500 | `원작능력_제한_강보명` |
| 영원한 | 김영원 | 1.220 | 500,016 | `원작능력_영원_김영원` |
| 영원한 | 조세민 | 1.075 | 500,008 | `원작능력_영원_조세민` |
| 불멸의 | 이승우 | 0.870 | 450,005 | `원작능력_불멸_이승우` |
| 불멸의 | 박은석 | 0.444 | 50,000 | `원작능력_불멸_박은석` |

**채널별 성격:**
- **`더미채널_*` 3개** — 진입 게이트가 **`DUMMY_CHANNEL_GATES.csv`에 이미 있다.** 대조하면 바로 잡힌다
- **`절대쿨_*` 1개** — `ORIGINAL_GATED_SKILLS_14.csv` 대조
- **`카이도_*` 2개** — `Kaido_Skill_1_8`·`Kaido_Dragon_buster`의 게이트를 내가 이미 냈다(`fb03073` 이전 조사)
- **`원작능력_*` 8개 · `원작024_*` 1개** — 게이트 출처 불명. **여기가 진짜 미지다**

**정준영(불멸) 4.167이 단독 최대**다 — 이 하나가 불멸의 발동률 합의 상당 부분을 차지한다.

## ④ 🔴 조사 중 잡은 내 파서 버그 (뿌리 ⑲ 자기 적용 3)

처음 계산에서 **`불멸_고도현`의 `triggerChance`를 9.50(950%)으로 읽었다.**
원본은 **`9.5e-05`(0.000095)** — **과학 표기법**인데 내 정규식 `([-\d.]+)`가 `9.5`만 잡고 `e-05`를 버렸다.

**이건 이전 세션에서 이미 한 번 물린 함정이다**(`bonus`의 `2e+06` 56행).
**같은 함정에 두 번 걸렸다** — 숫자 정규식을 쓸 때마다 `[eE][-+]?\d+`를 붙여야 한다.

고친 뒤 재계산: 중앙값은 대부분 그대로였고 **불멸의 B만 2.6643 → 1.6359로 내려갔다**(A는 불변 — 가중평균이라 raw가 작은 스킬의 영향이 작다).

## ⑤ 다음 작업

1. **`더미채널_*` 3개 + `카이도_*` 2개 + `절대쿨_*` 1개** — **이미 있는 표와 대조만 하면 된다.** 제일 싸다
2. **`원작능력_*` 8개 + `원작024_*` 1개** — 각 파일의 `description`에서 원작 트리거를 찾아 게이트를 새로 파야 한다
3. **정준영(불멸) 4.167부터** — 단독 기여가 가장 크다

⚠️ **`355f5d39`(게이트 없음) 13개는 건드리지 말 것.** 원작 그대로다.

---

# 추가 — 「싼 6개」 대조 완료 + 🔴 내 모형이 **쿨다운을 안 봤다** (2026-09-06)

## ① 6개 판정 → `GATE_FIX_6.csv`

| 유닛 | 원작 게이트 | 권장 | 판정 |
|---|---|---|---|
| **전설적인_정준영** | 버프 `B00B` 미보유 = **5절대쿨 징베 7.0초** | **`cooldown = 7.0`** (2.632 → **0.143**) | 🔴 고칠 것 |
| **영원_윤현모** | 버프 `B017` 미보유 = **5절대쿨 에이스영원 4.25초** | **`cooldown = 4.25`** (1.724 → **0.235**) | 🔴 고칠 것 |
| 불멸_신지우 (더미 `e0H5`) | 평타 트리거는 확정, stage0→4 분기 미확인 | `[미확인]` | ⚪ 유지 |
| 불멸_신지우 (카이도 buster) | `Kaido_Skill_int >= 10` — **단조 증가 카운터** | 1.0 유지 | ⭕ 정당 |
| 불멸_신지우 (카이도 skill18) | 경로A(`>=11`) / 경로B(1/7) 배타 | 1.0 유지 | ⭕ 정당 |
| 초월_강주혁_AP | 버프 `B012` 미보유 = **5절대쿨 나미 3.5초** | **이미 `cooldown = 3.5`** (실제 **0.286**) | ⭕ 정당 |

**둘은 `ORIGINAL_ABSOLUTE_COOLDOWN.csv`에 이미 값이 있었다** — `B00B` 7.0 · `B017` 4.25 · `B012` 3.5.
「버프 미보유」로 축 밖에 뺐던 조건이 **사실은 절대쿨이었고, 우리 `SkillLevel.cooldown`에 그대로 담긴다.**

## ② 🔴 내 발동률 모형이 **`cooldown`을 빠뜨렸다** (뿌리 ⑲ 자기 적용 4)

`UnitAttacker.cs:444`:
```csharp
if (level.cooldown > 0f && Time.time < state.onHitChanceLockedUntil) continue;
```
**`OnHitChance`에도 `cooldown`이 절대쿨로 걸린다.** 내 모형은 `triggerChance × attackSpeed`만 썼다.

올바른 식: **`rate = min(triggerChance × 초당타수, 1 / cooldown)`**

### 게다가 쿨다운 스캔도 처음엔 틀렸다

처음 세었을 때 **`cooldown > 0`이 1개**로 나왔는데 **실제로는 4개**다.
`levels:` 블록을 `\n  - `로 자른 뒤 첫 조각에서 찾았는데, 그 방식이 파일에 따라 첫 항목을 놓쳤다.
`^  - cooldown:`(줄 시작 앵커)로 바꾸니 4개가 나왔다.

| 스킬 | cooldown | 내가 잰 값 | 실제 |
|---|---|---|---|
| `절대쿨_랜덤_한마_바키` | 14.00 | — | **0.071** |
| `원작024_h04D`(불멸 정준영) | 8.50 | **4.167** | **0.118** |
| `절대쿨_초월_강주혁_AP` | 3.50 | 1.589 | **0.286** |
| `절대쿨_불멸_정윤식` | 4.75 | (chance 0.02) | 0.021 |

**「불멸 정준영 4.167이 단독 최대」는 내 오류였다.** 실제는 0.118이다.

### 다만 등급 중앙값은 거의 안 바뀐다

쿨다운이 걸린 스킬이 4개뿐이라 중앙값에 영향이 없다 — **불멸의 B만 1.6359 → 1.5515**.
**A 값(전설 0.2717 · 제한 0.5203 · 초월 0.0305 · 영원 0.1948 · 불멸 0.1297)은 그대로다.**

→ **격차의 원인은 쿨다운이 아니다.** 남은 `원작능력_*` 9개가 핵심이다.

## ③ 남은 것

**13개**(16개에서 6개 처리, 3개는 이번에 정당/미확인으로 닫힘 → 실제 남은 고칠 대상):
- `원작능력_*` 8개 + `원작024_*` 1개 — **게이트 출처 불명. 새로 파야 한다**
- 정준영(h04D)은 쿨다운 8.5가 이미 있으므로 **우선순위가 내려간다**

**다음 최대 기여자**(쿨다운 반영 후): 영원_윤현모 1.724(→ 이번에 4.25초로 해소) 다음은 **불멸_정윤식 1.724 · 제한_이충민 1.639 · 제한_김민규 1.492**다.

---

# 추가 — `원작능력_*` 10개. **전부 `ACbh`(Bash)였고 확률이 능력 필드에 있었다** (2026-09-06)

## ① 이 열 개는 1채널(능력ID)이고 **전부 `ACbh` 기반**이다

`A0FS` · `A0EM` · `A15N` · `A0QQ` · `A0GK` · `A0G3` · `A0J7` · `A044` · `A05P` — **아홉 능력 전부 base가 `ACbh`**(Bash, 강타)다.

**게이트는 트리거가 아니라 능력 자신의 `w3a` 필드에 있었다.** 부모 트리거를 파는 방향이 아니었다.

### `Hbh1` = 발동 확률(%) — 맵 안에서 검증

`ACbh` 기반 능력 **469개** 전수의 필드 분포:

| 필드 | 명시 | 범위 | 흔한 값 |
|---|---|---|---|
| **`Hbh1`** | 449 | **0.00 ~ 100.00** | 0(291) · **20(79)** · 25(12) · **100(9)** · 10(8) · 30(6) |
| `Hbh2` | 54 | 1.00 ~ 50.00 | 2 · 10 · 5 · 3 |
| `Hbh3` | 469 | 0 ~ 3,333,333 | 0(384) · 500,000(7) · 150,000(7) |

**`Hbh1`이 0~100에 갇혀 있고 20·25·100처럼 깔끔한 백분율만 나온다** → **확률(%)로 확정.**
`Hbh3`는 우리가 이미 피해로 옮긴 값이라 **`Hbh1`이 그 짝인 발동 확률**이라는 것과 아귀가 맞는다.

## ② 판정 → `GATE_FIX_9.csv`

| 유닛 | 능력 | `Hbh1` | 권장 | 판정 |
|---|---|---|---|---|
| **불멸_이승우** | `A044` 패기의 주먹 | **20.0** | **`triggerChance = 0.20`** (0.870 → **0.174**) | 🔴 **고칠 것** |
| 제한_김민규 | `A0QQ` | **100.0** | 1.0 유지 | ⭕ 정당 |
| 제한_강보명 | `A0GK` | **100.0** | 1.0 유지 | ⭕ 정당 |
| 영원_김영원 | `A0G3` | **100.0** | 1.0 유지 | ⭕ 정당 |
| 불멸_박은석 | `A05P` | **100.0** | 1.0 유지 | ⭕ 정당 |
| 불멸_정준영 | `A0FS` | 0.0 (`Hbh3`도 0) | — | ⚪ **효과 자체가 비어 있다** |
| 불멸_정윤식 · 불멸_고도현 | `A0EM` | **미명시** | `[미확인]` | ⚪ base 상속 |
| 제한_이충민 | `A15N` | **미명시** | `[미확인]` | ⚪ base 상속 |
| 영원_조세민 | `A0J7` | **미명시** | `[미확인]` | ⚪ base 상속 |

**고칠 것은 1개**다. **넷은 원작이 100%라 우리 `1.0`이 맞다.**

## ③ `[미확인]` 4개 — 왜 못 푸는가

`A0EM`·`A15N`·`A0J7`은 **`Hbh1` 필드 자체가 없다** = 스톡 `ACbh`의 기본값을 상속한다.
**그 기본값은 맵 파일에 없다**(`war3map.w3a`는 덮어쓴 필드만 담는다) — **맵 밖 자료가 필요하다** `[미확인]`.

⚠️ 「`Hbh1` 명시분 중 20.0이 가장 흔하니(79개) 기본값도 20일 것」은 **추론이지 근거가 아니다.** 적지 않는다.

## ④ 이번 조사의 교훈

**「게이트는 트리거에 있다」는 전제가 이번엔 틀렸다.** 이 열 개는 1채널이라 **게이트가 능력의 데이터 필드**에 있었다.
부모 트리거를 아무리 파도 안 나온다 — **채널이 다르면 게이트가 있는 자리도 다르다.**

**남은 매 타 구멍**: 이번 1(이승우) + 앞서 2(정준영·윤현모) = **3건**이 고칠 대상이고,
나머지는 **정당 8건 · 미확인 5건**으로 닫힌다.
