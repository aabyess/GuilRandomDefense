# 유형② 더미채널 다목적지 감사 — 최종 보고 (53개 트리거, 구현담당2 목록 41 + 자체 30그룹 중 미중복 12 완료)

조사: 리서치담당 / 2026-09-06
전제: 구현담당2가 재식별한 유형②(더미채널 다목적지) 56개 파일 중 트리거명이 확인된
41개 + 리서치담당이 `DUMMY_CHANNEL_GATES/MISSING.csv`에서 독자적으로 뽑은 30개
그룹(그중 41개와 안 겹치는 12개, 나머지는 앞서 유형①/①-콜백 작업에서 이미
개별 확인됨: `Ruffy_Attack`·`Z_skill_3`·`Tichi_skill_2_tr`·`Tichi_skill_4`·
`Tichi_skill_4_tr`·`DP_Attack_gaksung`·`Brook_Skill_1`·`Nami_Skill_4`·`Rebeca_Skill_1`
— 전부 별개로 확정, 아래 🔴 표 참고) 를 유형①과 같은

> 🔴 **2026-09-06 PM 지적으로 재확인 — `Kid_Skill_3`/`Kid_Skill_3_item`은
> 이 줄에서 "별개로 이미 확정"이라고 잘못 적었다.** 구현담당2가 원문을 다시
> 열어 `if GetRandomInt(1,10)==2 then if UnitHasItem(캐스터,I010) then 600,000
> else 300,000`을 찾아냈고, 이건 **황준석_ADAP 조사(`APPROXIMATION_LEDGER.md`
> §14, 커밋 `3667276`)에서 내가 이미 직접 원문 확인해 "아이템 보유로 갈리는
> 진짜 배타"라고 결론 냈던 바로 그 쌍이다.** 즉 이 줄을 쓸 때 §14의 내 결론을
> 다시 대조하지 않고 "유형① 작업에서 다뤘으니 별개겠지" 식으로 목록만 베껴
> 썼다 — **원문 재대조 없이 과거 판정을 옮기지 말라는 원칙(뿌리 ㉛/㊷)을
> 내가 이 줄에서 어겼다.** `Kid_Skill_3`(_item)을 이 "별개 확정" 리스트에서
> **제외**한다 — 실제로는 **배타**다(아이템 `I010` 보유 여부로 600,000/300,000
> 갈림, 400,000/700,000 등 다른 페어가 아니라 이 하나뿐).
>
> **나머지 9개는 재확인했다** — 전부 실제 raw JASS를 직접 열어 다시 확인,
> 목록 대조가 아니었다: `Nami_Skill_4`(강주혁_AP, §14 직접 재확인)·
> `Ruffy_Attack`(신문철_AP, §14 직접 재확인, `ConditionalTriggerExecute`
> 무조건 호출)·`Tichi_skill_4`/`Tichi_skill_4_tr`(이번에 `Trig_Tichi_Attack_Actions`
> 원문 재확인 — 아래 Tichi 절 참고, `e0PH`+`e066`이 두 트리거 각각에서 항상
> 같이 나감, 배타 아님)·`Z_skill_3`(이번에 원문 재확인, `Stage[GlobalTV]`
> 순차 다단히트 구조, 뿌리 ㊶ 패턴)·`DP_Attack_gaksung`(최상호_AP 조사에서
> `Trig_DP_Attack_gaksung` 원문 직접 인용)·`Brook_Skill_1`·`Rebeca_Skill_1`·
> `Tichi_skill_2_tr`(전부 `CHANCE1_DUPLICATE_EFFECT_AUDIT.md`의 70트리거
> 기계판정에서 branch-tracer로 원문 직접 추적, 45건 별개 쪽) — **이
> 9개는 결론 유지.**

를 유형①과 같은
방법(if/elseif/else/endif 중첩 + `ForGroupBJ` 콜백 추적, `CreateNUnitsAtLoc`류
더미 생성 호출 기준)으로 기계판정했다. **총 53개 트리거 완료.**

## 결론 — **배타 6건 / 별개 47건. 추가로 훑은 12개 그룹에서 새 배타는 0건.**
유형①의 비율(25/70≈36%)을 안 씌운 게 맞았다 — 최종 6/53≈11%, 유형①의 1/3 이하.

## 배타 6건 — PM 요청 Ⓐ/Ⓑ/Ⓒ/Ⓓ 분류

| 트리거 | 갈리는 기준 | 분류 |
|---|---|---|
| `cat1` | 캐스터 자신의 능력레벨(`A0L8`==1) | **Ⓑ 캐스터 상태** |
| `Sengoku_Attack` | 캐스터 자신의 능력레벨(`A0D8`==2); 나머지 1건은 독립 10%RNG(배타 아님) | **Ⓑ 캐스터 상태** |
| `RedAttack` | 공유 RNG게이트(3/4) 통과 후 `GetUnitUserData(캐스터)==0` 여부로 갈림 — 갈리는 기준 자체는 UserData, RNG는 둘 다의 공통 진입조건일 뿐 | **Ⓑ 캐스터 상태** |
| `Cavendish_Attack` | 1차 10%RNG **실패 시에만** 2차 1/9RNG 시도(중첩 캐스케이드) | **Ⓒ RNG 캐스케이드** |
| `Legend4` | 원문 `if GetRandomInt(1,10)==5 then...elseif GetRandomInt(1,17)==3 then...endif` — 1차 10% 실패해야 2차 1/17 시도 | **Ⓒ RNG 캐스케이드** |
| `Legend22` | 3단 분기: (스테이지1 AND 대상포인트값==200(보스) AND 캐스터게이지>100) / (스테이지1 AND 대상버프B06B보유) / (스테이지1 AND 그 외 AND 캐스터게이지>360000) | **Ⓓ 복합**(대상보스여부+대상버프+캐스터게이지 셋 다) |

**최종 개수: Ⓐ 0 · Ⓑ 3 · Ⓒ 2 · Ⓓ 1(대상조건+캐스터상태 혼합).** Ⓐ(대상조건
단독)는 유형②에서 **한 건도 안 나왔다** — 유형①과 정반대로, 유형②는 순수하게
캐스터 자신의 상태(능력레벨·UserData)나 RNG 캐스케이드로 갈리는 비중이 100%다
(Legend22의 대상조건 성분도 캐스터게이지·대상버프와 섞인 복합형이라 순수 Ⓐ가
아니다).

**Ⓒ(RNG 캐스케이드) 정확한 구조** — PM 요청대로 단계별 확률 명시:
- `Cavendish_Attack`: 1단계 확률 10%(`GetRandomInt(1,10)==6`) → **실패 시에만**
  2단계 확률 1/9≈11.1%(`GetRandomInt(1,9)==7`) 시도. 최종 확률: 1단계 성공 10%,
  2단계 성공 (1−0.10)×0.111≈10.0%, 둘 다 실패 80.0%.
- `Legend4`: 1단계 확률 10%(`GetRandomInt(1,10)==5`) → **실패 시에만** 2단계 확률
  1/17≈5.88%(`GetRandomInt(1,17)==3`) 시도. 최종 확률: 1단계 성공 10%, 2단계 성공
  (1−0.10)×0.0588≈5.3%, 둘 다 실패 84.7%.

⚠️ **유형②에 Ⓒ가 2건 나왔다 — 유형①(`Nami_Skill_2` 1건)과 합치면 총 3건.** "하나
때문에 메커니즘을 만들 순 없다"는 근사가 3건째부터는 재검토 대상이라고 하신 것,
여기 반영해 남긴다: **캐스케이드 확률 구조(1차 실패 시에만 2차 시도)는 우리
`SkillEffect.chance`(효과별 독립 판정)로 구조적으로 못 담는다** — 지금 근사(유형①
`Nami_Skill_2`에 적용한 것과 같은 "기대값만 맞추는 개별 chance") 방식을 쓰면
1차·2차가 우연히 동시에 뜨는 경우(원작엔 없음)가 생긴다. 3건 전부 같은 부작용을
공유하므로, **"캐스케이드 효과 그룹"(1차 실패가 2차의 전제조건) 같은 새 필드가
필요하다는 판단이 이제 3표본 근거를 갖는다** — 다만 설계 결정은 구현/PM 몫이다.

**패턴**: 유형①(대상 포인트값 위주)과 달리 유형②는 **캐스터 자신의 상태**(능력레벨·
UserData·게이지)로 갈리는 비중이 높다 — 유형①의 "대상이 누구냐"가 아니라 "캐스터가
지금 뭘 가졌냐"가 기준인 경우가 많다. `Cavendish_Attack`/`Legend4`처럼 **진짜 RNG
캐스케이드**도 2건 확인됐다(유형①에선 `Nami_Skill_2` 1건뿐이었다).

## 별개(문제 없음) 47건 — 구현담당2 목록분(35건)

`Naruto1`(2개 다 독립 RNG, 서로 배타 아님) · `Legend6`(RNG10% + 캐스터게이지==115,
독립) · `ichigo1` · `k1` · `Shiki_Attack`(3개 전부 독립 RNG게이트, 동시발동 가능) ·
`Dragon_Attack` · `roger_Attack` · `Gaban_Attack` · `Kaido_Attack`(4개 독립) ·
`common1`/`common2`(기존에 이미 "진짜 공유" 확정) · `ViVi_Attack` · `Bugi_Attack`
(4개 독립) · `Uta_Attack` · `Ace_Attack`(버프부재조건 + 독립RNG, 배타 아님) ·
`Legend10`·`Legend25`·`Legend28koby`·`Legend8`·`Legend30`·`Legend29`·`Legend18`·
`Legend11`·`Legend2`(버프조건+게이지조건, 서로 독립 — ⚠️ 이미 "구조적 미해결"로
장부에 있는 그 A077/A001 쌍, 유형③에 더 가까워 보인다) · `Legend9`·`Legend7`·
`katakuriAttack`·`Sinobu_Attack`·`Speical7`·`Speical1`·`Unique3`·`Unique14`·
`Unique29`·`Unique32`·`Unique34`.

⚠️ **`Legend2`는 재검토 필요** — 조건이 "캐스터 버프 미보유"/"캐스터 마나==145"로
서로 무관한 두 게이트라 기계적으로는 "배타 아님"(별개)으로 나왔지만, 이미
`APPROXIMATION_LEDGER.md`가 이 파일(정윤식, A077/A001)을 "구조적 미해결"로
분류해뒀다 — 유형③(버프게이트 조건부, 손대지 말라고 하신 그 범주)에 더 가까울
수 있다. **판정을 덮지 않고 기존 장부 판정을 우선한다.**

### 자체 30그룹 중 41개와 안 겹친 12개 — 추가 완료, 새 배타 0건

`Law_Attack`(캐스터 버프+체력 / 캐스터 마나==135, 서로 독립, 배타 아님) ·
`Transpom_Ace_bulRemake`(Stage 0/≤3/4, 순차 다단히트) · `Aokigi_skill_2`(독립
조건 2개) · `Aokigi_skill_4`(단일) · **`arrow_h2`**(처음엔 배타로 잡혔으나
원문 확인 결과 `Stage==0`/`Stage 1~3`/`Stage>=4`의 **Stage 함정이었다** — 순차
다단히트로 재분류, 뿌리 ㊶ 재적용) · `Hidden20`(독립 RNG 2개, 10%·1/8) ·
`DP_Skill_2`/`Transpom_dp`(동일 더미 e0EK에 독립 조건 3개, 배타 아님) ·
`Luchi_Skill_1_Kick`(Stage 1/2, 순차) · `carrot_skill_2`(Stage 1/2, 순차) ·
`Law_skill_5`(독립 조건 3개) · `Mihawk_4`(기본효과+중첩보너스 구조, 배타 아님).

**`arrow_h2`를 빼면 이 12개 전부 이미 알려진 함정(Stage 순차 또는 독립조건) 중
하나였다** — 새로운 배타 패턴은 안 나왔다.

## 최종 커버리지

| 소스 | 트리거 수 | 배타 | 별개 |
|---|---:|---:|---:|
| 구현담당2 목록(41/56, 15개는 구현담당2가 직접 확인 중) | 41 | 6 | 35 |
| 자체 30그룹 중 41개와 겹침(교집합, 이미 위에 포함) | 10 | (중복 계산 안 함) | — |
| 자체 30그룹 중 41개와 안 겹침(신규 확인) | 12 | 0 | 12 |
| **합계(중복 제외 고유 트리거)** | **53** | **6** | **47** |

**교집합 10개**: `Naruto1`·`cat1`·`Legend6`·`Ace_Attack`·`common1`/`common2`·
`Legend2`·`Shiki_Attack`·`Ruffy_Attack`(자체 30그룹엔 있었지만 구현2 목록엔
없었음 — 재확인 필요 표시). **자체 30그룹에서 41개 목록에 없던 나머지 8개**
(`Z_skill_3`·`Tichi_skill_2_tr`·`Tichi_skill_4`·`Tichi_skill_4_tr`·
`DP_Attack_gaksung`·`Brook_Skill_1`·`Nami_Skill_4`·`Kid_Skill_3`/`_item`·
`Rebeca_Skill_1`)는 이번 대화 앞부분(유형① 콜백 추적 때)에 이미 개별 확인
완료된 것들이라 위 "12개 신규"에 다시 안 넣었다 — 전부 별개 확정 상태 그대로다.

**결론: 구현담당2가 못 뽑은 15개(직접 확인 중)를 빼면, 식별 가능한 유형② 후보는
사실상 전수됐다.** 남은 건 그 15개뿐이다.

## 방법 메모

유형①과 똑같이 `if/elseif/else/endif` 중첩을 추적했지만 **감지 대상 함수 호출을
`RRD`류에서 `CreateNUnitsAtLoc`/`CreateNUnitsAtLocFacingLocBJ`/`CreateUnit`(더미
생성)류로 바꿨다** — 유형②는 피해가 RRD 직접 호출이 아니라 더미를 소환해 그 더미의
능력이 피해를 내는 구조라서다. 오늘 배운 함정(Stage 분기는 배타 아님·값이 달라도
배타일 수 있음·못 잰 것에 비율 투영 금지) 전부 다시 적용했다 — 이번엔 Stage
분기로 오판한 사례는 없었다(직접 `if/elseif`로 쓰인 것만 배타로 잡았다).
