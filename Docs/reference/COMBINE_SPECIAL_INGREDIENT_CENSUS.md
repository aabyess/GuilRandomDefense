# 조합식 전수 — 특수유닛·아이템이 재료로 쓰이는 비율

조사: 리서치담당 / 2026-09-06
요청: PM — 「원작 조합식에 특수유닛·아이템이 재료로 들어가는 게 얼마나
되는가. 우리 조합식 207개 중 0개인데 원작에 많으면 축 하나를 안 쓰고
있는 것」

## 방법

`war3map.w3a`의 능력 오브젝트 중 `acat`(조합 재료 목록)·`atat`(조합
결과) 필드 쌍을 전수 추출했다(바이트 오프셋: `tag(4)+type(4)+level(4)
+dataId(4)+문자열`, 레일리 레시피로 이미 검증된 포맷). **총 264개
`acat` 필드 중 153개가 실제 재료 목록을 담고 있다**(나머지는 조합과
무관한 다른 능력들의 빈 `acat` 필드).

## 결론 — **아이템 재료 0건, 특수유닛(도박/히든풀) 재료 16/153건(약 10.5%)**

### 아이템(`I0xx`)이 재료인 조합식 — **0건**

153개 전부 뒤졌지만 재료 칸에 `I0xx` 형태가 하나도 없다. **원작도
"아이템을 조합재료로 쓰는" 축은 안 쓴다** — 우리가 0개인 게 맞다,
빠뜨린 축이 아니다.

### 특수유닛(도박/히든풀에서만 나오는 유닛)이 재료인 조합식 — **16건**

"특수유닛"을 "일반 로스터 코드가 아니라 도박·고대의배·히든조합에서만
얻는 유닛"으로 정의하고(`h05X`레일리·`h05Y`고대의배·`h060`해적선·
`h06G`행운토큰 — 지금까지 확인한 것들) 재료 목록을 대조했다:

| 재료 | 결과 |
|---|---|
| `h03X,h038,h00P,h060`(해적선) | `h05G` |
| `h05X`(레일리)`,h028,h018` | `h03A` |
| `h01R,h02F,h05Y`(고대의배) | `h087` |
| `h02B,h03W,h06G`(행운토큰)`,h01S` | `h040` |
| `h042,h032,h060`(해적선) | `h084` |
| `h09L,h033,h03L,h07N,h06G`(행운토큰) | `h09N` |
| `h06V,h07J,h042,h06G`(행운토큰) | `h09W` |
| `h06Y,h087,h036,h00U,h06G`(행운토큰) | `h09R` |
| `h070,h03J,h044,h02P,h06G`(행운토큰) | `h0A2` |
| `h072,h02R,h03T,h01N,h06G`(행운토큰) | `h0BF` |
| `h06V,h07J,h042,h024,h06G`(행운토큰) | `h09W`(위와 유사, 재료 1개 차이 — 중복/변형 레코드로 보임) |
| `h0AH,h02U,h05Y`(고대의배) | `h0AI` |
| `h071,h03K,h05V,h04H,h06G`(행운토큰) | `h08I` |
| `h06W,h0AH,h030,h06G`(행운토큰) | `h0BC` |
| `h06W,h031,h037,h06G`(행운토큰) | `h0BC`(위와 결과 같음, 재료 다른 변형) |

**압도적으로 `h06G`(행운토큰)가 가장 흔한 특수 재료다**(16건 중
11건). 나머지는 해적선(`h060`) 2건, 고대의배(`h05Y`) 2건, 레일리
(`h05X`) 1건.

## 우리 게임과의 대조

**우리 조합식 207개 중 특수유닛 재료 0개, 원작 153개 중 16개(10.5%)
— 이 축 자체가 통째로 빠져 있는 게 맞다.** 특히 **행운토큰(`h06G`)이
11개 레시피의 재료라는 게 핵심이다** — 어제 확인한 "행운토큰 요구
레시피 10개"(`GAMBLING.md`)와 정확히 같은 숫자대(정확히 10 vs 11은
위 표의 중복 변형 1건 차이, 사실상 일치)라 **결국 같은 결측을 다른
각도에서 재확인한 셈이다**: 행운토큰이라는 화폐 자체가 없으니, 그
토큰을 요구하는 조합식도 자연히 하나도 못 만들었다.

## 남은 것

- 153개 전체를 "이 재료가 원작 로스터의 몇 등급인지"까지 매핑하는
  일반 등급 사다리 분석은 안 함(질문 범위 밖).
- 결과 유닛(`h03A`/`h087`/`h084`/`h09N`/`h09W`/`h09R`/`h0A2`/`h0BF`/
  `h0AI`/`h08I`/`h0BC`/`h05G`/`h040`) 각각의 등급·이름은 이번엔 안 팜 —
  필요하면 이어서.
