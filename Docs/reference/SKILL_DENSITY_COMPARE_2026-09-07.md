# 02번(스킬 밀도) 등급별 비교표 — 결정용 (2026-09-07)

작성: 구현담당1 / PM 지시(쪼개기 종결 후속). 사장님이 「원작 스킬 일부를 버려 밀도를
맞출지」를 등급별로 결정하실 수 있도록, **과잉(밀도)과 결손(로스터 자리)을 한 표에서
같이 본다.**

## 0. 집계 규칙 — 먼저 고정한다 (PM 지시 1번, 2026-09-07 밤 3차 정정)

**"능력 1개"의 정의(v3, 최종)**: **전투에 실제로 작용하는** 원작 능력ID(AXXXX)
1개 = 우리 쪽 능력 1개. 파일 수가 아니라 **능력 수**로 센다(한 파일에 여러
능력이 남아 있으면 그만큼 센다, PM 지시 2번). ⚠️ **오늘 밤 이 정의가 세
번 바뀌었다** — 처음엔 "AXXXX가 uabi에 있으면 1개"(→ 안흔함 42개로
부풀었다), 다음엔 "atar=enemies면 1개"(→ 조합버튼·피해아님 필드가
섞였다), **이번이 세 번째다. 아래 배제 목록을 반드시 같이 적용한다** —
안 그러면 네 번째 오류가 난다(`MISSING_ABILITIES_2026-09-07.md` 참고).

**다음은 "능력 1개"에서 제외한다(배제 목록)**:
1. **조합(`[조합]`) 레시피 버튼** — `anam="[조합]"`, base가 `Aroa`/`AHwe`인
   것. 전투 능력이 아니라 조합 시스템 UI다.
2. **필드값이 0인데 툴팁에만 숫자가 있는 것** — `w3a`의 실제 필드(`Hbh1`/
   `Hbh3`/`Efk1` 등)를 직접 읽어서 확인한다. 툴팁(`aub1`) 숫자를 그대로
   옮기지 않는다(뿌리 ㉒) — **단, 필드가 실제로 0이 아니면(툴팁과 일치하면)
   그건 유효한 능력이다.** 개체마다 다르므로 하나씩 확인한다.
3. **`FIELD_MEANINGS_RESOLVED.md`가 이미 "피해 아님"으로 닫은 11개 필드
   그룹**(`Aroa`·`ANbr`·`AUfa`·`Asph`·`Ablo`·`ANht`·`AUls`·`Ainf`·`AOae`·
   `Apg2`·`AHad`)에 걸리는 능력 — base를 확인해서 이 목록에 있으면 제외.
4. (기존) 더미/사운드/순수 연출·스톡 껍데기(base만 있고 파생 수치 없음),
   트리거 배타 쌍은 1개로 접는다 — v1부터 있던 규칙, 유지.

- **원작 쪽**: `UNCOVERED_18_UNITS.md`(★★★ 최종 확정판)이 정한 대로 — 원작 트리거·오브젝트
  데이터에서 실제로 수치가 있는 능력만 센다. 더미/사운드/순수 연출(Asph 등)·스톡 껍데기
  (base만 있고 파생 수치 없음)는 제외, 트리거 배타 쌍(예: 원작이 대상 강함에 따라 A식/B식
  중 하나만 결정적으로 쓰는 경우)은 **1개**로 센다.
- **우리 쪽**: 로스터 `skill`/`skills`로 배선된 `SkillData_원작능력_*`/`SkillData_더미채널_*`
  자산에서, 파일당 "고유 AXXXX 인용 개수"(`DENSITY_REMEASURE_ABILITY_ID_2026-09-06.md`
  방법 그대로) + `CHANCE1_DUPLICATE_EFFECT_AUDIT.md`가 확정한 배타 트리거를 **1슬롯**으로
  접은 값. **원작과 같은 규칙(배타=1개)을 우리 쪽에도 그대로 적용했다** — 이게 이 표가
  기존 373-기준표와 다른 점이다.
- ⚠️ **"유닛당" 지표가 두 가지다 — 반드시 같이 본다.**
  - **등급평균**(주 지표) = 그 등급 **로스터 전체** 유닛 수 대비 능력 수. 코버리지(스킬을
    가진 유닛 비율)와 유닛당 능력 수를 한 숫자로 합친다 — 사장님께 지금까지 보고된
    "과잉/부족" 판정은 전부 이 지표 기준이다. **"몇 개를 덜어내야 하는지"도 이 지표로
    계산한다**(우리 로스터 크기가 고정값이므로).
  - **보유당**(참고 지표) = **스킬을 실제로 가진 유닛만** 대상으로 한 평균. 코버리지 차이를
    지운다. 🔴 **이 지표로 보면 결론이 달라지는 등급이 있다** — 아래 §2에 표시.
  - 두 지표가 갈리는 이유는 하나다: **코버리지 차이.** 우리는 원작보다 훨씬 많은 비율의
    유닛에 스킬을 배선했다(예: 안흔함 원작 14.3% vs 우리 53.3%) — 그래서 "보유한 유닛만
    보면" 우리가 오히려 원작보다 가볍고, "로스터 전체로 보면" 우리가 훨씬 무겁다.

### 🔴 규칙 불일치 발견 (2026-09-07, §6 검산에서 드러남) — "같은 규칙"이 실제로는 안 지켜졌다

위에서 "원작과 같은 규칙을 우리 쪽에도 그대로 적용했다"고 적었지만, **틀렸다.**
`UNCOVERED_18_UNITS.md`:65행의 원작 집계 기준은 **"그 능력이 실제로 트리거에서
발동하는지까지 원문으로 확인한 것만"** 센다 — `uabi`(유닛이 들고 있는 능력 목록)에
있다고 다 세지 않는다. 반면 **이 문서의 "우리" 열은 그 발동 검증을 거치지 않고
`SkillData` description의 AXXXX 인용 개수를 그대로 센다.** 즉:

```
원작 열 = 트리거 발동까지 확인된 능력 수 (엄격)
우리 열 = description에 인용된 능력ID 수, 실제 발동 여부 재검증 안 함 (느슨함)
```

**두 열은 같은 규칙이 아니다.** §6에 전수 검산 결과가 있다 — 안흔함은 원작 로스터
14가 정확히 일치할 만큼 방법 자체는 맞게 따라갔지만, 우리 쪽 "10개"가 정말
전부 원작에서 발동하는 능력인지는 확인이 안 됐다. **§1의 배수는 이 불일치가
있는 채로 나온 수치다 — 아래 §1 표의 주석과 §6을 같이 보지 않으면 잘못 읽는다.**

## 1. 등급별 본표 (등급평균 기준 — 배타 접은 뒤)

| 등급 | 원작 로스터 | 원작 보유(스킬있음) | 원작 능력 수 | 원작 등급평균 | 우리 로스터 | 우리 보유 | 우리 능력 수 | 우리 등급평균 | 배수 | 신뢰도 | 총량차 | **몇 개 덜어내야 원작 수준(우리 로스터 기준)** |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|---:|---:|
| 흔함 | 32 | 0 | 0 | 0.000 | 9 | 0 | 0 | 0.000 | 1.00 | 확정 | 0 | 0 |
| 안흔함 | **13**✅(가챠풀 좌표기반 확정, PM) | **7**🔧 | **9**🔧 | **0.692**🔧 | 15 | 8 | **10** | 0.667 | **0.96배 — 우리가 원작보다 살짝 적다** | 🟢[확정] | −3 | **0개(오히려 늘려야 함)** |
| 특별함 | **33**✅ | **18**🔧(원작21, "피해아님" 오염6건 제거 후) | **21**🔧 | **0.636**🔧 | 33 | 18 | **21**🔧(우리도 A0A0·A0RF 오염 2건 발견해 제거 후, 원 23) | **0.636**🔧 | **1.00배 — 정확히 일치** | 🟢[확정, 양쪽 동일필터] | 0 | **0개(정확히 맞음)** |
| 희귀함 | **45**✅ | **?**(원작62) | **62**🔧 | **1.378**🔧 | 43 | 37 | **51**🔧(우리도 A095 오염 1건 제거 후, 원 52) | **1.186**🔧 | **0.86배 — 우리가 살짝 적다** | 🟢[확정, 양쪽 동일필터] | −11 | **약 5개** |
| 전설적인 | 43 | 36 | 71 | 1.651 | 33 | 27 | **59**\* | 1.788 | **1.08배(검증 완료)** | 🟢 | **−12** | 약 5개 |
| 변화됨 | 9 | 7 | 11 | 1.222 | 4 | 4 | 11 | 2.750 | **≥2.25배(하한)** | 🟡[표본 3개뿐, 부분검증] | 0 | 약 6개(⚠️ 아래 참고) |
| 제한됨 | 12 | 11 | 29 | 2.417 | 9 | 9 | **29**\* | 3.222 | **1.33배(검증 완료)** | 🟢 | 0 | 약 7개 |
| 초월함 | 64 | 34 | 85 | 1.328 | 25 | 21 | 60 | 2.400 | **1.81배(검증 완료)** | 🟢[§8, 자산 재확인으로 복구] | −25 | **약 27개** |
| 영원한 | 9 | 9 | 39 | 4.333 | 8 | 8 | 44~45 | 5.50~5.63 | **1.27~1.30배(검증 완료)** | 🟢 | +5~+6 | 약 10개 |
| 불멸의 | 18 | 15 | 52 | 2.889 | 8 | 8 | 57~58 | 7.13~7.25 | **2.47~2.51배(검증 완료)** | 🟢 | +5~+6 | **약 34개(가장 큼)** |

🔧 **2026-09-07 밤 — 완전 재계상 확정(리서치담당, 커밋 `f264f51`).** 안흔함·
특별함·희귀함 **셋 다 과소계상이었다, 방향이 같다**:
```
              옛(무효)             완전 재계상
안흔함 14    2/4  (보유당 2.00)  → 7/9   (1.29)
특별함 42?  11/18 (1.64)        → 26/29 (1.12)
희귀함 45?  32/50 (1.56)        → 42/65 (1.55)
```
누락 사유는 등급마다 달랐다 — 안흔함=중복집계+미반영, 특별함=`atar` 패시브
다수 누락, 희귀함=**트리거 직결 RRD**(능력ID가 `uabi`에 아예 안 뜨고 트리거가
직접 `RRD`/`UnitDamageTargetBJ`를 부르는 형태) 다수 누락.

✅ **로스터 분모 — 원작 자기 기준(가챠풀 좌표배치)으로 재확정(리서치,
`Trig_tier_Random_Actions`).** 이 트리거는 **맵 초기화 시 배치 사각형 안
유닛을 등급별 랜덤그룹으로 쓸어담는 코드**라 "그 등급에 실제로 가챠로
나오는 유닛"을 좌표 기반으로 직접 센다 — 내 이름-문자열 방법과 **독립
경로**인데 특별함은 정확히 같은 값(33)이 나왔다:

```
안흔함  14 → 13   (h00H 좀비가 사각형 밖 + uabi가 [조합]버튼뿐 실전능력 0 — 두 신호 일치)
특별함  33         (이름필터·좌표기반 둘 다 33, 확정)
희귀함  48 → 45   (이름필터 48엔 더미2+장난감1 오염 — 가챠풀은 42, 차3은 레일리·고대의배·도킹6)
```

**희귀함은 45를 쓴다(PM 판단)** — 가챠풀(42)에 없어도 레일리·고대의
배·[히든]파이러츠도킹6은 다른 획득 경로(스토리·도박)로 실제 플레이어가
얻을 수 있는 희귀함 유닛이라 "그 등급에 존재하는 유닛" 정의에 들어간다.
가챠 풀만 비교할 땐 42를 쓴다(용도가 다르면 분모도 다르다).

🔴 **분모 기준이 등급군마다 다르다 — §0의 핵심 한계.** 안흔함·특별함·
희귀함(하위 3등급)은 `Trig_tier_Random_Actions`가 실제로 등급별 그룹을
채워서(`Random1~5` 계열) 가챠풀 기준으로 로스터를 셀 수 있다. **전설적인·
제한됨·초월함·불멸의·영원한(상위 5등급)은 이 트리거의 대응 그룹
(`Random6/7` 등)이 선언만 되고 안 쓰인다** — 즉 **상위 등급은 가챠로
안 나온다.** 그 등급들의 "원작 로스터"는 다른 기준(밴 레지스트리·히든조합
목록 등)으로 정해야 하고, 리서치가 찾는 중이다 — **상위 5등급은 로스터
분모 자체가 아직 없다**(전에 쓰던 UNCOVERED 값은 §6-2 감사에서 이미
[무효]로 내린 상태 그대로).

**우리 쪽 로스터 수(15·33·43)도 같은 함정을 확인했다** — `Assets/Data/
Units/Roster/*.asset` 중 이 세 등급에 `isSystemUnit: 1`(퀘스트 토큰류,
실제 배치 불가 유닛)로 표시된 게 있는지 전수 스캔했고 **0건**이다. 원작
쪽만 더미를 걸러내고 우리 쪽은 안 걸렀을 위험은 없다 — 우리 로스터는
애초부터 실제 배치 가능한 유닛만 등록되는 폴더라 이 문제 자체가 안 생긴다.

🔴 **최종 정정(2026-09-07 밤, 마지막) — 리서치가 원작 열에서 "피해 아님"
base(AOae·AHad·Ablo) 오염 6건을 걷어내다가(특별함29→21·희귀함65→62),
"우리 쪽엔 같은 오염이 없는지"를 나에게 넘겼다. PM이 지목한 6개 ID는
우리 자산에 0건이었지만(거울 확인, 위 §), 그 확인 범위를 우리가 실제로
**배선한** 특별함 16개·희귀함 30개 전체로 넓혀 `base`를 다시 대조하니
**우리 쪽에도 다른 ID로 같은 오염이 있었다** — 리서치가 지목한 6개
ID와는 다른, 우리 로스터에 배정된 별개 원작 ID들이다:**

```
특별함: A0A0(조도연 파일, base Ablo)·A0RF(주영호 파일, base AOae)
       — 둘 다 이미 값은 제거돼 있었지만(2026-09-06) description
         인용은 남아 있어 "23개" 집계에 여전히 잡히고 있었다.
희귀함: A095(박민수 파일, base Ablo) — 이미 effects:[](§5-3 각주)
       였지만 같은 이유로 "52개"에 잡히고 있었다.
```

**양쪽에 같은 필터(피해 아님 base 제외)를 적용한 최종 배수**:
```
안흔함  원작 9/13=0.692   우리 10/15=0.667  → 0.96배(원작보다 살짝 적음)
특별함  원작 21/33=0.636  우리 21/33=0.636  → 1.00배(정확히 일치)
희귀함  원작 62/45=1.378  우리 51/43=1.186  → 0.86배(원작보다 살짝 적음)
```
**특별함이 우연이 아니라 계산으로 정확히 1.00배가 나온다** — 양쪽에서
같은 종류의 오염을 걷어내니 분자가 똑같이 21/33으로 수렴했다.

**§9 결론도 이 확정치로 다시 썼다** — "하위 세 등급 과잉"은 대부분 원작
쪽 과소계상에서 나온 허상이었다.

\* 전설적인·제한됨은 이번에 **처음** CHANCE1 배타 폴드를 적용했다(§3 참고, PM 미검토 —
반드시 확인 요청). 나머지 등급 수치는 `APPROXIMATION_LEDGER.md` §17(PM 지시로 이미 확정)
그대로 인용.

### 신뢰도 열 읽는 법 (§6 검산 반영, PM 지시)

- **확정** — 흔함은 양쪽 다 0이라 이 문제 자체가 성립 안 한다.
- **🔴[미확정]** (안흔함·특별함·희귀함·**초월함**) — `uabi` 역색인으로 **직접 확인**해서
  "전 유닛이 공유하는 커스텀 능력ID(2~3개) + 캐릭터별 고유 1~2개" 구조를 찾은 등급들이다
  (§6, §6-2). 초월함은 처음엔 🟡였는데 재검사에서 같은 구조(`A07O`, 30명 중 21명 공유)가
  나와 강등했다 — **미검증이 아니라 확인해서 위험이 있는 걸로 나온 것**이다. 안흔함은
  실제로 "고유 ID 12명분이 죽은 참조일 수 있다"는 구체적 의심까지 나왔다(§6).
  **배수는 하한(≥)으로 읽는다** — 우리 열의 분자(능력 수)가 재검증되면 내려갈 수도, 그대로일
  수도 있다(우리가 실제로 더 많은 원작-발동-확인 능력을 놓쳐서 지금 값이 오히려 과소일
  가능성도 배제 못 한다 — 방향은 안 정해졌다, §6 참고). **상한은 계산 불가**다: "문제의
  능력이 전부 원작에서 안 돎으로 확정될 경우"에도, 그 능력들이 **우리 게임 안에서는 이미
  실제로 발동하는 피해로 구현돼 있어** 밀도(우리 쪽 실효 능력 수) 자체는 안 줄어든다 —
  줄어드는 건 "밀도"가 아니라 "원작 근거"다. 그래서 이 불확실성은 배율을 **위로도 아래로도
  움직일 수 있는 폭 자체를 특정 못 하는 것**이지, 깔끔한 상하한 구간이 아니다.
- **🟢** (전설적인·제한됨·불멸의·영원한) — **§6-2에서 실제로 `uabi` 역색인을 돌려 검증
  완료했다** — 정황이 아니라 결과다. 네 등급 다 전원-공유 커스텀 능력ID가 없고(스톡
  `Avul`/`Aloc`만 공유), 흘러내림처럼 보였던 5건(초월함·불멸의)도 전부 "같은 캐릭터의
  다른 등급 폼이 베이스 능력을 공유"하는 걸로 재확인돼 기각됐다(§6-2 표). 출처불명 0건.
  **이 네 등급의 배수는 지금 값 그대로 믿을 수 있다.**
- **🟡[표본 부족]** (변화됨) — 같은 방법을 적용했지만 **로스터가 3명(능력 3개)뿐이라
  표본이 작다.** 3개 다 확인 결과 같은 등급이었고 전원-공유 마커도 못 찾았지만, 표본이
  작아 "없다"고 단정하기엔 이르다 — 안전 쪽으로 🟡을 유지한다. 게다가 변화됨은 §4에서
  이미 "전설 슬롯 위에 얹는 구조"라는 별건 문제가 있다 — 그쪽이 더 큰 불확실성이다.

**출처**: 원작 로스터/보유/등급평균 — `DENSITY_REMEASURE_ABILITY_ID_2026-09-06.md`
41~56행(등급별 비교표), 로스터 결손 원본은 `BLOCKERS_AND_ROSTER_DEFICIT.md`:126~138행.
우리 능력 수(안흔함·특별함·희귀함·변화됨) — 같은 문서 같은 표(폴드 대상 없음, §17 확인).
우리 능력 수(초월·불멸·영원) — `APPROXIMATION_LEDGER.md`:1104~1110행(§17 표). 우리 능력
수(전설적인·제한됨) — 아래 §3(이번 세션 신규 계산).

## 2. 🔴 보유당 기준으로 보면 뒤집히는 등급 — 참고 지표

⚠️ **아래 표의 안흔함·특별함·희귀함 "원작 보유당"은 위 §1 정정 전 옛 값(구
"2/4"·"11/18"·"32/50")으로 계산된 것이라 안흔함은 특히 낡았다** — 참고
지표라 이번엔 시간 관계상 재계산 안 함, §1의 새 값과 방향만 대조해서 읽을 것.

| 등급 | 원작 보유당 | 우리 보유당 | 배수(보유당) | 등급평균 배수와 방향이 같은가 |
|---|---:|---:|---:|---|
| 안흔함 | 2.00 | 1.25 | **0.625배(오히려 적음)** | 🔴 **반대** — 등급평균으론 과잉(2.33배)인데, 스킬 가진 유닛만 보면 원작보다 가볍다 |
| 특별함 | 1.64 | 1.28 | 0.78배(적음) | 🔴 반대 |
| 희귀함 | 1.56 | 1.41 | 0.90배(적음) | 🔴 반대 |
| 전설적인 | 1.97 | 2.15 | 1.09배 | 🟢 같음(약함) |
| 제한됨 | 2.64 | 3.22 | 1.22배 | 🟢 같음(약함) |
| 초월함 | 2.50 | 2.86 | 1.14배 | 🟢 같음(약함, 등급평균 1.81배보다 훨씬 약함) |
| 영원한 | 4.33 | 5.50~5.63 | 1.27~1.30배 | 🟢 같음(등급평균과 거의 동일 — 보유율 둘 다 100%라서) |
| 불멸의 | 3.47 | 7.13~7.25 | **2.06~2.09배** | 🟢 같음(등급평균과 거의 동일 — 보유율 둘 다 100%라서) |

**읽는 법**: 영원한·불멸의는 양쪽 다 보유율 100%라 두 지표가 사실상 같다 — **이 둘의
"과잉"은 진짜로 유닛당 능력이 많은 것**이다. 반면 **안흔함·특별함·희귀함의 "과잉"은
태반이 "코버리지 과잉"이다** — 즉 원작은 그 등급 유닛의 일부(14~70%)만 스킬을
가지는데 우리는 훨씬 많은 비율에 배선해놨다. **사장님이 고르실 수단이 다르다**:
코버리지 과잉이면 "능력을 없는 유닛으로 옮기지 말고, 애초에 스킬 배선 자체를 줄인다"가
맞는 처방이고, 유닛당 과잉(영원한·불멸의)이면 "한 유닛 안의 능력 개수를 줄인다"가
맞는 처방이다 — 같은 "밀도 초과"라도 원인이 다르면 자르는 대상이 다르다.

## 3. 전설적인·제한됨 신규 폴드 — 이번 세션 계산, PM 검토 요청

`APPROXIMATION_LEDGER.md` §17은 "PM 지목 5등급"(안흔함·특별함·초월함·불멸의·영원한)만
재계산했다. 그런데 `CHANCE1_FIX_INSTRUCTIONS.md`(21파일 확정)를 다시 보니 **전설적인·
제한됨에도 확정된 배타 트리거가 있었다** — §17이 다루지 않은 구멍이다. 이번에 채웠다:

| 등급 | 트리거 | 원본 파일 | 접기 전 | 접은 뒤 | 델타 |
|---|---|---|---:|---:|---:|
| 전설적인 | `Legend_han_1` | `SkillData_게이트_전설적인_이현주_0e5cf375.asset` | 2(`<200`/`ELSE`) | 1 | −1 |
| 전설적인 | `Legend26_tichi_crows` | `SkillData_게이트_전설적인_홍인창_fbb3c4ff.asset` | 2(`<200`&버프/`ELSE`) | 1 | −1 |
| 전설적인 | `Legend13_2` | `SkillData_게이트_전설적인_정준영_3a13fd3d.asset` | 2(`<200`/`==200`) | 1 | −1 |
| 제한됨 | `Marco_S1` | `SkillData_게이트_제한_박성호_874ea782.asset` | 2 | 1 | −1 |
| 제한됨 | `Marco_S2` | `SkillData_게이트_제한_박성호_3a13fd3d.asset` | 2 | 1 | −1 |
| 제한됨 | `King_skill_2_tr` | `SkillData_게이트_제한_임준성_355f5d39.asset` | 2 | 1 | −1 |
| 제한됨 | `Ain_Skill_1` | `SkillData_게이트_제한_이충민_18aa2343.asset` | 2(버프有/無) | 1 | −1 |
| 제한됨 | `Ain_Skill_2` | `SkillData_게이트_제한_이충민_8f2b5872.asset` | 2(버프有/無) | 1 | −1 |

전설적인: 62(구 기준선) − 3 = **59**. (A07K 중복 제거, `755972e`는 이 총량에 **영향 0** —
아래 §5 참고, PM 지적으로 정정.)
제한됨: 34(구 기준선) − 5 = **29**.

✅ **PM 검토 완료 — 대조 결과: 8건 전부 배타 25건 목록과 대조 완료, 되돌린 것 0건.**
`CHANCE1_DUPLICATE_EFFECT_AUDIT.md`가 스스로 경고한 함정("Stage 분기는 배타가 아니다"
— `s__TrigVariables_Stage[GlobalTV]==0/1/2` 다단히트를 배타로 오판한 `Legend19_0`·
`Sandi_skill_1`)에 해당하는지 8건 각각을 그 문서의 원문 판정표(A/C 분류, 39~67행)와
직접 대조했다 — 8건 전부 대상포인트값(`<200`/`==200`) 또는 대상버프(`UnitHasBuffBJ`)
조건이고, Stage 변수를 쓰는 항목은 하나도 없다(Legend19_0·Sandi_skill_1은 애초에 내
8건에 없음). 8건 모두 문서의 배타 25건 목록에 실제로 올라 있다 — 되돌릴 것 없음.

## 4. 로스터 결손 — 같은 표에 나란히 (PM 지시 5번)

| 등급 | 결손(원작 로스터 − 우리 로스터) | 이 등급이 동시에 과잉인가 |
|---|---:|---|
| 전설적인 | **−10**(43→33) | 🔴 과잉(등급평균 1.08배로 거의 해소됐지만 총량은 −12, 로스터가 좁아서 절대량은 원작보다 적다) |
| 제한됨 | **−3**(12→9) | 🟡 과잉(1.33배) — 결손도 있고 밀도도 아직 높다 |
| 초월함 | **−39**(64→25, 그중 스킬보유 결손 −9는 `BLOCKERS_AND_ROSTER_DEFICIT.md`:134행) | 🔴 과잉(1.81배) **+** 결손(−9) 동시 — 깎을 여유가 가장 적은 등급 |
| 영원한 | **−1**(9→8, 스킬보유 결손 동일) | 🟡 과잉(1.27~1.30배, 다섯 중 가장 약함) |
| 불멸의 | **−10**(18→8, 스킬보유 결손 −7) | 🔴 과잉(가장 심함) **+** 결손(−7) 동시 — 초월함과 같은 딜레마 |
| 안흔함 | 0(14→15, 오히려 +1) | 🔴 과잉(2.33배, 대부분 코버리지 과잉 — §2 참고) — 로스터는 넉넉하니 깎을 여유 있음 |
| 특별함 | 0(33→33) | 🟡 약간 과잉(1.28배) — 로스터 결손 없음, 깎을 여유 있음 |
| 희귀함 | +3(46→43 — 로스터는 원작보다 3 적지만 스킬보유 결손은 0) | 🟡 약간 과잉(1.11배) — 여유 있음 |
| 변화됨 | 별건 — 전설 슬롯 위에 얹는 구조라 결손표에 안 잡힘(`BLOCKERS_AND_ROSTER_DEFICIT.md`:142~146행 참고) | 🔴 과잉(2.25배) — 전설적인 결손 부담을 더 키우는 구조 |

**출처**: `BLOCKERS_AND_ROSTER_DEFICIT.md`:126~138행(등급별 결손표), 142~146행(변화됨 별건 설명).

**한 줄 요약(사장님용)**: **초월함·불멸의는 "과잉이면서 동시에 로스터도 좁다"** —
깎아도 원작 수준을 못 채우는 등급이다. **안흔함·특별함·희귀함은 로스터가 넉넉해서
깎을 여유가 있는데, 그 과잉의 태반이 "유닛당 개수"가 아니라 "너무 많은 유닛에
배선했다"(코버리지)는 점이 다르다.**

## 5. 미확정 2건 — 둘 다 판정 자체는 닫혔다 (PM 지시, 정정 반영)

- ✅ **A07K(전설적인_최상호)** — 해소됨. `A07K_4TH_INSTANCE_RESOLVED.md` 결론대로
  `SkillData_원작능력_전설적인_최상호.asset`의 중복 4건 제거(커밋 `755972e`).
  🔴 **밀도 집계 영향은 −1이 아니라 0이다(PM 지적으로 정정)** — 이 표의 집계 단위는
  `effects:` 배열 행 수가 아니라 **description의 AXXXX 인용 개수**(§0)이고, 그 인용은
  유닛 단위로 **합집합**한다(`DENSITY_REMEASURE_ABILITY_ID_2026-09-06.md`:106행). 이 파일의
  description은 애초부터 A07K를 인용하고 있었고(제거 전/후 동일, 3회 그대로), 같은 유닛의
  `더미채널_전설적인_최상호_1/_2` 파일도 A07K를 인용한다 — 합집합에서 A07K는 **원래도 1개로만
  잡혀 있었다.** 제거한 4개 raw effect 행은 이 문자열 기반 카운트에 애초에 안 잡히던
  중복 데이터였다 — 지운 것은 옳지만(데이터 정합성 문제), **밀도 총량엔 영향 없음.**
  전설적인 총량은 §3의 CHANCE1 배타 폴드(−3)만 반영해 **62→59**.
- ✅ **`vivi_Skill_4_Mana` #1/#3 (영원_문필환)** — 판정 닫힘, `VIVI_SKILL4_1_3_GATE_RESOLVED.md`
  (리서치담당): #1/#3(가변 CHAOS)과 #2/#4(고정 500000 HERO)는 **같은 if/else의 반대쪽**,
  조건은 `GetUnitPointValue(대상)<200` — ⓐ(원래 근사)가 맞았다. **[확정, 미구현]**으로
  분류한다: 능력 수(4개, 영원한 47에 그대로 포함)는 확정이고, **다만 지금 자산은 4개가 무조건
  같이 나가 원작보다 과다 발동 중**(targetCondition 게이트 미배선) — 이건 밀도(개수) 문제가
  아니라 배선(구현) 문제라 이 표의 집계에는 영향 없다. 게이트 배선은 별도 작업 큐(③)로
  넘긴다 — targetCondition 필드·`EnemyData.pointValue`(95종) 이미 존재.

## 5-2. 🔴 차단 5건 — 지금 집계에 **포함돼 있다**(과대 계상 가능성)

`FIELD_MEANINGS_RESOLVED.md`가 "피해 아님"으로 확정했지만 **같은 값이 파일 안에 중복돼
어느 인스턴스가 그 능력인지 못 갈라 아직 못 지운 5건**(§0의 집계 규칙 — description의
AXXXX 인용 기준 — 그대로 적용하면 **지금 이 5개는 전부 총량에 포함돼 있다**, 아직
description에서 인용을 빼지 않았기 때문이다):

| 파일 | 능력ID | 등급 | 현재 포함 여부 |
|---|---|---|---|
| `SkillData_원작능력_전설적인_정윤식.asset` | A07B(AUfa) | 전설적인 | 포함 |
| `SkillData_원작능력_영원_윤현모.asset` | A0CB(AUfa) | 영원한 | 포함 |
| `SkillData_원작능력_영원_김영원.asset` | A0LY(AUfa) | 영원한 | 포함 |
| `SkillData_원작능력_불멸_신지우.asset` | A0ZX(Ainf) | 불멸의 | 포함 |
| `SkillData_원작능력_영원_김정래.asset` | A12V(Ainf) | 영원한 | 포함 |

**최대(5건 포함, 지금 표 §1의 수치 그대로) vs 최소(5건 다 제거된다고 가정) 비교**:

| 등급 | 최대(현재, 포함) | 최소(5건 제거 가정) | 등급평균 배수 변화 |
|---|---:|---:|---|
| 전설적인 | 59 / 1.788 / 1.08배 | 58 / 1.758 / **1.06배** | −1 |
| 영원한 | 44~45 / 5.50~5.63 / 1.27~1.30배 | 41~42 / 5.13~5.25 / **1.18~1.21배** | −3 |
| 불멸의 | 57~58 / 7.13~7.25 / 2.47~2.51배 | 56~57 / 7.00~7.13 / **2.42~2.47배** | −1 |

**결론은 안 바뀐다** — 최소 시나리오로 가도 세 등급 다 여전히 1.00배를 넘는다(과잉
유지). 다만 영원한은 폭이 제일 크게 움직인다(1.27~1.30배 → 1.18~1.21배) — 다섯 중
가장 약한 과잉이라는 순위 자체는 안 바뀐다.

## 5-3. 박민수(A095) 빈 껍데기 1건 — 각주

`SkillData_원작능력_희귀함_박민수.asset`은 이미 전에 처리되어 `effects: []`(완전히
빈 리스트)로 남아 있다. **§0의 집계 규칙(description AXXXX 인용) 기준으로도 능력
0개로 센다** — 인용은 있지만("A095" 언급) 그 인용 자체가 "제거했다"는 정정 내역이라
활성 인용이 아니다(위 §0/§1의 희귀함 52는 이 파일을 0개로 이미 반영한 수치다 —
새로 뺄 것 없음). 참고용으로만 남긴다.

## 6. 검산 — 역색인과 §1 원작 열의 대조 (PM 지시, 모순 해소)

PM이 지적한 모순: `COVERAGE_TRIM_CANDIDATES_2026-09-07.md`의 `uabi` 역색인
결과("우리 안흔함 능력은 전부 원작에서도 안흔함 것")와 이 표 §1("원작 안흔함
능력 4개")가 나란히 놓으면 앞뒤가 안 맞는다. **직접 재검산했다 — 답은 3번
(두 조사가 "능력"을 다르게 센다)이다.**

### 원작 안흔함을 `uabi` 원본 그대로 다시 세면

`war3map_new.w3u`에서 이름에 "안흔함" 등급 접미사가 붙은 유닛은 18개인데,
그중 4개(`e017`위습·`e0DG`/`e0E6`더미·`e0DO`위습)는 `Aloc`(로커스트, 장식용
표시)가 붙은 비캐릭터라 로스터에서 빠진다 — **딱 14개가 남는다**
(`UNCOVERED_18_UNITS.md`:63행 "309기 = h*/H* · 등급 라벨 보유 · Aloc 없음"과
정확히 같은 규칙, 그리고 §1의 "원작 로스터 14"와 정확히 일치한다).

**그런데 이 14개 전부가 다음 3개 능력ID를 예외 없이 공유한다**: `A07Y`,
`A0JE`, `A0B8`. 그 위에 캐릭터마다 자기만의 ID 1~2개가 더 있다(예: 하찌
`A06P`·`A0N8`·`A01P`, 브룩 `A04C`·`A00K`·`A019`·`A02E`). **`uabi`를 그대로
합집합하면 42개**(공용 3 + 캐릭터별 고유 39개 근방)가 나온다 — §1의 "4개"와
10배 차이다.

### 두 표가 세는 대상이 다르다

`UNCOVERED_18_UNITS.md`:65행("★ 원작 스킬 밀도")이 스스로 밝힌 집계 기준:

> **"겹치는 능력 대표화 기준(한 스킬 = 한 이름의 능력/트리거)"** — `uabi`에
> 능력ID가 있다고 다 세지 않는다. **그 능력이 실제로 트리거에서 발동해
> 수치가 나가는지**를 원문(`war3map.j`)까지 따라가 확인한 뒤, 발동하는
> 것만 "스킬 1개"로 센다. `A07Y`·`A0JE`·`A0B8`처럼 14명 전부가 공유하는
> ID는 **등급 공용 표식/패시브 스탯일 가능성이 높고**(이 세션 내내 반복된
> "표식 vs 실효" 패턴, 예: `Aloc`·`atar=none`·`Hbh1=0`류), 캐릭터별 나머지
> ID들도 상당수가 **트리거에 안 걸린 죽은 참조이거나 UI 표식**일 수 있다 —
> `UNCOVERED_18_UNITS.md`의 "보유 2/14"가 바로 이 필터링 결과다: **14명 중
> 실제로 발동하는 능력을 가진 건 2명뿐이고, 그 2명이 각각 2개씩 = 4개.**

즉 **원작 열의 "4개"는 "uabi에 적힌 능력 수"가 아니라 "실제로 원작에서
발동을 확인한 능력 수"**다. 반면 이 표 §0/§1의 **"우리 능력 수"는 원작
쪽과 같은 발동 확인을 다시 거치지 않고, `SkillData` description의 AXXXX
인용 개수를 그대로 센다**(DENSITY_REMEASURE_ABILITY_ID_2026-09-06.md 방법).
**§0이 "양쪽을 같은 규칙으로" 세겠다고 했지만, 실제로는 원작 쪽만 트리거
발동 검증을 거쳤고 우리 쪽은 안 거쳤다** — PM이 지목한 3번이 맞다.

### 🔴 이게 더 심각한 질문을 연다 — 우리 10개 중 일부가 "죽은 능력"일 수 있다

원작 14명 중 12명의 "고유 ID"(하찌 `A06P`, 브룩 `A04C`, 포트거스.D.에이스
`A06J`, CP9 후쿠로/블루노 `A046`·`A0A2` 등)가 실제로는 **원작에서 발동
안 하는 죽은 참조/장식**일 가능성이 있다 — "보유 2"가 사실이라면 정확히
그런 뜻이다. **그런데 우리는 이 12명 전부의 ID에 `ORIGINAL_UNIT_ABILITIES.csv`
DPS 순위 배정으로 실제 피해 수치를 넣었다**(안흔함_박민수·문필환·엄태웅·
이재윤·황정기·이호준 6개 파일 전부 이 경로). **원작에서 안 도는 능력에
우리가 새로 피해를 만들어 넣은 것이라면, 이건 코버리지 확산이 아니라
"전부 원작대로" 위반(지어낸 피해)에 더 가깝다.**

**이건 이 세션 도구로 못 닫는다** — "그 능력이 실제로 트리거에서 발동하는지"는
`war3map.j` 트리거 원문을 `A046`·`A04C`·`A06J`·`A06P`·`A03M`·`A0A2` 각각에
대해 개별 확인해야 하고(`UNCOVERED_18_UNITS.md`가 다른 14기에 대해 이미 한
바로 그 작업), 이건 리서치담당의 방법론이다. **여기서는 "이게 왜 다른지"만
밝히고 추측으로 손대지 않는다.**

### 결론 — 3번(방법 상이)이 근본 원인, 그리고 2번(우리 쪽 과다 가능성)도 열려 있다

- **모순의 직접 원인은 3번**이다: 원작 "4개"는 트리거 발동 확인을 거친 수,
  우리 "10개"(§1)는 description 인용 수 — 같은 규칙이 아니었다.
- **01번의 근거 자체가 흔들리는 건 아니다** — 원작 열(4개)은
  `UNCOVERED_18_UNITS.md`가 트리거 원문까지 검증한 신뢰도 높은 수치라
  1번(원작 쪽이 틀렸다)은 기각한다.
- **다만 2번(우리 쪽이 틀렸을 수 있다)이 완전히는 안 닫힌다** — 안흔함
  10개 중 최대 8개(2명분 4개를 뺀 나머지)가 원작에서 실제로 안 도는
  능력에 배선된 것일 수 있다. **이게 사실이면 "2.33배 과잉"은 과소평가고,
  실제로는 훨씬 더 심하다**(원작 진짜 발동 4개 vs 우리 실제 발동 10개
  전부라면 배율이 더 커진다). **반대로 우리도 확인 결과 대부분 안 도는
  걸로 나오면 배율은 오히려 줄어든다** — 어느 쪽이든 리서치담당의 트리거
  발동 확인 없이는 특별함·희귀함을 포함한 이 세 등급 전체의 "우리 능력 수"
  분자가 확정이 아니다.
- **특별함·희귀함도 같은 구조를 보인다** — 특별함은 전 유닛이 `A0JE`·`A07Y`·
  `A0BA` 3개를, 희귀함은 `A07Y`·`A0B9` 2개를 예외 없이 공유한다. 세 등급
  다 "공용 표식 3종 내외 + 캐릭터별 고유 1~2개"라는 같은 뼈대다.

### 🔴 추가 — 리서치 발동확인 결과 반영(2026-09-07, PM 전달) 및 다른 등급 교차확인

리서치담당이 안흔함 6개 파일을 직접 열어 발동 확인한 결과: **2번(우리 쪽
분자 과다)은 기각.** 안흔함 태그 파일은 8개가 아니라 6개뿐이고, 인용된
원작 유닛은 정확히 `h00A·h00M·h00D·h00E·h00F·h00K` 6명 — **이 6명이 실제로
발동하는 그 6명과 일치**한다. 죽은 참조 7명(나머지 8명 중)은 자산 어디에도
인용된 적이 없다. **우리 열 "10개"는 부풀지 않았다.**

**대신 반대쪽 의심이 열렸다** — §1의 원작 열은 "보유 2기"인데 실제 발동
확인은 "6명"이다. 둘 다 트리거 발동 확인을 거친 값인데 2와 6이 다르다.
🛑 **PM 지시로 §1의 원작 열은 이 답이 올 때까지 안 고친다** — 리서치담당이
`UNCOVERED_18_UNITS.md`가 왜 2로 셌는지 직접 판정 중이다.

**다른 등급에서도 "원작 보유 수" vs "실제 발동 유닛 수"가 어긋나는지** 같은
방법(우리가 배선한 능력ID의 실제 소유자를 전부 세기)으로 교차 확인했다:

| 등급 | 우리가 배선한 능력ID의 실제 소유자(고유 캐릭터) 수 | §1의 "원작 보유" | 일치? |
|---|---:|---:|---|
| 안흔함 | 6 | 2 | 🔴 불일치(위 내용) |
| 특별함 | 11 | 11 | ✅ 정확히 일치 |
| 희귀함 | 15 | 32 | ⚠️ 직접 비교 불가(아래 참고) |

**특별함은 11=11로 정확히 맞다** — 안흔함의 어긋남이 **전 등급에 퍼진
문제가 아니라 안흔함 하나에 국한된 것일 가능성**을 시사한다. **희귀함의
15는 32와 비교 대상이 아니다** — 이 15는 "우리가 실제로 배선한 30개
능력ID가 원작 몇 명 것인가"를 센 것이고, §1의 32는 "원작 희귀함 46명 중
스킬 가진 사람 전체"를 센 것이라 모집단이 다르다(우리가 원작 32명 중
15명분만 골라 썼다는 뜻일 뿐, 오류가 아니다) — 안흔함처럼 "우리 배선
전부의 소유자 수 = 원작 보유 수"가 성립해야 비교가 되는데, 희귀함은
원작 보유(32)가 우리 로스터 홀더(37)보다도 커서 애초에 전수가 아니다.

**PM께 드리는 답**: 01번을 오늘 아침 결정하실 근거(등급평균·배율)는 **원작
쪽 수치가 트리거-검증까지 거친 값이라 무너지지 않는다.** 다만 **"우리
안흔함/특별함/희귀함 능력 수"가 description 인용 기반이라, 그중 원작에서
실제로 발동 안 하는 능력이 섞여 있을 가능성**이 새로 열렸다 — 이게 맞으면
과잉 배율이 지금보다 커질 수도, 작아질 수도 있다(방향 미확정). 리서치담당이
`UNCOVERED_18_UNITS.md`와 같은 방법(트리거 원문 발동 확인)을 안흔함·특별함·
희귀함 전체 배선 능력에 적용해야 분자가 확정된다 — 그 전까지는 **"방향은
과잉이 맞다(등급평균 1.00배 밑이 없다), 정확한 배율만 유동적"**으로 읽어
주시면 된다.

## 6-2. 🟡[미검증] 6개 등급에 같은 역색인 적용 (PM 지시)

전설적인·제한됨·초월함·불멸의·영원한·변화됨에 §6과 같은 방법(`uabi` 전수
역색인)을 그대로 돌렸다. **영원한·불멸의(🟢)도 포함했다** — 정황(보유율
100%)이 아니라 실제로 검증한다.

### 결과 요약

| 등급 | 고유 능력ID | 같은 등급 | 원작이 더 낮음(상승) | 원작이 더 높음(흘러내림 후보) | 출처불명 | 전원 공유하는 커스텀 능력ID |
|---|---:|---:|---:|---:|---:|---|
| 전설적인 | 31 | 29\*\* | 12(재확인 후 조정, 아래 참고) | 0 | 0 | 없음(`Avul`만, 스톡 플래그) |
| 제한됨 | 13 | 22 | 0 | 0 | 0 | 없음(`Avul`·`Aloc`만) |
| 초월함 | 17 | 18 | 0 | 3→**0**(재확인) | 0 | 🔴 **`A07O`(21/30명, 70%)** |
| 불멸의 | 26 | 19 | 2 | 2→**0**(재확인) | 0 | 없음(`Avul`만) |
| 영원한 | 23 | 10 | 28(대부분 `A07N` 1건이 히든조합 23종에 뿌려짐) | 0 | 0 | 없음(`Avul`만) |
| 변화됨 | 3 | 3\*\*\* | 0 | 0(A0GD는 이미 알려진 스턴전용 공유, 아래 참고) | 0 | 확인 안 됨(아래 한계 참고) |

\*\* 전설적인은 능력ID 14개가 **등급 색코드 텍스트 없는 더미 이름**(`!전설 징베 더미`
류)에만 걸려 있어 1차 판정에서 "등급텍스트없음"으로 빠졌다 — 하지만 더미 이름
자체가 **"!전설"**로 시작해 전설적인 계열임을 이름표가 이미 말해준다(이 세션
내내 써온 "제작자 이름이 계열을 말해준다" 방법, `BLOCKERS_AND_ROSTER_DEFICIT.md`
참고). 이 14개를 전설적인으로 인정하면 same=15+14=29.

\*\*\* 변화됨은 **스크립트 버그를 하나 잡았다** — 원작 텍스트가 "변화**됨**"이
아니라 "변화**된**"이었다(`|c00ff0080변화된|r`). `GRADE_WORDS` 목록에 "변화됨"만
있어서 1차 판정에서 A041·A0S7 둘 다 "등급텍스트없음"으로 잘못 빠졌다. 직접
재조회하니 둘 다 소유자가 정확히 "변화된" 텍스트를 가진 유닛이었다 — **3개
전부 같은 등급.** 공유 커스텀 능력ID 확인은 로스터가 3명뿐이라 표본이 작아
"확인 안 됨"으로 남긴다(있어도 없어도 이상하지 않은 크기).

### "흘러내림 후보"로 잡혔던 5건 — 전부 재확인하니 진짜 공유였다

초월함(`A0IT`×2·`A0ZV`)과 불멸의(`A0YZ`·`A0Z8`)에서 처음엔 "원작이 더 높은
등급"으로 잡혔다. **각 능력ID의 소유자를 전부(하나만 보지 않고) 다시 뽑으니
전부 같은 캐릭터의 다른 폼/등급 버전이 원작에도 그 능력을 같이 갖고 있었다**
(뿌리 ㉞와 같은 패턴, §6의 `A0JW`/바르토로메오와 동일):

| 능력ID | 우리 등급 | 흘러내림처럼 보인 소유자 | **놓쳤던 같은-등급 소유자** |
|---|---|---|---|
| `A0IT` | 초월함 | `h04Q`/`h04Y` 빅맘(불멸의) | `h09J` **폭주한 빅맘(초월함)** |
| `A0ZV` | 초월함 | `h0AD` 카이도(불멸의) | `h04Z` **쵸파(초월함)** |
| `A0YZ`·`A0Z8` | 불멸의 | `h0AC` 히그마(랜덤전용[제한됨]) | `h0AE` **히그마 리얼리스트(불멸의)** |

**5건 전부 흘러내림이 아니다.** 하나의 강한 캐릭터가 여러 등급 폼을 갖고
그 폼들이 베이스 능력을 공유하는 구조 — 원작 자체가 그렇다.

### 🔴 초월함 — 안흔함·특별함·희귀함과 같은 구조 발견, [미확정]으로 강등

**초월함 원작 캐릭터 30명 중 21명(70%)이 `A07O`를 공유한다** — 안흔함의
`A07Y`/`A0JE`/`A0B8`, 특별함의 `A0JE`/`A07Y`/`A0BA`, 희귀함의 `A07Y`/`A0B9`와
**정확히 같은 패턴**(전원 공유 커스텀 능력ID + 캐릭터별 고유 1~2개). 이건
"코버리지 우려가 없는 등급"이 아니라 **같은 위험을 가진 네 번째 등급**이다.
**초월함을 🟡에서 🔴[미확정]으로 내린다.**

### 🟢 전설적인·제한됨·불멸의·영원한 — 검증 완료, 🟢 근거를 실제로 얻었다

이 네 등급은 `A07O`류 전원 공유 커스텀 능력ID가 **하나도 없다**(스톡
`Avul`/`Aloc`만 공유, 이건 안흔함 등에도 있던 것과 무관한 범용 패시브
플래그다). 흘러내림 후보도 재확인 결과 전부 기각됐고, 출처불명도 0건이다.
**정황(보유율 100%)이 아니라 이번에 실제로 §6과 같은 방법으로 확인했다** —
🟢 표시가 이제 근거를 갖는다. 다만 영원한의 `A07N`(히든조합 23종 공유)은
이미 이 세션에서 "원작에도 대응 값 자체가 없다"로 별도 확정된 결측 케이스라
같은 위험군이 아니다(공유는 하지만 "죽은 참조에 실제값을 넣은" 패턴이
아니라 "애초에 아무도 값이 없다"는 이미 닫힌 사안). **변화됨은 같은 방법을
거쳤지만 표본(3개)이 너무 작아 🟡으로 남긴다** — 위 신뢰도 열 설명 참고.

## 8. A07O — 초월함 공유 ID의 자산 쪽 실태

PM 지시대로 `A07O`가 우리 자산에 실제로 어떻게 들어와 있는지 확인했다 —
안흔함에서 리서치가 한 것의 자산 쪽 절반이다.

### ① `A07O`를 인용하는 우리 자산 — **0개**

`Assets/Data/UnitSkills/*.asset` 전체(`Assets/Data/` 전체로 넓혀도 동일)에
`A07O`를 인용하는 파일이 **하나도 없다.** 몇 명분인지 셀 것도 없이 **0명분**이다.

### ② `A07O`의 진짜 정체 — 초월함 21명이 아니라 **맵 전체 87명**이 공유

처음엔 "초월함 30명 중 21명"으로만 봤는데, `war3map_new.w3u` 전수로 다시
세니 **`A07O` 소유자는 87명**이고 등급도 초월함·영원한·제한됨·랜덤전용,
그리고 등급 색코드가 아예 없는 `H07S`~`H0BT`류(이름이 "흔함영웅"·특정
칭호뿐, 대부분 원작에서 '영웅선택'류의 스톡 슬롯으로 보인다) 등 **등급을
가리지 않는다.** 이건 초월함 전용 표식이 아니라 **맵 전체 히어로 유닛
공통의 뭔가**(스톡 패시브이거나 히어로 공통 판정용 표식)일 가능성이 높다
— **PM이 미리 경고한 대로, 공유가 많다고 마커라는 확증은 아니다**(진짜
능력일 수도 있다). 값·발동 여부는 이 세션 도구로 트리거까지 못 따라가서
**[미확인]으로 남기고 리서치담당에 넘긴다.** 다만 **우리 쪽 대응 여부는
확정**이다 — 우리는 이 능력을 어디에도 안 썼다.

### ③ 초월함에 배선한 17개 능력ID 전부 — 공유 마커가 아니라 개별 능력

우리가 초월함에 실제로 배선한 고유 능력ID 17개 전부의 **원작 전체 소유자
수**를 다시 세봤다:

```
A03W·A04D·A051·A0AG·A0AK·A0CS·A0HA·A0HK·A0HN·A0IT·A0KA·A0KE·A0X0·A0ZV·A101·A103
  → 전부 소유자 2명(대부분 §6-2에서 이미 확인한 "같은 캐릭터 다른 폼 공유" 쌍)
A0G4 → 소유자 4명(가장 많음, 그래도 A07O의 87명과는 차원이 다르다)
```

**17개 중 `A07O`류(수십 명 공유) 패턴에 해당하는 건 0개다.** 전부 "특정
캐릭터 1~2명(폼 포함 최대 4명)만의 개별 능력"이고, 안흔함·특별함·희귀함·
`A07O` 자체에서 본 "전 유닛 공유 표식"과는 구조가 다르다.

### 🟢 판정 — 초월함, 실사용 기준으로는 문제 없음. 🔴→🟢로 재상향

**PM의 판정 기준 그대로 적용**: "21명(실제로는 87명)이 공유하는 ID에 우리가
실제 피해 수치를 넣었다면" 문제인데, **우리는 그 ID를 아예 안 썼다.**
안흔함이 그랬던 것과 정확히 같은 결과다 — **구조적 위험 신호(원작에 공유
마커가 존재한다)는 실재하지만, 우리 자산이 그 신호를 실제로 밟은 적이
없다.** §6-2에서 "구조가 같다"는 이유만으로 🟡→🔴로 내렸던 판단을
**자산 확인 결과로 다시 🟢로 올린다** — 이번엔 정황이 아니라 우리 자산
전체(17개 능력ID)를 직접 대조한 결과다.

⚠️ 남는 것: `A07O` 자체가 원작에서 진짜 발동하는 능력인지 아닌지는 여전히
[미확인]이다 — 다만 그 답이 무엇이든 **우리 초월함 배선에는 영향이 없다**
(우리가 안 썼으므로).

## 9. 결론 — 사장님이 보실 것 (2026-09-07 저녁, 원작 열 정정 반영 재작성)

**기준3(`a59e113`) 원상복구 실행 완료(커밋 `eae5aa1`)** — 특별함 5건(A05E·A0DJ·
A0HZ·A0JT·A0MK, 임채준·정승준·유재헌·이정범·임장혁)의 삭감 목표치가 위 오염된
원작 값(11명·18능력, 등급평균 0.545)에 근거했다는 게 확인돼 effects를 되돌렸다
— 다시 깎지는 않았다, 정확한 수량은 재계상 후. **불멸의 A13R·영원한 A0J7 2건은
그대로 둔다** — 그 둘의 회수 근거는 등급평균 목표치가 아니라 "같은 스톡 값을
두 유닛이 원작에서부터 공유해서 대표 하나만 남긴다"는 것이라 이번 오염(등급
평균 계산 오류)과 무관하다.

🔴 **최종 결론(오늘 밤 마감)**: **하위 세 등급의 밀도는 원작과 거의 같다
(0.86~1.00배).** 처음 보고된 "안흔함 2.33배·불멸의 2.47배" 같은 과잉은
**원작 쪽 집계 오류에서 나온 허상**이었고, 정정 후 어느 등급도 원작에서
크게 벗어나지 않는다 — 특별함은 정확히 1.00배로 수렴했다. **상위
다섯 등급(전설·제한·초월·불멸·영원)은 원작 로스터 기준 자체가 아직
[미확정]이라 배수를 낼 수 없다**(`Trig_tier_Random_Actions`의 대응
그룹이 그 등급들엔 없어 가챠풀 기준을 못 쓴다, 위 §0 참고). `DENSITY_
SOURCE_AUDIT_2026-09-07.md`가 독립적으로 이 다섯 등급의 원작 열이
`UNCOVERED_18_UNITS.md` 자체와도 안 맞는다는 걸 이미 확인해뒀다.

### 오늘 밤 이 숫자가 움직인 기록 — "이 표 하나만 믿지 말 것"

이 표는 하룻밤 사이 **다섯 번** 바뀌었고 그때마다 근거가 있었다 —
다음에 이 표를 보는 사람이 "이 숫자"만 보고 결정하지 않도록 남긴다:

```
안흔함   2.33배 → 1.04배(원작 2/4→7/9, 로스터14) → 0.96배(로스터 13으로 재확정)
특별함   1.28배 → 0.79배(원작 11/18→26/29) → 1.10배(원작 오염 6건 제거, 29→21)
                → 1.00배(우리 쪽도 같은 오염 2건 발견, 23→21) ← 최종
희귀함   1.11배 → 0.89배(원작 32/50→65) → 0.84배(로스터 45로 재확정)
                → 0.86배(우리 쪽도 같은 오염 1건 발견, 52→51) ← 최종
```
**매번 "왜 바뀌었나"가 문서에 남아 있다** — 로스터 정의 변경 2회, 원작
분자 재계상 2회, 원작/우리 양쪽의 "피해 아님 base" 오염 제거 각 1회.

1. **안흔함 — 과잉 소멸.** 완전 재계상(로스터14·보유7·능력9) → 등급평균
   0.643 vs 우리 0.667, **배수 1.04배(사실상 일치).** 깎을 근거 없음.
2. **특별함 — 확정, 우리가 원작보다 적다.** 로스터 분모를 PM이 독립적으로
   재현해 33으로 확정했다(리서치의 42는 "공격더미"를 캐릭터로 오포함한
   것으로 보임). 능력 26/29 → 등급평균 0.879 vs 우리 0.697, **배수 0.79배.**
   안흔함에 이어 **두 번째로 "과잉"이 뒤집힌 등급이다.**
3. **희귀함 — 확정, 우리가 원작보다 적다.** 로스터 48로 확정(PM 독립 재현).
   능력 42/65 → 등급평균 1.354 vs 우리 1.209, **배수 0.89배.** 이 세 등급
   중 **둘(특별함·희귀함)이 "부족"으로 뒤집혔다** — "하위 등급 과잉"이라는
   01번의 출발점 자체가 무너졌다.
4. **전설적인·제한됨·초월함·불멸의·영원한 — "🟢"을 전부 철회한다.**
   `DENSITY_SOURCE_AUDIT_2026-09-07.md`가 `UNCOVERED_18_UNITS.md` 자체 최종
   표와 직접 대조한 결과, **초월함(85 vs 103, 18개 차이)·전설적인(71 vs 72)·
   영원한(39 vs 40)·불멸의(52 vs 54)·변화됨(11 vs 13, 로스터 수까지 다름)
   전부 불일치**로 확인됐다. 안흔함·특별함·희귀함이 전부 과소계상(같은 방향)
   이었다는 점을 감안하면, **이 다섯도 과소계상 쪽으로 의심하는 게 합리적이다**
   — 초월함이 가장 크다(103이 맞다면 등급평균이 1.328→1.609로 올라가 배수가
   1.81배→약 1.49배로 낮아진다). **다섯 등급 전부 [무효]로 내리고 리서치의
   같은 방법(트리거 직결 RRD·atar 패시브·중복 등재 확인)을 기다린다.**
5. **불멸의·초월함은 그래도 로스터 결손(§4)은 별개 사실로 남는다** — 원작
   열이 위로 조정돼도 "로스터 자체가 좁다"는 결론은 안 바뀐다. 다만 정확한
   배수는 재검증 전까지 전부 유보.
6. **다른 문서로의 오염 확산 — 목록만, 내용 검토는 안 함(시간 관계상)**. 옛
   "안흔함 2/4"·"특별함 11/18"·"희귀함 32/50"을 인용하는 문서를 검색하면:
   `CRITERION3_TARGET_LIST_2026-09-06.md`(⚠️ **이미 실행된 결정 문서** —
   `a59e113`으로 커밋된 기준3 삭감이 이 숫자에 근거했을 수 있다, 우선 확인
   필요) · `DENSITY_REMEASURE_ABILITY_ID_2026-09-06.md`(원 출처) ·
   `DENSITY_DECISION_ANALYSIS_2026-09-06.md` · `DUMMY_OWNER_AND_SKILL_DENSITY.md` ·
   `ORIGINAL_VALUES.md` · `UNIT_MAPPING_RESEARCH.md` · `UNIT_STATS_RESEARCH.md` ·
   이 문서 자신의 §2/§4/§6/§6-2(안 고친 부분) · 내 `COVERAGE_TRIM_CANDIDATES_
   2026-09-07.md`(안흔함 목표 보유 수 계산이 옛 "2/14" 기준이라 다시 내야 함).
   **가장 급한 건 `CRITERION3_TARGET_LIST_2026-09-06.md`다 — 이미 실행된
   삭감이 잘못된 숫자 근거였다면 되돌릴지 여부를 사장님이 아셔야 한다.**
7. **다음 단계**: 리서치담당이 (a) 특별함·희귀함 완전 재계상(`UNCOVERED_18_
   UNITS.md` 수준의 전수 RRD·오더 추적), (b) 🟢 다섯 등급의 원작 열 오염 여부
   확인, (c) `CRITERION3_TARGET_LIST_2026-09-06.md`가 옛 숫자에 근거했는지
   확인. 이 셋이 끝나야 밀도표 전체가 확정된다.

커밋 예정: 이 문서만(수정).
