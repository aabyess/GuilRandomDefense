# 보물찾기(탐색) 이벤트 — JASS 검증 전체 사양 (2026-09-12, 리서치담당)

> 조사·문서 작성만. 코딩 없음. 출처는 전부 `Tools/w3x/원본/war3map_new.j`(직접 문자열 검색)와
> `war3map_new.w3u`/`war3map_new.w3a`/`war3map.w3t`(유닛·능력·아이템 바이너리 파싱)이다.
> `[파일확인]` = 원문에서 직접 읽음, `[미확인]` = 근거 없음(추측 금지), `[엔진표준 추정]` =
> WC3 필드명 컨벤션으로 추정했으나 이 맵 파일로는 100% 확정 못함.
> PM이 준 09-03 웹조사 요약(`EVENTS_RESEARCH.md`)은 근거로 인용하지 않는다
> (`docs-are-not-evidence`) — 값이 맞는지는 전부 원문으로 새로 검증했고, 아래에서
> 몇 군데는 그 웹조사가 틀렸다는 것도 같이 확인했다.

---

## 0. 트리거 전수 — "보물·탐색" 이름이 붙은 건 딱 하나뿐이다

`treasure`/`Treasure`/`search`/`Search`/`탐색`/`보물` 키워드로 전체 8,156개 함수명을 훑은
결과, **`Trig_treasure_*`(9개 함수) 하나뿐**이다. 다만 "탐색"이라는 능력을 쓰는 다른 유닛
전용 서브 트리거가 하나 더 있다(`Trig_sinobu_Exploration_*`, §6-2). 이름에 "탐색"이 없는
`sido`(시도) 계열(`Trig_Save_sido*`)은 세이브/로드 시스템이라 이 이벤트와 무관 — 검색은
됐지만 무관함을 확인만 하고 더 파지 않았다.

| 트리거 | 역할 |
|---|---|
| `InitTrig_treasure` | 플레이어 0~3의 `EVENT_PLAYER_UNIT_SPELL_EFFECT`(스펠 시전 완료)에 등록 |
| `Trig_treasure_Conditions` | 시전된 능력이 `AHta`(탐색)인지 체크 |
| `Trig_treasure_Actions` | 클릭 지점 주변에서 `n00H`(보물상자) 유닛을 찾아 `Trig_treasure_Func007A`를 실행 |
| `Trig_treasure_Func006002003` | 그룹필터 조건: `GetUnitTypeId()=='n00H'` |
| `Trig_treasure_Func007A` | **실제 보상 지급 로직 전부**(§4) |
| `Trig_treasure_Func007Func006/007/008C` | `udg_treasure`(1~3 랜덤값) 분기 조건 |
| `Trig_treasure_Func007Func009C` | `udg_treasurequest==9`(9번째 발견) 조건 |

## 1. 해금 조건 — 🔴 "10라운드 보스 처치 후"가 아니라 "10의 배수 라운드 시작 시"

보물상자(`n00H`)의 유일한 생성 코드는 `Trig_Round_10ver_Actions` 안, 다음 조건이 전부
참일 때다:

```
Stage[GlobalTV]==2               (Trig_Round_10ver_Func014C)
AND NOT Level==61                (Func014Func008C가 거짓 — 61이면 스킵)
AND Level<61                     (Func014Func008Func001C)
AND ModuloInteger(Level,10)==0   (Func014Func008Func001Func001C)
```

이 블록은 **보스 사망 여부와 무관하게, 그 라운드가 시작되는 시점에 곧바로** 실행되고,
**같은 스코프 안에서 그 직후 `ConditionalTriggerExecute(gg_trg_Enemy_Boss_create)`로 보스
전투도 같이 시작**한다(`75.40초 보스 타임리밋` 타이머도 이 블록에서 같이 켜진다). 즉
**보물상자는 10·20·30·40·50·60라운드가 시작될 때(보스전 시작과 동시에) 스폰되고, "보스를
잡아야 나온다"는 순서 관계는 코드에 없다.** `[파일확인]`

🔴 **"초고속 모드"는 이 맵에 존재하지 않는다.** `초고속`/`고속모드`/`speedmode` 등 어떤
철자로도 전체 JASS에 0건 — PM이 준 웹조사의 "초고속 모드는 1라운드부터 탐색 가능" 항목은
**근거가 없다.** 난이도 6종(쉬움~악몽, `DIFFICULTY_SPEC_2026-09-11.md`)과도 무관하게, 이
스폰 조건에는 `udg_Mode` 비교가 전혀 없다 — **난이도 무관, 항상 라운드 10/20/30/40/50/60
여섯 번뿐**이다. (61라운드 이후 "신세계" 구간엔 이 스폰 블록 자체가 안 돈다 — 신세계에서
보물상자가 새로 생기는지는 이번 조사로 못 찾았다, 관련 코드 0건 `[미확인]`.)

## 2. 탐색 조작 — 능력 `AHta`, 클릭 지점 기준, 쿨타임은 필드값 우선

`Trig_treasure_Conditions`가 잡는 건 `GetSpellAbilityId()=='AHta'` 하나뿐이다. `AHta`는
**단축키 R("탐색")**, `war3map_new.w3a`에서 직접 읽은 값:

| 필드 | 레벨1 | 레벨2 |
|---|---|---|
| `aare`(범위/AoE) | 750 | 750 |
| `acdn`(쿨타임, 실제 엔진값) | **100초** | **70초** |
| `adur`/`ahdu`(지속) | 3초 | 3초 |
| `aran`(사거리, `[엔진표준 추정]`) | (미지정, 상속) | **99999**(사실상 무제한) |

⚠️ **툴팁 텍스트(`aub1`)와 실제 필드값이 레벨2에서 다르다** — 툴팁은 "쿨타임 80초"라고
쓰여 있지만 `acdn` 필드의 실제 값은 **70초**다. 실행에 쓰이는 건 `acdn`이지 툴팁 문자열이
아니므로, **구현 시엔 70초를 써야 한다** — 이 맵 자체에 툴팁 오기가 있었다는 뜻이고, PM이
준 웹조사의 "쿨타임 100초"는 레벨1(기본) 값만 반영한 것으로 보인다(레벨2 존재를 몰랐을
가능성). `[파일확인]`

레벨2는 아이템 `I00K` 습득 시 부여된다(`Trig_item_up_Actions`:
`SetUnitAbilityLevelSwapped('AHta',GetTriggerUnit(),2)`) — 그 유닛 자신에게 걸리는
레벨업이라 **아이템을 먹은 유닛 개인** 단위다(플레이어 전체 아님). `[파일확인]`

`Trig_treasure_Actions`는 `GetSpellTargetLoc()`(클릭한 지점, 시전 유닛 위치가 아님)을
중심으로 `udg_treasure_range_int[pid]` 반경 내 `n00H` 전부를 찾는다 — **AHta 자체의
`aare`(750)가 아니라 별도 전역변수 `treasure_range_int`가 실제 탐지 반경으로 쓰인다**
(§5-2에서 이 값이 플레이어별로 나중에 바뀐다). `[파일확인]`

## 3. 보물 위치 — 단일 rect 내 완전 무작위, 사전 정의 좌표 목록 없음

```
gg_rct_treasure_chest_zone = Rect(-5856.0, -2400.0, 2464.0, 6080.0)   (맵에 딱 하나)
```

스폰 코드는 `GetRandomLocInRect(gg_rct_treasure_chest_zone)`를 **`udg_treasure_box_int`번
반복**해서 각기 독립적으로 뽑는다 — 이름표가 붙은 좌표 목록이나 서브존 배열은 없다.
`udg_treasure_box_int`는 기본 **7**(`InitGlobals`), 전설나미 조합 후 **9**(§5-2)로 전역
1회 변경된다(플레이어별 아님, 게임 전체 공용 상수). `[파일확인]`

⚠️ `AHta` 툴팁은 "창고,조합식 제외 랜덤한 위치"라고 적어 두었지만, **스폰 코드 자체에는
창고·조합식 지역을 걸러내는 조건이 없다** — 단일 rect 안에서 순수 무작위이므로, 창고·조합
구역이 실제로 제외되는 이유가 있다면 그건 **`gg_rct_treasure_chest_zone`이라는 rect 자체가
월드에디터에서 그 구역들을 미리 피해서 그려졌기 때문**이고, 런타임 필터링 로직은 아니다.
`[파일확인 — 배제 로직 0건]`

## 4. 찾는 판정과 보상표 — `Trig_treasure_Func007A` 전문 기준

`n00H`(보물상자)는 `Aloc`(Locust, 투명·선택불가) + 모델 `.mdl`(빈 모델) 유닛이라 **평소엔
전혀 보이지 않는다.** 판정은 순수하게: **클릭 지점으로부터 `treasure_range_int[pid]` 반경
내에 있는 `n00H` 전부**(유닛 종류 체크만, 시야·이동능력·탐지 조건 전혀 없음) — **원거리에서
찍어보는 "블라인드 서치"가 설계 의도**로 보인다. `[파일확인]`

🔴 **한 번의 시전으로 반경 내 상자가 여러 개면 전부 동시에 발견된다**(`ForGroupBJ`가 그룹
전체를 순회) — "1회 시전 = 최대 1개 발견"이 아니다. `[파일확인]`

찾은 상자마다 `udg_treasure=GetRandomInt(1,3)`으로 **1/3씩 균등 확률**로 세 보상 중 하나:

| `treasure` 값 | 보상 | 지급 유닛 | 지급 개수 |
|---|---|---|---|
| 1 (1/3) | 랜덤위습 | `e0IX` | `udg_TreasureChest[pid]`개(기본 1, §5-2 조합 후 2) |
| 2 (1/3) | 흔함선택위습 | `e018`, `gg_rct_StoryReward_Base1` 위치 | 〃 |
| 3 (1/3) | 안흔함 위습 | `e017`, `gg_rct_StoryReward_Base2` 위치 | 〃 |

보상은 **찾은 플레이어 개인에게만** 간다(`GetOwningPlayer(GetTriggerUnit())`). `[파일확인]`

## 5. 세이브 +2 / I002 카운트 — 전부 이 함수 맨 끝, 그러나 서로 다른 스코프다

`Trig_treasure_Func007A` 맨 끝은 **매번 상자를 찾을 때마다** 4인 전원을 도는 루프를 하나
더 돈다(보상 종류·treasurequest 값과 무관하게 항상 실행):

```jass
loop (1..4)
  call IncUnitAbilityLevelSwapped('A0MX', udg_Q_buliding_Cooperation[p])
  call SetItemCharges(udg_Item_Treasure[p], GetItemCharges(udg_Item_Treasure[p])+1)
endloop
```

**이 카운트는 발견한 사람만이 아니라 4명 전원에게 동시에 올라간다** — 즉 "누가 발견했는지"
와 무관하게 파티 전체가 공유하는 진행도다. `[파일확인]`

- `udg_Item_Treasure[p] = UnitItemInSlotBJ(udg_Q_Buliding[p], 1)`(모드선택 시 1회 대입,
  `Trig_Select_effect_Actions`). `udg_Q_Buliding[p]`는 `h07C` 유닛이고, 그 유닛의 슬롯
  0에는 게임 시작 시 정적으로 **`I002`("찾은 보물 개수")** 한 개가 놓인다
  (`UnitAddItemToSlotById(h07C,'I002',0)`, `CreateBuildingsForPlayer0~3`). `I002`는
  `war3map.w3t`에서 확인: `iabi='A109'`(⚠️ 무효과 스텁 — `item-research-traps` 메모리와
  동일 패턴), 순수 카운터용 아이템. `UnitAddItemToSlotById`의 슬롯 인자(0)와
  `UnitItemInSlotBJ`의 슬롯 인자(1)가 물리적으로 같은 슬롯을 가리키는지는 **이 두 네이티브의
  BJ 래퍼 소스가 맵 파일 안에 없어서(엔진 내장) 100% 확정은 못 하지만**, WC3 커뮤니티
  표준 관례상 `UnitItemInSlotBJ`가 1-인덱스를 0-인덱스로 변환해서 넘기는 게 일반적이라
  **`Item_Treasure[p] == I002` 인스턴스로 강하게 추정한다** `[엔진표준 추정, 상황증거로는
  거의 확정 — I002가 카운터 전용 스텁 아이템이라는 점과 완전히 일치]`. → **I002 충전량 =
  그 판에서 팀 전체가 지금까지 찾은 보물상자 총수.**
- `udg_treasurequest`(전역 정수, 플레이어 구분 없음)는 **상자를 찾을 때마다 무조건 +1**
  (`Trig_treasure_Func007A` 최상단). **정확히 9가 되는 순간**(`Func007Func009C`) 별도로
  "도전과제-트레저헌터" 완수 처리가 실행된다:
  ```jass
  전원에게 목재 +2, Q_buliding_Cooperation에서 A0MX 제거하고 A035로 교체(업그레이드 완료 표시),
  present_Save_playpoint[p] += 2, Save_playpoint[p] += 2   (전원, 루프 1~4)
  ```
  → **"세이브 +2"는 매 발견마다가 아니라, 팀 전체가 정확히 9번째 상자를 찾는 그 순간
  단 한 번만** 일어난다. `[파일확인]`

## 6. 탐색범위·보상 증가 — 나미(전설) 2배 보상은 실제로 있다, 단 "영원" 나미와는 다른 별개 유닛이다

### 6-1. 전설 나미(`h02P` 도둑고양이 나미, 능력 `A04X`) — PM 웹조사의 "2배 보상"이 정확히 여기서 나온다

`A04X` 툴팁 원문: *"도둑 고양이 나미를 한번이라도 조합했을 경우 탐색범위가 15% 증가하고
앞으로 보물 탐색 성공시 2배의 보상을 얻게 됩니다. 10라운드마다 리젠되는 보물상자의 개수가
2개 증가합니다(조합시 플레이어 전체 한번만 적용)."* — 이 세 문장이 `Trig_UnitJohabCounter_Actions`
(맵 전체를 도는 `UNIT_TYPE_GIANT` 진입 감지 트리거, `A04X` 레벨1 보유 + `Nami_legend_Boolean`
가드로 팀 전체 1회만 발동)의 실제 코드와 **전부 정확히 일치한다**:

```jass
udg_treasure_box_int      = 9        // 7+2, 툴팁의 "+2개"와 일치
udg_treasure_range_int[p] = 863.00   // 750×1.15=862.5≈863, 툴팁의 "+15%"와 일치
udg_TreasureChest[p]      = TreasureChest[p]+1   // 기본1→2, §4의 "지급 개수"가 그대로 2배가 됨
                                                   // = 툴팁의 "2배의 보상"과 정확히 일치
udg_Gold_Plus[p]          += 0.60    // 툴팁엔 없는 별도 보너스(공통 골드 배율)
```

**결론: PM 웹조사의 "나미(전설) 보유 시 보상 2배"는 사실이다 — 다만 그 실체는 "보상에
곱연산 2×를 거는" 게 아니라, "발견 시 지급 개수(`TreasureChest`)가 1에서 2로 늘어나는" 것이다.**
결과적으로 위습 개수가 정확히 2배가 되므로 체감상 완전히 동일하다. `[파일확인]`

### 6-2. 영원 나미(채팅 "날씨는맑음"/"nami tr" 획득) — 별개의, 더 큰 범위 보너스

`Trig_Eternal_Nami_Actions`(전설 나미와 무관한 별개 트리거, 채팅명령+5개 재료유닛 조건으로
나미를 **영웅으로 영입**)도 `treasure_range_int[p]`를 만진다: **`=940.00`으로 직접 대입**
(가산이 아니라 절대값 대입). 그 외 보상 배율·상자 개수 변경은 없다(§4 지급 개수엔 손대지
않음, `Gold_Plus`만 +0.20 별도 추가).

🔴 **둘 다 대입(`=`) 연산이라 가산되지 않는다** — 전설 조합(863) 후 영원 영입(940)이
일어나면 최종값은 940(더 최근에 실행된 대입이 이긴다), 반대 순서로 일어나면 최종값은
750이 아니라 여전히 863에 그친다(순서가 뒤바뀌면 863이 940을 못 덮어씀). **구현 시 이
순서 의존성을 그대로 두면 원작과 다른 결과가 나올 수 있다** — 두 이벤트의 실제 발생
선후관계(어느 게 먼저 오는 조합/영입 경로인지)는 이번 조사 범위 밖이라 `[미확인]`.

## 7. 모건(`h05H`)의 "100초마다 추가 탐색" — 근거 없음, AHta 쿨타임 오독으로 보인다

전체 JASS에서 `모건`/`Morgan`/`h05H`을 검색하면 **`CreateUnitsForPlayer7`의 정적 배치
1건뿐**이고, 모건 전용 트리거나 주기적 타이머는 0건이다. 모건의 `uabi`는 `AHta,A0EP,Avul`
— `AHta`는 §2의 그 "탐색" 능력 그대로(고유 능력이 아니라 다른 캐릭터들도 갖는 공용 능력),
`A0EP`("!탐욕-모건")는 평타에 11% 확률로 8배+마법피해 5만을 추가하는 **패스캐스트형
자동효과**(`base=ACbh`)로, 툴팁에 "◎탐색 사용 가능"이라는 문구가 있을 뿐 자동 재탐색과는
무관하다. **"100초마다 추가 탐색"은 AHta 레벨1의 쿨타임(100초)을 자동 발동으로 오독한
것으로 보인다 — 코드상 모건에게 주기적/패시브 탐색은 없다.** `[미확인 — 오히려 반증 근거 있음]`

## 8. `AHta`를 실제로 쓸 수 있는 유닛 — 🔴 정정: 전 플레이어의 항해일지(H0C4)가 진짜 답이다

> **PM 검수 정정**: 초판은 "건물군은 용도 불명, 실사용은 모건·시노부(제한됨) 둘뿐"이라
> 썼는데 틀렸다. **모든 플레이어가 자기 항해일지(`H0C4`)로 탐색할 수 있다**가 맞는
> 결론이다 — `H0C4`는 게임 시작 시 4인 전원에게 하나씩 정적 배치되는(`CreateUnitsForPlayer0~3`)
> **기지 건물**이고, 그 `uabi`에 `AHta`가 원래부터 들어있다. 아래에 다시 검증했다.

`war3map_new.w3u`에서 `uabi`에 `AHta`를 포함한 유닛 9종을 전수 확인:

| 유닛 | 등급/역할 | 비고 |
|---|---|---|
| `H0C4` 항해일지 | **4인 전원의 기지 건물**(1인당 1개, 정적 배치) | `upoi=22`, `uabi='Asud,AHta,AInv,Avul'`. **이게 실제로 플레이어가 접근 가능한 탐색 창구다** |
| `h05H` 모건 | 특수함 | §7, 캐릭터 고유 4스킬 세트의 일부(AHta는 이 캐릭터의 것이 아니라 공용 능력을 얹은 것) |
| `h084` 시노부 | 제한됨 | 캐릭터 고유 스킬 세트의 일부(마찬가지로 공용 AHta) |
| `e0DL` 더미(도전과제 초기화용) | 더미 | 용도상 실사용 의문 |
| `H0C0` 도움소 잠그기 / `H0C1` 도박광 / `H0C2` 연합세력 / `H0BZ` 패왕의길 / `H0BQ` 최고의도움 / `h0BW` 항해일지-실험용 | **전부 0건 — JASS 어디에도 이 5+1개 유닛ID가 등장하지 않는다**(`CreateUnit`/스톡/트리거 참조 전부 0) | §9-1에서 이게 뭔지 확정 — H0C4가 "변신"하는 대상이 아니라, 전혀 다른 메커니즘(항법 스킬 5택1)의 **결과물 이름을 그대로 딴 미사용 더미 유닛**이다 |

→ **탐색을 실제로 쓸 수 있는 건 (1) 전원의 항해일지(H0C4) 자체, (2) 모건, (3) 제한됨
시노부 — 이 세 갈래다.** H0C4는 전원 공용이라 실질적으로 "탐색은 항상 누구나 가능"이고,
모건·시노부는 그 캐릭터를 얻으면 추가로(같은 `AHta`를) 갖게 되는 보너스 접근 경로다.
`[파일확인]`

### 8-2. 참고: 시노부(전설적인, `h042`)의 별개 능력 `A0YP` — 탐색이 아니라 "위치 감지"

`h042` |시노부|(전설적인 그레이드, `h084`와는 별개의 다른 유닛)는 `AHta`가 없고 대신
**`A0YP`("!시노부 보물찾기")**를 가진다 — `Trig_sinobu_Exploration_Actions`가 이 시전을
잡아 `n00H`를 탐지하고 **`PingMinimapLocForForce`(미니맵 핑)로 알려줄 뿐, 상자를 없애거나
보상을 주지 않는다.** 쿨타임 300초, 시전시간 1초, 자기 자신 중심(범위 0). **§4의 "보상
찾기"와 이 "위치 감지"는 완전히 다른 두 시스템**이다 — 이름이 비슷해서(둘 다 "탐색"/
"보물찾기") 혼동하기 쉬우니 구현 시 분리해서 다룰 것. `[파일확인]`

## 9. PM 추가 요청 3건

### 9-1. `H0C0`/`H0C1`/`H0C2`/`H0BZ`/`H0BQ`의 정체 — 건물 분기가 아니라 "항법"(Navigation) 스킬 5택1의 미사용 더미다

이 5개 유닛은 **JASS에 전혀 등장하지 않는다**(`CreateUnit`도, 스톡도, 어떤 트리거 참조도
0건) — H0C4가 실제로 이것들 중 하나로 "변신"하는 코드는 **존재하지 않는다.** 대신, H0C4 자신의
`uhab`(영웅 능력 목록) 필드에 걸린 **5개의 서로 다른 능력**(`A15W`/`A15L`/`A15X`/`A15K`/`A15M`,
전부 `base=ACbh`, `aher=1`)이 진짜 메커니즘이다 — 각 능력 툴팁이 하나같이 *"※항법은
한개만 선택 가능합니다"*라고 명시한다. 이 5개 유닛의 `unam`(도움소 잠그기/도박광/연합세력/
패왕의길/최고의도움)은 그 5개 능력의 이름과 **정확히 1:1로 일치**한다:

```
A15W "!!!@#항법-도움소 잠그기"        ↔ H0C0 "도움소 잠그기"
A15M "!!!@#항법-도박광(도박)"          ↔ H0C1 "도박광"
A15K "!!!@#항법-연합세력(다상위)"      ↔ H0C2 "연합세력"
A15L "!!!@#항법-패왕의길(원딜)"        ↔ H0BZ "패왕의길"
A15X "!!!@#항법-최고의 도움(도움소 강화)" ↔ H0BQ "최고의도움"
```

**결론: 이 5개 유닛은 건물 변신 목적지가 아니라, 5개 항법 스킬의 이름을 그대로 딴
사용되지 않는 더미 레코드다** — 아마 맵 제작자가 오브젝트에디터에서 이름을 맞춰보려고
만들어 둔 참고용 복사본으로 보이며, 게임 로직에는 전혀 연결돼 있지 않다.

실제 선택 메커니즘은 `InitTrig_onedill_Tech`가 **4인 전원의 H0C4 인스턴스에
`EVENT_UNIT_HERO_SKILL`(영웅 스킬 습득 이벤트)를 등록**하고, `GetLearnedSkillBJ()`로
어느 능력을 배웠는지 판별한다 — 즉 **H0C4 건물 자체가 "영웅처럼" 스킬 포인트를 배분받아
5개 능력 중 하나를 배우는 것**이 원작의 진짜 방식이다. 각 분기의 실제 효과
(`Trig_onedill_Tech_Actions`에서 확인):

| 항법 | 학습 시 즉시 효과(코드) | 추가 효과(툴팁만, 코드 미확인) |
|---|---|---|
| 패왕의길(`A15L`) | `R02E` 연구, `Damage_level_Fixed[p]+2` | 최상위조합 1기 제한(랜덤전용 제외)·폭발피해+10%·적 이속 추가-5%·보스처치 시 목재+1(신세계 전까지) |
| 연합세력(`A15K`) | `Tech_union[p]=true` (플래그만, `Trig_BossReward_Actions`가 이 플래그로 별도 체크 — 보스처치 보상 쪽 로직, 이번 조사 범위 밖 `[미확인]`) | 전설·히든·제한급 유닛 조합마다 랜덤위습+1 |
| 도박광(`A15M`) | `Dobak_Tech_int[p]=1` | 고급/다른세계 도박 실패시 토큰 2배+목재1, 리롤 성공률 100%+횟수 1회 증가 |
| 최고의도움(`A15X`) | `R037` 연구, `Manso[p]`(도움소 건물)의 `A0ID`/`AOeq`/`A0JR` 레벨을 2로 | 도움소 마나회복+0.35/초, 어쓰퀘이크 데미지+100만, 버스터콜 쿨/마나-30%, 해루석 강화 |
| 도움소 잠그기(`A15W`) | `Tech_No_support[p]=true`, `R036` 연구 | 도움소 최대마나 100으로 감소, 스토리8 격파시 히든 레일리(희귀함) 지급, 아이템 도박 시 희귀함 이상 보장+전설이상 확률 증가 |

이 5개는 **보물찾기 시스템과 직접 관련 없다**(H0C4가 AHta를 갖고 있다는 사실 하나만
공유할 뿐, 항법 선택 자체는 탐색범위·보상에 손대지 않는다) — PM이 `AHta` 보유 유닛
전수에서 같이 걸려 나온 것이라 여기 정리했다. `[파일확인]`

### 9-2. `AHta` 레벨2 부여 조건 — I00K가 맞다, 단 **H0C4 계열(`upoi==22`)에 습득될 때만** 걸린다

`Trig_item_up_Actions`의 `I00K` 분기(`SetUnitAbilityLevelSwapped('AHta',GetTriggerUnit(),2)`)
는 최상위 `if(Trig_item_up_Func005C())` 안에 있고, 그 조건은:

```jass
function Trig_item_up_Func005C takes nothing returns boolean
  return GetUnitPointValue(GetTriggerUnit())==22
```

`upoi==22`인 유닛은 `war3map_new.w3u` 전수 검색 결과 **정확히 H0C4/H0C0/H0C1/H0C2/H0BZ/H0BQ/h0BW
7종뿐**(§9-1의 그 미사용 더미 6종 + 실제 건물 H0C4) — 모건·시노부는 `upoi=22`가 아니다.
즉 **I00K 아이템을 얻어 AHta가 레벨2(쿨타임 100→70초, §2)가 되는 건 그 아이템을 자신의
항해일지(H0C4) 인벤토리에 넣었을 때뿐**이다(`H0C4`의 `uabi`에 `AInv`가 있어 건물도
아이템을 인벤토리에 넣을 수 있다 — `EVENT_PLAYER_UNIT_PICKUP_ITEM`이 건물 픽업에도
반응). 모건·시노부가 직접 이 아이템을 먹어도 레벨업하지 않는다(`upoi` 불일치로 조건 실패).
`[파일확인]` — PM이 인용한 정확한 원문 행 번호(83894)는 이쪽 사본 기준(우리 `war3map_new.j`를
`\n` 기준으로 세면 83846행, `\r` 기준 세면 사실상 무의미 — 이 파일은 코드 본문은 `\n`,
툴팁 문자열 내부만 군데군데 `\r\n`이라 PM의 줄셈법과 48행 정도 차이가 나는데 어느 쪽이
맞는지는 확인 못했다 `[미확인 — 함수명으로 대신 인용]`).

### 9-3. 보물 구역의 위치 — 4개 레인 전체를 통째로 덮는 큰 사각형, 창고는 밖·조합소는 안

`gg_rct_treasure_chest_zone`은 **4개 레인 rect(`gg_rct_p1_life_zone`~`p4_life_zone`)를
전부 완전히 포함**한다(4개 전부 X·Y 양축 모두 `treasure_chest_zone` 안에 들어간다,
좌표 대조 결과):

| Rect | 좌표(X1,Y1,X2,Y2) | 폭×높이 | `treasure_chest_zone`과의 관계 |
|---|---|---|---|
| `treasure_chest_zone` | (-5856,-2400)~(2464,6080) | 8320×8480 | — |
| `p1_life_zone` | (-5440,2848)~(-2240,5600) | 3200×2752 | **완전 포함** |
| `p2_life_zone` | (-1280,2848)~(1984,5536) | 3264×2688 | **완전 포함** |
| `p3_life_zone` | (-5504,-1248)~(-2240,1440) | 3264×2688 | **완전 포함** |
| `p4_life_zone` | (-1440,-1248)~(1856,1472) | 3296×2720 | **완전 포함** |
| `johab1`~`johab4`(조합소, 각 레인 내부의 작은 지점) | 예: johab1 (-3904,3520)~(-3648,3680) | 256×160 | **완전 포함**(레인 안에 있으니 자동으로 포함) — §3에서 이미 확인한 "런타임 배제 코드 없음"이 좌표로도 재확인됨: 조합소는 실제로 그 구역 **안에** 있어서, 배제하려면 반드시 별도 필터가 있어야 하는데 없다 |
| `warehouse1`~`warehouse4`(창고) | 예: warehouse1 (3488,3456)~(5312,4736) | 1824×1280 | **완전히 밖**(X≥3488 > treasure_chest_zone의 X 최대 2464) — "창고 제외"는 이 zone이 창고 구역 자체를 안 덮기 때문이라 자연히 성립 |

→ **보물상자는 4개 레인 전역(플레이어의 유닛 배치 공간 전체) 어디에나 무작위로 나타난다.**
창고가 배제되는 건 우연이 아니라 이 rect가 원래 창고까지 안 뻗어서다. 조합소는 명목상
"제외"라고 툴팁에 쓰여 있지만 실제로는 zone 안에 있다 — 다만 조합소 자체가 매우
작아서(256×160, 전체 zone 면적의 0.06% 미만) 실제로 그 위에 상자가 뜰 확률은 낮다.

**레인 크기 대비 탐색 반경 비율**(우리 맵으로 옮길 때 쓸 것 — 4개 레인 평균: 폭 3256,
높이 2712):

| 반경 | 값 | 레인 폭 대비 | 레인 높이 대비 |
|---|---|---|---|
| 기본 | 750 | **23.0%** | **27.7%** |
| 전설 나미 조합 후 | 863 | **26.5%** | **31.8%** |
| 영원 나미 영입 후 | 940 | **28.9%** | **34.7%** |

전체 맵 대비 `treasure_chest_zone` 자체의 비율도 참고용으로: 전체 플레이구역
(`gg_rct_Playzone`, 18752×17568) 대비 **폭 44.4% / 높이 48.3%**. `[파일확인, 좌표
직접 대조]`

---

## 구현자가 옮길 표

| 항목 | 값 | 원문 근거 |
|---|---|---|
| 조작 능력 | `AHta`(R키, "탐색"), 클릭 지점 중심 | §2, `Trig_treasure_Conditions` |
| 쿨타임 | L1=100초 / L2(`I00K`를 **H0C4 자신의 인벤토리**에 넣었을 때)=**70초**(툴팁 "80초"는 오기) | §2·§9-2, `war3map_new.w3a acdn` |
| 해금 조건 | 🔴 **별도 해금 절차 없음 — 전원의 항해일지(`H0C4`) 자체가 처음부터 `AHta` 보유**. 모건·시노부(제한됨)를 얻으면 그 캐릭터로도 추가 시전 가능(같은 능력) | §8 |
| I00K 레벨업 대상 제한 | `GetUnitPointValue==22`인 유닛(H0C4 계열)에만 적용 — 모건·시노부가 직접 먹으면 효과 없음 | §9-2 |
| 항해일지의 "항법" 5택1(A15W/L/X/K/M) | 보물찾기와 무관한 별개 시스템(연합세력/도박광/패왕의길/최고의도움/도움소잠그기) — `H0C0`/`H0C1`/`H0C2`/`H0BZ`/`H0BQ`는 그 이름을 딴 미사용 더미일 뿐, 건물이 실제로 그걸로 바뀌지 않음 | §9-1 |
| 상자 스폰 시점 | **라운드 10·20·30·40·50·60 "시작"과 동시**(보스전 시작과 겹침, 보스 처치와 무관) | §1, `Trig_Round_10ver_Actions` |
| 난이도/모드별 차이 | **없음**(전 모드 동일) — "초고속 모드"는 이 맵에 존재하지 않음 | §1 |
| 상자 위치 | 단일 rect(`gg_rct_treasure_chest_zone`, `(-5856,-2400)~(2464,6080)`) 내 완전 무작위 — **4개 레인 전체를 완전히 포함**, 창고(warehouse1~4)는 구조적으로 밖, 조합소(johab1~4)는 안에 있지만 매우 작아 확률상 희박 | §3·§9-3 |
| 레인 대비 탐색 반경 비율(우리 맵 이식용) | 레인 폭 대비 기본23.0%/전설26.5%/영원28.9%, 레인 높이 대비 27.7%/31.8%/34.7% | §9-3 |
| 상자 개수(1회 재생성분) | 기본 7개 → 전설나미 조합 후 9개(전역 공용값, 플레이어 무관) | §3·§6-1 |
| 탐색 반경(`treasure_range_int`) | 기본 750 → 전설나미 조합 시 863(+15%) → 영원나미 영입 시 940(절대값 대입, 순서의존적, 가산 아님) | §2·§6 |
| 판정 조건 | 거리(반경 내)만, 시야·이동능력 조건 없음(상자는 Locust+빈 모델로 항상 안 보임) | §4 |
| 1회 시전 결과 | 반경 내 상자 **전부** 동시 발견 가능(1개 제한 없음) | §4 |
| 보상표 | 1/3씩: 랜덤위습(`e0IX`) / 흔함선택위습(`e018`) / 안흔함위습(`e017`), 개수=`TreasureChest[pid]`(기본1, 전설나미 후 2) | §4 |
| 보상 대상 | 발견한 플레이어 개인에게만 지급 | §4 |
| 나미(전설) 2배 보상 | **실제로 있다** — `TreasureChest` 1→2 (곱연산이 아니라 지급개수 자체가 2배) | §6-1 |
| I002("찾은 보물 개수") | 발견 때마다 **전원**(4인 공용 진행도) +1, `iabi=A109`(무효과 스텁·순수 카운터) | §5 |
| 세이브 +2 | **매 발견 아님** — `treasurequest`가 정확히 9에 도달하는 순간 1회, 전원에게 목재+2/`Save_playpoint`+2/`present_Save_playpoint`+2 | §5 |
| 모건 "100초마다 추가 탐색" | **근거 없음**(반증: 모건 전용 트리거 0건, AHta 쿨타임 오독으로 추정) | §7 |
| 시노부(전설적인) 위치감지(`A0YP`) | 별개 시스템 — 보상 없이 미니맵 핑만, 300초 쿨타임 | §8-2 |

## 3줄 결론 (PM 검수 반영, 회신용)

1. 정정 완료 — 해금 조건은 "전원의 항해일지(H0C4)가 처음부터 AHta 보유"가 맞았다.
   H0C0/H0C1/H0C2/H0BZ/H0BQ는 건물 변신 대상이 아니라 "항법" 5택1 스킬(A15W/L/X/K/M,
   보물찾기와 무관)의 이름을 딴 미사용 더미이고, I00K의 레벨2 승급은 `upoi==22`인
   H0C4 계열에만 걸린다(모건·시노부가 직접 먹으면 무효).
2. `treasure_chest_zone`은 4개 레인(`p1~p4_life_zone`)을 전부 완전히 포함하는 하나의
   큰 rect다 — 창고(warehouse1~4)는 구조적으로 밖, 조합소(johab1~4)는 안에 있지만
   면적이 zone의 0.06% 미만이라 확률상 거의 안 걸린다.
3. 탐색 반경을 레인 크기 대비 비율로 냈다: 레인 폭 대비 기본23.0%/전설26.5%/영원28.9%,
   레인 높이 대비 27.7%/31.8%/34.7%.
