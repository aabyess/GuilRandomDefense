# 근사 장부 — "원작대로 다 넣었다"고 말하려면 이 표가 비어야 한다

2026-09-06 첫 작성(구현담당2, PM 지시). **읽기 전용 조사 결과다** — `Assets/Data/UnitSkills/`는
안 고쳤다(구현담당1이 그 디렉터리를 만지는 중이라 이 문서만 새로 썼다).

## 0. 어떻게 모았나 — 한계부터 밝힌다

`Assets/Data/UnitSkills/*.asset`의 `description` 필드에서 세 가지 꼬리표 패턴을
정규식으로 찾았다: `[게이트 미확인]`, `[미확인]`, "축밖/축 밖/축이 없다/축 없음/우리
축에 없다/우리에 없다". **이 세 패턴 중 하나라도 있는 파일만 잡힌다** — 꼬리표 없이
조용히 근사된 자리는 이 방법으로는 안 잡힌다(그 자체가 뿌리 ⑯·⑲의 위험이다: 꼬리표
없는 근사는 있는지도 모른다). 즉 아래 숫자는 **바닥이 아니라 "적어도 이만큼"**이다.

```
스킬 에셋 전체            365개 (2026-09-06 저녁 재집계 — 구현담당1의 분할 작업으로 344→365,
                          파일 수가 계속 바뀐다. 첫 집계 때의 119는 아래 §4 분류 이후 116으로,
                          그중 확인된 3건을 빼면 113으로 내려간다 — §4-4 참고)
꼬리표 하나 이상 걸린 것   116개 (31.8%, 재집계 시점 기준)
```

## 1. 카테고리별 스코어보드

| 카테고리 | 건수 | 왜 다른가 | 되돌릴 조건 |
|---|---:|---|---|
| 스톡 필드 값 그대로(필드 뜻 미확인) | 54 | 원작 트리거·더미 채널의 필드 값을 그대로 옮겼는데, **그 필드가 정말 피해를 뜻하는지 자체가 미확인**이다(슬로우·스턴·버프 등 다른 효과였을 수 있음) | 원작 트리거 함수 원문을 직접 대조해 필드 뜻을 확정하면 — **2026-09-06 리서치담당이 판정법을 확립함**(같은 `base`를 쓰는 형제 능력들의 제작자 이름·툴팁을 본다), `Docs/reference/STOCK_FIELD_RESEARCH_INPUT.csv`(80행, 능력ID당 1행)로 입력 준비 완료. `base`/`문제의필드` 두 열은 리서치 담당이 채울 자리라 비워뒀다(구현 쪽이 판정 안 함) |
| **대상 A11S(폭발형 피해 감수도) 축 고정** | 16 | 원작 "계수 = 상수 + 증분×레벨" 16건을 **최솟값(레벨1)으로 고정**했다. ⚠️ **2026-09-06 정정: 이 축은 「연구」도 「스킬 레벨」도 아니다.** 대부분은 **대상(적)이 가진 `A11S`「폭발형데미지 증폭」의 레벨**이고(잡몹 14 · 보스 16 · 신세계 광폭화 1), 일부만 진짜 연구(`R01V`/`R01O`, **최대 3레벨**)다. 세 축 판정은 `LEVEL_COEFFICIENT_AXES.md` | 적 유닛에 A11S 대응 축(피해 감수도)을 만들면 |
| 버프 조건 드롭(항상 발동으로 근사) | 10 | "버프BXXX==true/미보유" AND 조건을 빼고 다른 조건(확률 등)만 남겼다 — **실제보다 자주 발동한다** | 06번(버프 게이트) 배선 완성 — 아래 §2 표에 개별 진행상황 |
| 미분류 → 분류 완료(§4) | 34 | **2026-09-06 저녁 분류 완료** — (c)축없음 25(허용된 근사 2 포함)·(a)필드뜻미확인 3(54건 합류)·(b)게이트미확인 2·🔴제거확정 3·🟡재확인필요 2 | §4 참고. 제거 확정 3건은 이미 위 §0 총계(113)에 반영함 |
| 게이트 확률로 잠금(`[게이트 미확인]`) | 2 | 원작 발동 조건을 못 찾아 `triggerChance=0`으로 잠갔다 | 원작 트리거 원문에서 진짜 게이트를 찾으면(과소평가 145배 사고의 거울 — PM이 구현담당1에게 재확인 요청함, §5) |
| 게이지 조건 드롭(OnHitCount 미적용) | 1 | 버프가 아니라 순수 게이지 조건(MANA==85)인데도 축 밖으로 뺐다 — B03M/B078처럼 OnHitCount로 바로 담을 수 있었을 가능성 | 개별 확인 필요(§4) |
| GetUnitUserData 조건 드롭 | 1 | 원작이 유닛에 붙인 커스텀 정수값(UserData)을 조건으로 쓰는데 우리에 그런 축이 없음 | 그 UserData가 실제로 뜻하는 상태(예: 스택 카운터)를 밝히면 |
| 타입(damageType/attackType) 미확인 | 1 | 더미(AHtc 계열)의 공격/피해 타입이 어느 원작 표에도 없어 기본값(AD/Unassigned)으로 둠 | 원작 표에서 그 더미의 타입을 찾으면 |
| 즉사 등 우리 축 없는 효과 | 1 | "포인트값<200 즉사" 같은 원작 즉사 판정 축이 없어 100%현재체력 피해로 근사(레일리) | 즉사 이펙트 킨드가 새로 생기면 |

합은 121로 총 119보다 조금 많다 — 파일 하나가 두 카테고리에 동시에 걸리는 경우가
있어서다(예: 같은 파일에 "버프 드롭"과 "레벨·연구 비례 계수 고정"이 같이 있음).

## 2. 06번(버프 게이트) 진행상황 — "버프 조건 드롭" 10건의 지금 상태

| 파일(원작 유닛) | 버프ID | 상태 |
|---|---|---|
| `SkillData_게이트_랜덤_미도리야_이즈쿠_079f80a5.asset`(히그마) | B05N | opener 완성(`6638ebb`) — 소비 쪽 requiredBuffId 배선 대기(디렉터리 잠금) |
| `SkillData_게이트_영원_김영원_86ef2902.asset`(핸콕/영원) | B03M | opener 완성(`6638ebb`) — 배선 대기 |
| `SkillData_게이트_전설적인_이현주_0e5cf375.asset`(핸콕/전설) | B00D | opener 완성(`6638ebb`) — 배선 대기 |
| `SkillData_게이트_제한_김민규_136d09f9.asset`(카타쿠리) | B045 | opener 완성(`dadb0e9`, 바닥조건+비용 신규 축) — 배선 대기, **비용 차감 시점 원작 확인 대기**(`6cfb9d2`) |
| `SkillData_게이트_영원_이지원_3ba504d5.asset`(니카) | B078 | **해결 완료**(`c77ff17`) — 게이지 조건과 완전히 같은 순간이라 별도 opener 불필요로 확정, requiredBuffId 안 씀 |
| `SkillData_게이트_랜덤_이즈미_신이치_351a0f00.asset`(키쿄우) | B06B | 거는 쪽 완성(`2f36e6b`, `SideBossEncounter`가 R62·66·71 광폭화 소환체에 영구 태그) — 이 파일의 requiredTargetBuffId 배선 대기 |
| `SkillData_게이트_제한_이유범_351a0f00.asset`(시노부) | B06B | 위와 동일 — 배선 대기 |
| `SkillData_게이트_히든_호치킨_21297197.asset`(아카이누 히든) | B06B | 위와 동일 — 배선 대기 |
| `SkillData_회수_초월_김만경_AD_0ac0451e.asset`(신 해군원수) | B06B | 위와 동일 — 배선 대기 |
| `SkillData_회수_초월_양재모_AD_f54a123f.asset`(로우) | B03Z | **차단** — MANA==135(연다)와 LIFE>=250(쓴다)가 다른 게이지라 지속시간 근거가 없다. `RemoveBuff` 효과 kind도 아직 없음. 리서치담당에게 원작 `adur` 확인 요청함(PM, 답 대기) |

⚠️ 이 표 자체가 §1의 "버프 조건 드롭 10건"과 정확히 같은 대상이다 — 완료돼도 이
장부에서 지우지 말고 "완료"로 표시만 바꿀 것(과거에 뭐가 문제였는지가 기록으로
남아야 나중에 검증할 수 있다).

## 2-2. `ResearchLevel` — 🔴 되짚어보니 "잇기"가 아니라 "시스템 통째로 없음" (2026-09-06 최종 정정)

`SkillEffectBasis.ResearchLevel`(연구단계×배수+상수)을 쓰는 효과 **9건**(5개 파일)이
전부 상수항(`bonus`)만 나가고 있다 — `UnitAttacker.CountResearchLevel()`이 `return 0;`
고정이라서다. **이 하루 동안 이 값을 세 번 다르게 판정했다:**

1. **2026-09-06 오전**: "축은 있는데 안 이어져 있다"고 보고 `UnitUpgrades.LevelForGrade`
   (등급 트랙, 최대 21)에 연결했다(`8f4c347`) — **버그였다.** 이 basis가 뜻하는 건
   등급 업그레이드가 아니라 원작 **타입 업그레이드**(공격타입별, 최대 3레벨)라, 최대
   7배 과대가 실주행 중이었다(제한됨·히든 5효과, 뿌리 ㉜ 네 번째).
2. **같은 날 오후**: 구현담당3이 그 연결을 끊고 `CountResearchLevel()`을 다시 `0`으로
   되돌렸다 — 회귀 없음(등급 트랙 사고 이전과 값이 같다).
3. **2026-09-06 저녁**: "그럼 타입 업그레이드 축을 실제로 만들면 되지 않나"를
   확인했는데 **그때는 만들 수 없다고 판단했다.** 9건이 같은 연구 하나를 읽는 게
   아니라 서로 다른 타입 업그레이드 ID를 참조하는 것처럼 보였고, 무엇보다
   **레벨당 값·구매 비용**을 원작에서 조사한 적이 없어 지금 지으면 그 비용을
   **우리가 지어내야 하는** 상황이었다(사장님 기준 "전부 원작대로, 우리 창작은
   조합식뿐"상 구매 비용은 창작 허용 범위 밖). `CountResearchLevel()`을
   `return 0;`으로 둔 채 리서치담당에게 원작 조사를 넘겼다.
4. **2026-09-06 밤(최종, 연결 완료)**: 리서치담당이 `war3map.w3q`를 전수해
   `ATTACKTYPE_UPGRADE_RAW_DUMP.md`를 냈다 — 비용(마스터 버튼 4개, 각 골드3000+
   목재500, 최대 3레벨)과 구조(원작 "강화소 3", 자식 12개는 트리거로 자동 공짜
   부여)가 확정돼 **지어낼 게 없어졌다.** "9건이 서로 다른 ID를 읽는다"고 봤던 건
   **원작(재배정 전) 캐릭터가 참조하던 ID였다** — 우리는 원작 이름 1:1 매핑을
   안 쓰므로, "지금 이 유닛에 배정된 공격타입"을 기준으로 읽는 게 등급트랙
   (`LevelForGrade`, "원작 그 캐릭터의 등급"이 아니라 "지금 이 유닛의 등급"을
   쓴다)과 같은 원칙이다. `AttackTypeUpgradeTrackData.cs`(신규)·
   `UnitUpgrades.LevelForAttackType`(신규, `LevelForGrade`와 같은 패턴)·
   `UnitAttacker.CountResearchLevel()`(연결)로 구현했다(직렬화 필드는 추가만).
   `Assets/Data/AttackTypeUpgrades/`에 4개 트랙 자산(일반·공성·관통·패기) 생성.
   ⚠️ **처음엔 구매 UI 없이 커밋했다** — 씬 어디에도 이 트랙을 참조하는 컴포넌트가
   없어 레벨이 항상 0이었다(PM 지적, 뿌리 ㉜ — "축이 섰다"와 "연결됐다"는 다른
   질문). **바로 이어서 `AttackTypeUpgradeShop.cs`(신규)로 구매 경로를 만들었다**
   — `UnitUpgradeShop`(강화소1)과 같은 패턴이되 별개 컴포넌트다(원작이 다른
   건물이라 하나로 안 합침). 마스터 4개만 구매 가능, 골드+목재 동시 차감
   (확인 후 차감 순서로 절반만 깎이는 사고 방지), maxLevel(3) 상한.

**뼈대 집계 정정**: 이 항목은 "잇기만 하면 되는 자리"가 아니라 **"원작 시스템
(타입 업그레이드, `R00G`~`R01W`, 공격타입×등급 매트릭스) 하나가 통째로 없다가
오늘 밤 데이터·연결·구매 경로까지 다 생겼다"**이다 — 상태 페이지가 이걸 "거의
다 된 것"으로 셌다면 그 셈은 틀렸었다. **남은 건 씬 배치뿐**(PM이 맵 생성기에
붙이기로 함 — `Assets/Data/AttackTypeUpgrades/`의 4개 트랙 자산을
`AttackTypeUpgradeShop.tracks`에 연결).

### 검산 — 레벨0(지금, 회귀 없음) vs 레벨3(원작 최대, 미리보기)

⚠️ **Python 재구현으로만 확인했다 — 런타임 미검증**(§9, 이 프로젝트에 Unity
배치테스트 인프라가 없다는 공통 구멍 그대로 적용된다). `level×multiplier+bonus`
공식을 그대로 계산했다:

| 효과 | multiplier | L0(=지금) | L3(원작 최대, 미리보기) |
|---|---:|---:|---:|
| 최상호 효과1·2 | 45,000 | 2,200,000 | 2,335,000 |
| 전법규 | 12,500 | 182,500 | 220,000 |
| 황정기(2251ce1a) | 52,500 | 525,000 | 682,500 |
| 황정기(2a646778) 효과1·2 | 40,000 | 500,000 | 620,000 |
| 황정기(2a646778) 효과3 | 40,000 | 850,000 | 970,000 |
| h05C(조세민/핸콕) 효과1·2 | 30,000 | 360,000 | 450,000 |

**L0 열이 위 §"과소 크기" 표의 "지금" 값과 정확히 같다** — 씬에 아직 구매 경로가
없어 `attackTypeLevels`가 항상 비어 있고, `LevelForAttackType`은 항상 0을
돌려준다(구매 경로가 생긴 뒤엔 아래 실제 구매 시뮬레이션 참고).

### 검산 ② — 실제로 사면 오르는가 (진짜 검산, PM 지시)

⚠️ 마찬가지로 Python 재구현 — **런타임 미검증**(§9). "패기" 트랙만 3번 산다고
가정(골드·목재 충분, `AttackTypeUpgradeShop.TryUse`와 같은 로직 재현):

```
구매 시도1: 성공 — 레벨1 (골드/목재 3000/500 차감)
구매 시도2: 성공 — 레벨2
구매 시도3: 성공 — 레벨3
구매 시도4: 실패 — 이미 최대 레벨(3)  ← 상한 확인
```

| 효과 | 트랙 | L0(안삼) | L1 | L2 | L3(최대) |
|---|---|---:|---:|---:|---:|
| 최상호 효과1·2 | 패기 | 2,200,000 | 2,245,000 | 2,290,000 | 2,335,000 |
| 전법규 | 일반(안 삼) | 182,500 | 182,500 | 182,500 | 182,500 |
| 황정기(2251ce1a) | 패기 | 525,000 | 577,500 | 630,000 | 682,500 |
| 황정기(2a646778) 효과1·2 | 패기 | 500,000 | 540,000 | 580,000 | 620,000 |
| 황정기(2a646778) 효과3 | 패기 | 850,000 | 890,000 | 930,000 | 970,000 |
| h05C 효과1·2 | 패기 | 360,000 | 390,000 | 420,000 | 450,000 |

**패기 소속 8건은 레벨을 따라 정상 상승, 일반 소속 1건(전법규)은 패기를 사도
182,500 그대로다** — 트랙 독립성(공격타입마다 다른 유닛이 영향 안 받는다) 확인.
안 사면(레벨0) 지금과 정확히 같고, 사면 오르고, 3에서 멈춘다 — 세 검산 전부
통과했다(런타임 실측은 아직 없음, §9 그대로).
돌려주므로 **회귀 없음**을 숫자로 확인했다. L3는 원작 최대치가 원작대로 구현됐을
때의 미리보기이지, 지금 도달 가능한 값이 아니다(구매 UI가 아직 없다).

### 효과별 값과 — **과소 크기 (레벨0 vs 원작 최대인 레벨3)**

식은 `level × multiplier + bonus`. 지금은 `level`이 항상 0이라 `bonus`만 나간다.
원작 최대(레벨3)와 비교하면:

| 파일 | 효과 | multiplier | bonus(=지금, L0) | **L3(원작 최대)** | 과소 크기 | 과소 비율 |
|---|---|---:|---:|---:|---:|---:|
| `..._최상호_a8343962.asset` ×2 | 카벤디시(`R01M`) | 45,000 | 2,200,000 | 2,335,000 | −135,000 | −5.8% |
| `..._전법규_9ffa9c45.asset` | 에넬(`R01L`) | 12,500 | 182,500 | 220,000 | −37,500 | −17.0% |
| `..._황정기_2251ce1a.asset` | (`R01V`/`R01O` 중 하나, 미상) | 52,500 | 525,000 | 682,500 | −157,500 | −23.1% |
| `..._황정기_2a646778.asset` 효과1·2 | 〃 | 40,000 | 500,000 | 620,000 | −120,000(각각) | −19.4% |
| `..._황정기_2a646778.asset` 효과3 | 〃 | 40,000 | 850,000 | 970,000 | −120,000 | −12.4% |
| `SkillData_원작028_h05C.asset` ×2 | 핸콕(ID 미상, 리서치 요청함) | 30,000 | 360,000 | 450,000 | −90,000(각각) | −20.0% |

**9건 합계**: L0 합 7,677,500 → L3 합 8,682,500, **과소 1,005,000(전체 대비 −11.6%)**.
개별로는 −5.8%~−23.1% 범위다 — h05C·황정기 쪽이 비율상 더 크게 깎인다.

⚠️ **위 L3은 "원작 타입 업그레이드가 최대(레벨3)까지 있다면"의 가정치다** — 실제로
플레이어가 이 레벨을 살 수 있는 경로(구매 비용)가 우리 게임에 없으므로, **지금은
이 과소가 사실상 영구적이다**(다른 축처럼 "플레이어가 안 사서 낮다"가 아니라 "살
방법 자체가 없어서 낮다").

**시뮬레이터는 안 바꿨다** — `Tools/simulate_balance.py`는 원래부터 이 basis를
"연구 0단계(=bonus만)"로 고정 가정해왔다(플레이어 진행 상태를 모델링 안 함). 오늘
런타임에서 있었던 오전의 버그(그레이드 트랙 오배선, 최대 7배)가 시뮬 출력엔 애초에
반영된 적이 없어 시뮬 쪽 회귀는 없다 — 상세는 `SIMULATOR_RUNTIME_DIVERGENCE_2026-09-06.md`
A-2 참고.

## 3. 오늘 세션에서 직접 확인한 개별 항목 (표 밖에서 발견된 것들)

| 대상 | 원작 값 | 우리 값 | 왜 다른가 | 되돌릴 조건 |
|---|---|---|---|---|
| 짧은 자기버프 창 8종(시간→타수 변환) | 초 단위 창(0.20~1.40초) | 타수(`buffHitCharges`) | 원작 실효 공격간격이 연구로 크게 빨라져 "N초 창"의 진짜 뜻이 "다음 M대"였다 — 우리 유닛은 그 공격속도 스케일이 없어 시간을 그대로 옮기면 뜻이 사라진다 | 이미 단위 변환이 정답으로 확정됨(리서치담당 원작 레지스터 확인) — 남은 건 배정뿐: `B00M`(제트, 완료)·`B00D`(2대,완료)·`B03M`(2대,완료)·`B05N`(3대,완료)·`B045`(4대,완료) / `B06G`(비비,1대)·`B00Y`(브룩,5대)·`B03P`(킹,4대)는 **해당 유닛이 아직 로스터에 없어 대상 자체가 없음**(02번 판단 자료용 — §4-2) |
| 레일리(정윤식) ANCIENT 대상 분기 | "B070 미보유 AND 대상=ANCIENT면 100%" | 그 분기 자체를 버림(일반 대상 1/50 근사만 반영) | ANCIENT(고대) 유닛 타입 판정 축이 없음 | 유닛 타입(ANCIENT/SAPPER 등) 개념이 생기면 |
| 사이드보스 광폭화 소환체(§⑦) — 신앙의 오라(방어+5)·재생 오라(체력재생+95000/+250000)·`A11S` 피해감수성×0.25·`ANCIENT`/`SAPPER` 타입·크기 282% | `ORIGINAL_BOSS_COMBAT_SPEC.md` §⑦ 원문 | 소환+`B06B` 영구 태그만 구현(`2f36e6b`) | 오라 2종은 새 지속효과 시스템(EnemyAuraCaster급)이 더 필요하고, 감수성·크기·유닛타입은 대응 필드가 아예 없음 | 각 축이 개별로 생기면(오라는 이미 EnemyAuraCaster가 있어 가장 쉬움) |
| `A0F1`(김정래, "죄수크리") 크리티컬 배수 효과 | 평타 20% 확률로 다음 타 크리 배수 1.5× | `CasterAttackPower×1.5` 보너스 피해로 근사(대미지 킨드) | "크리티컬 배수를 일시적으로 올리는" 효과 킨드가 없어 같은 기댓값의 보너스 피해로 대신함 | 크리티컬 배수 버프 킨드가 생기면 |
| 영웅 능력치(STR/AGI/INT) 비례 basis 4건 | `GetHeroStatBJ` 비례 | `UnitData.baseX/xPerLevel`이 전부 0이라 상시 0 | 01번(영웅 스탯)이 실제 값을 넣기 전까지 구조만 있고 실질값 없음(회귀 아님, 의도적) | 01번이 실값을 배정하면(진행 중, 구현담당3) |
| 세이브 누적 확률 변환(예시: `GetRandomReal(1,100)<=7.25` 꼴) | `GetRandomReal`은 하한 1이라 단순 %가 아님 | 7.25% → 6.313%로 재계산해 반영(`TRAIT_PROC_CHANCE.md` 참고) | **이미 해결된 사례** — 표기만 남긴다(같은 함정에 또 안 걸리게: `GetRandomReal(1,100)<=N`은 `(N-1)/99`로 바꿔야 정확하다) | — (참고용, 재작업 불필요) |

## 4. 미분류 34건 — 개별 분류 완료(판정 아님, PM 지시 2026-09-06)

첫 집계 때 35건이었는데 재집계 시점엔 34건이다(구현담당1의 진행 중 작업으로 파일
하나가 다른 카테고리로 흡수되거나 없어짐 — `SkillData_더미채널_히든_호치킨_79행_10000.asset`가
이번 재집계엔 없다). 하나씩 열어 "무엇이 미확인인가"만 갈랐다 — **값은 하나도
안 고쳤다.**

### 4-1. 분류 결과 스코어보드

| 분류 | 건수 | 비고 |
|---|---:|---|
| (c) 우리에 축이 없음(허용된 근사 2건 포함) | 25 | 대부분(§1의 "축 없음" 계열과 같은 성격, 되돌릴 조건도 같다) — 그중 카이도 2건은 PM이 이미 "무해함" 확인 완료 |
| (a) 필드 뜻 미확인 → 54건 쪽으로 합류 | 3 | 리서치담당 대상, `STOCK_FIELD_RESEARCH_INPUT.csv`에 아직 없음(형식이 달라 별도 추적 필요) — 원작019는 (b)와 중복 1건 |
| (b) 게이트 미확인 → 리서치 대상 | 2 | 원작019(H08Z)에 (a)와 중복 1건 포함 |
| 🔴 실은 확인됐는데 꼬리표 안 지워짐(제거 확정) | 3 | **장부에서 뺀다** |
| 🟡 부분 해소 — 엔진 축은 생겼는데 이 스킬엔 아직 미확인 | 2 | 완전히 못 뺀다, 재확인 필요로 다운그레이드(위 (c) 25건에는 안 넣음, 별도 표시) |

합 25(c)+3(a, 원작019 중복분 -1 보정)+2(b)+3(제거)+2(재확인) = 34(원작019가 a·b
양쪽에 걸려 1건 중복 계산됨을 감안하면 정확히 34) — 상세는 아래 표.

### 4-2. 상세 표

| 파일 | 분류 | 근거 |
|---|---|---|
| `게이트_제한_최영민_30f704bf.asset` | (c) 축없음 | 레벨 비례항(A11S와 같은 계열, 다른 문구) |
| `게이트_제한_최영민_33bd8aa4.asset` | (c) 축없음 | 위와 동일 |
| `더미채널_랜덤_이즈미_신이치_1.asset` | (c) 축없음 | "능력레벨 조건" 드롭 |
| `더미채널_랜덤_이타도리_유지_1.asset` | (c) 축없음 | 위와 동일 |
| `더미채널_랜덤_카마도_탄지로_1.asset` | (c) 축없음 | 위와 동일 |
| `더미채널_변화됨_박은석_79행_01000.asset` | (c) 축없음 | "양의 상태조건(스테이지·버프 보유 등)" 드롭 — `79행` 배치 공통 문구 |
| `더미채널_불멸_고도현_79행_01250.asset` | (c) 축없음 | 위와 동일, 실제 조건 있었음(`GetRandomInt(1,8)==5`) |
| `더미채널_불멸_고도현_79행_10000.asset` | 🔴 **제거 후보** | 진입게이트 둘 다 "(조건없음)"인데 공통 경고 문구가 그대로 붙어 있다 — 드롭된 조건 자체가 없다 |
| `더미채널_불멸_박은석_79행_00625.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 — 기본값대로 유지 |
| `더미채널_불멸_신지우_1.asset` | (b) 게이트 미확인 | "stage0→4 분기 조건 미확인 — 매 타로 근사" |
| `더미채널_불멸_신지우_h04F.asset` | (c) 축없음(필드 뜻은 이미 해결) | `Nbr1`=공격력 증가(배틀로어)로 **오늘 확정됨** — 임시 공격력 버프 축이 없어 효과 자체를 제거해뒀다(피해로 잘못 넣는 것보단 안전). **필드 뜻 미확인은 닫혔고, 남은 건 "임시 공격력 버프" 엔진 축 부재뿐** |
| `더미채널_불멸_정윤식_79행_10000.asset` | 🔴 **제거 후보** | 위 고도현_10000과 같은 이유 — 진입게이트 "(조건없음)" |
| `더미채널_영원_김정래_1.asset` | (c) 축없음 | "체력>N 조건(거의 항상 참)" 드롭 — 영향 작을 가능성 있으나 축 자체는 없음 |
| `더미채널_영원_김정래_A0F1.asset` | (a) 필드 뜻 미확인 → 54건 합류 | "공격타입 원본 [미확인]" |
| `더미채널_영원_최상호_79행_10000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `더미채널_제한_최영민_79행_00833.asset` | (c) 축없음 | 실제 조건 있었음(`GetRandomInt(1,12)==6`) |
| `더미채널_제한_최영민_79행_02000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `더미채널_초월_강주혁_AP_79행_10000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `더미채널_초월_구주호_AD_79행_01429.asset` | (c) 축없음 | 실제 조건 있었음(`GetRandomInt(1,7)==2`) |
| `더미채널_초월_신문철_AP_79행_10000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `더미채널_초월_임채민_AP_79행_01000.asset` | (c) 축없음 | 실제 조건 있었음(복합 `GetRandomInt`) |
| `더미채널_초월_최상호_AP_79행_10000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `더미채널_초월_황준석_ADAP_79행_01000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `더미채널_초월_황준석_ADAP_79행_10000.asset` | (c) 축없음 | 능력 상세 없이 공통 문구만 |
| `원작007_H08V.asset`(나미) | (a) 필드 뜻 미확인 → 54건 합류 | "realD 변동항 [미확인]으로 뺐다"(어느 레지스터인지 미확정) |
| `원작018_H09I.asset`(브룩) | (c) 축없음 | "소환된 더미가 지속적으로 오라를 낸다"를 표현할 방법이 없어 effects 비워둠(사이드보스 오라 문제와 같은 계열) |
| `원작019_H08Z.asset`(샹크스) | (a)+(b) 이중 | (a) attackType [미확인] · (b) "14.25초마다" 툴팁과 마나재생 계산(25.9초)이 안 맞음 — 다른 마나 공급원 의심, 미확정 |
| `원작022_h04G.asset`(제트) | 🔴 **제거 후보(고신뢰)** | "버프 보유 축이 우리에 없어 못 채운다"(2026-09-05 기록) — **오늘 B00M/`requiredBuffId`로 그 축을 직접 만들었다**(`902dbeb`·`9a2fb10`, 이 세션 본인 작업) — 문장이 안 지워진 것뿐, 실제로는 닫혔다 |
| `원작023_h04C.asset`(거프) | 🟡 **재확인 필요(다운그레이드)** | "버프개수 축이 우리에 없어"(2026-09-05 기록) — `casterBuffCountFactor`가 그 뒤 생겨(버프 레지스트리 기반으로 실제로 센다) 축 자체는 있을 수 있으나, **이 파일이 그 필드를 실제로 쓰는지는 확인 안 함** |
| `원작024_h04D.asset`(드래곤) | 🟡 **재확인 필요(다운그레이드)** | "버프 보유 여부 축이 우리에 없어"(2026-09-05 기록) — 버프 레지스트리 자체는 이제 있음(`requiredBuffId`), 이 스킬(A0FS)에 실제로 걸려 있는지는 미확인 |
| `원작025_h04B.asset`(시키) | (c) 축없음 | "소환 축이 없다"(더미 2기 소환) — 여전히 미해결 |
| `원작028_h05C.asset`(핸콕) | (c) 축없음 | "간격(공격 속도 변화) 축이 없다" — hitCount/duration으로 근사, 여전히 미해결 |
| `카이도_불멸_신지우_buster.asset` | (c) 축없음(허용된 근사) | `Kaido_Skill_int`(전용 카운터) 축이 없지만 **장기적으로 무해함을 PM이 이미 확인**(단조증가라 10타 이후 상시발동, 오차 무시 가능) — 열려는 있으나 조치 불필요 |
| `카이도_불멸_신지우_skill18.asset` | (c) 축없음(허용된 근사) | 위와 같은 이유, PM 확인 완료 |

### 4-3. "원작에도 값이 없음"(effects 비어있음) — 0건, 전부 오탐이었다

`원작022/023/024/025/028`(제트·거프·드래곤·시키·핸콕) 5개 파일 모두 "수치
미상 — effects가 비어 있다. 사장님 콘텐츠 배정 시 채울 것"이라는 **2026-09-05
초기 작성 시점 문구**를 그대로 갖고 있었는데, **실제 파일의 `effects:` 목록을
직접 세어보니 전부 값이 차 있었다**(1~6개씩). 그 뒤 배정 작업이 진행되면서
description에 새 단락만 계속 추가되고 맨 위 초기 문구는 안 지워진 것이다 — 이
문구 자체는 이미 낡았다(단, 위 표에서 보듯 022는 완전히 닫혔고 023/024는
축 자체 재확인이 남아 025/028은 다른 축 문제가 여전히 열려 있다).

### 4-4. 이번 분류로 바뀐 숫자

```
재집계 시점 총계        116건 (§0 기준, 365개 스킬 에셋 중)
🔴 제거 확정 3건 반영    116 - 3 = 113건
🟡 재확인 필요 2건       113건에 포함된 채로 유지(제거 아님 — 다운그레이드 표시만)
```

**113이 지금 시점의 갱신된 숫자다.** 54건(리서치 진행 중)·16건(레벨·연구 비례
계수, `LEVEL_COEFFICIENT_AXES.md`로 축 판정 진행 중)·10건(버프, §2 진행 중)은
그대로고, 이번에 미분류 34건 중 3건만 실제로 닫혔다 — **가장 큰 두 덩어리(54·16)는
아직 그대로**라 113도 여전히 크다. (별도로 `ResearchLevel` 스텁 9건은 §2-2에서
닫혔다 — 이건 §1 스코어보드에 애초에 안 잡혀 있던 항목이라 113에 포함되지 않는다.)

⚠️ `79행_XXXXX` 이름의 파일이 다수 보인다 — 구현담당1이 지금 진행 중인 79행
재이식·분할 작업의 산출물이다. 이 장부를 다시 돌릴 때(§0 스크립트) 파일명이
이미 바뀌어 있을 수 있다 — 그때그때 재생성할 것.

### 4-2. `B06G`·`B00Y`·`B03P` — 로스터에 아직 없는 원작 유닛(02번 참고 자료)

06번①②(짧은 자기버프 창) 조사에서 나온 부산물 — 아래 세 원작 유닛의 버프는
방향(창 길이→타수)까지 정해졌는데, **로스터·스킬 에셋 어디에도 해당 유닛이 아직
없다**. 이건 "우리가 원작보다 과하다"는 것과 반대 방향 증거라 02번(콘텐츠 범위)
판단 자료로 PM에게 전달함:

```
B06G  비비   — 0.20초 → 1대(원작에서도 연구 초반엔 0.3회라 못 덮는다, 손대지 말 것)
B00Y  브룩   — 0.85초 → 5대
B03P  킹     — 1.40초 → 4대
```

## 5. 별도 트랙 — "0이 과소평가일 수 있는" 3건 (PM→구현담당1 이관, 참고만)

`check_required_fields.py` #18이 잡는 `triggerChance=0` 자산 중 아래 3건은
"게이트를 몰라 잠갔다"인지 "옮기다 실수로 0이 됐다"(145배 사고의 거울 — 과소평가라
아무도 눈치 못 챔)인지 구분이 안 돼 PM이 구현담당1에게 넘겼다:

```
SkillData_원작능력_영원_김정래.asset
SkillData_원작능력_희귀함_윤현모.asset
SkillData_원작능력_희귀함_이상혁.asset
```

## 7. 완전 소실 — "원작에 있는데 우리 어디에도 없는 값" (근사·미확인과 다름, 2026-09-06 신설)

⚠️ 이 절은 위 §1~5의 "근사했다"·"미확인이라 안 넣었다"와 성격이 다르다 — **여기 있는
값은 한때 우리 데이터 어딘가에 있다가, 크로스채널 중복 정리 과정에서 중복 쪽(제거
대상)에선 지워졌는데 진짜 주인 쪽엔 못 옮겨져서 지금 게임 어디에도 없다.** 뿌리
㉖(과소는 아무도 눈치 못 챈다)의 정확한 사례다 — "미확인" 꼬리표조차 없어서 위
§0의 패턴 스캔으로도 안 잡힌다.

| 능력ID | 원작 값 | 진짜 주인(있어야 할 곳) | 왜 못 옮겼나 | 근거 |
|---|---:|---|---|---|
| `A077` | 2,000,000 | `SkillData_원작능력_전설적인_정윤식.asset` | 정윤식 파일 자체가 `[구조적 미해결]`(A07B·A077·A001 중 어느 효과가 어느 능력인지 값만으론 안 갈림) — 정준영 쪽 중복은 지웠지만 정윤식 쪽엔 추측으로 안 채웠다 | 2026-09-06, `CROSS_CHANNEL_DUPLICATE_SWEEP.md`/`65b7f96`, 커밋 `2647eaa`·`479f5da` |
| `A001` | 150,000 | `SkillData_원작능력_전설적인_정윤식.asset` | 위와 동일 | 위와 동일 |
| ~~`A097`~~ | ~~8,000~~ | ~~`SkillData_원작능력_희귀함_서민성.asset`~~ | **🟢 복구됨** — `war3map.w3a` `Ucs1` 필드가 파이프라인 단계에서 통째로 안 옮겨진 것이었다(소유 판정은 옳았음) | 2026-09-06, 리서치담당 `Ucs1` 디코드 확정(`0fe3562`), 구현담당1 복구 반영(`534f41e`) |
| ~~`A0BE`~~ | ~~6,000~~ | ~~`SkillData_원작능력_희귀함_배병규.asset`~~ | **🟢 복구됨** — 위와 동일 원인(`Ucs1` 미이관) | 2026-09-06, 리서치담당 `Ucs1` 디코드 확정(`0fe3562`), 구현담당1 복구 반영(`534f41e`) |

**남은 진짜 "완전 소실"은 2건뿐이다 — `A077`·`A001`(둘 다 전설적인_정윤식).**
`A097`·`A0BE`는 지우지 않고 취소선+"복구됨"으로 이력만 남긴다 — **한때 소실로
판정했다가 원문에서 되찾았다는 사실 자체가, 남은 2건도 같은 방법으로 되찾을
수 있다는 뜻이라 남겨둘 값어치가 있다.**

**📌 재발견 방법(다음에 "완전 소실"이 의심되면 먼저 해볼 것)**: 소유 판정을
다시 하기 전에 **`war3map.w3a`에서 그 능력의 필드를 통째로 덤프**해봐라 —
`A097`/`A0BE`처럼 "소유는 맞는데 특정 필드(`Ucs1` 등)만 파이프라인이 안
옮긴" 경우가 실제로 있었다. `A077`/`A001`은 아직 이 방법을 안 해봤다(정윤식
파일의 구조적 미해결 — 어느 효과가 어느 능력인지부터 갈라야 해서 이 방법
적용 전에 그 매핑을 먼저 풀어야 한다).

**나머지 9건(A08V·A07T·A0PV·A0A1·A0PC·A16W·A0A2×2·A0LJ)은 진짜 주인 쪽에 값이
살아있는 걸 확인했다** — 이 절에 해당 없음, 정상.

**되돌릴 조건(남은 2건)**: `A077`/`A001`은 정윤식 파일의 구조적 미해결(값 중복
3개 trio)이 풀려야 한다. `A097`/`A0BE`는 아래에서 이미 되돌렸다.

**`A097`/`A0BE` 재검증 완료(2026-09-06, 리서치담당, PM 요청)**: 소유 판정은 정정할
필요 없음 — **파이프라인 결손이 맞다.**
- 원작 raw JASS 전수: `A097`(base `ACca`)을 참조하는 더미 생성 지점은 `e038` 하나뿐이고
  그 유일한 생성원이 `Trig_Unique14`다(전체 8,156개 함수 중 `e038` 리터럴은 이 한 곳).
  `A0BE`(base `ACca`)도 `e092` 하나뿐이고, `e092` 리터럴이 `Trig_Unique3_Actions` 안에
  두 번 나오지만 **둘 다 같은 트리거 안**(하나는 직접 시전, 하나는 반경 배치용 for-loop
  잔상)이라 원작 발동 지점은 여전히 1개다. 즉 서민성(`Trig_Unique14` 루트 보유)·배병규
  (`Trig_Unique3` 루트 보유)가 진짜 주인이 맞고, 박도진·강재규 쪽 중복 제거는 옳았다.
- `war3map.w3a` 필드 직접 디코드(`Ucs1~4`): `A097`은 `Ucs1=8000.0`·`Ucs2=70000000.0`·
  `Ucs3=1050.0`·`Ucs4=550.0`, `A0BE`는 `Ucs1=6000.0`·`Ucs2=70000000.0`·`Ucs3=1050.0`·
  `Ucs4=375.0` — `ORIGINAL_SKILL_DAMAGE_TABLE.csv`가 지목한 `Ucs1`(8,000/6,000) 값 자체가
  실재함을 재확인했다. `Ucs3=1050`은 두 유닛(등급도 같고 파워도 다른데) 완전히 동일값이라
  유닛별 고유 데미지가 아니라 스톡 템플릿 잔재로 보이고, `Ucs2`(7e7)는 자릿수부터 게임
  스케일 밖이라 미사용 필드가 맞다 — `Ucs1`만 실사용 데미지 필드라는 기존 판정 그대로다.
- 현재 파일의 effects(1050.0 + bonus5000.0/mult2.0)를 대조하면: `1050.0`은 `Ucs3` 값과
  일치해 이 능력 자신의 필드에서 온 게 맞지만, 두 번째 항목(5000.0/2.0)은 `Ucs1~4` 어느
  것과도 안 맞는다 — 설명문의 "1채널 추가 배정"(h023·h029 자신의 별도 원작 능력)에서
  온 값으로, `A097`/`A0BE`와는 무관한 별개 효과다. 즉 **`Ucs1`(8,000/6,000)이 파이프라인
  단계에서 통째로 빠졌다** — `Ucs3`만 옮기고 `Ucs1`은 건너뛴 것으로 보인다.
- **결론**: 소유자는 정정 불필요. 서민성 파일에 `Ucs1=8000`(basis0, 대상 자신 평타
  타입 준용 — 필드 뜻·공격타입 [미확인] 컨벤션 그대로), 배병규 파일에 `Ucs1=6000`을
  기존 effects에 **추가**하면 된다(기존 1050·5000/2.0 항목은 그대로 유지 — 서로 다른
  근거의 별개 효과라 겹치지 않는다). **복구 완료(2026-09-06, 커밋 `534f41e`)** — 두 파일
  다 새 효과로 반영됨.

⚠️ **새 근사 하나 — `A0BE`는 원작에서 대상이 여럿, 우리는 `SingleTarget` 하나만**
(2026-09-06, PM 지적): `e092`가 `Trig_Unique3_Actions` 안에서 **직접시전 1 + 반경배치
for문 1**로 두 번 나온다 — 원작은 이 능력을 **반경 안 여러 대상**에게 건다는 뜻이다.
우리는 반경 값을 원문에서 못 뽑아 `target: 3`(SingleTarget)으로 안전하게 넣었다
(반경 미확인 상태로 `Enemies`를 쓰면 `range<=0` 무제한 범위 사고가 난다, `c49fab3`
참고 — `SingleTarget`이 옳은 선택이었다). **다만 대상 수만큼 원작 대비 과소다** —
반경 값이 나오면 `target`을 `Enemies`+정확한 `range`로 바꿔야 한다.
`A097`(`e038`)은 같은 조사에서 **생성 지점이 단 하나**로 확인돼(반경 for문 없음) 이
근사가 적용되지 않는다 — 이미 SingleTarget이 원작과 같다.

## 8. 아이템 풀 — "만들 게 없다"로 닫음 (2026-09-06, PM 지시로 조사)

`ItemData`/`ItemInventory`(`Assets/Scripts/Data/ItemData.cs`·`Assets/Scripts/Units/ItemInventory.cs`)가
뼈대 11개 중 마지막 하나였다. 조사 결과: **죽은 코드가 아니라 「쓰이는 자리는 있는데 채울 콘텐츠가
없는」 상태**다 — `CombineSystem.cs`가 `IngredientKind.SpecificItem` 재료를 실제로 처리하는 코드
경로를 갖고 있고(`itemInventory.Items`에서 소모), `itemInventory == null`이면 경고만 내고 안전하게
막는다. 다만 **`SpecificItem`을 쓰는 `CombineRecipe` 에셋이 현재 0개**이고, `ItemData` 인스턴스도
0개, 씬·프리팹 어디에도 `ItemInventory` 컴포넌트가 안 붙어 있다 — 코드는 살아있지만 콘텐츠가 없어
지금은 사실상 안 도는 경로다. (참고: `UnitInventory`는 이름이 비슷해 같이 지목됐지만 전혀 다른
시스템이다 — 플레이어가 필드에 낸 유닛 개체의 등록부로, `UnitSpawner`·`CombineSystem`·`GameHud`
등 핵심 경로 다수가 실제로 쓰는 살아있는 코드다. 아이템과 무관하다.)

`MISSING_SYSTEMS.md`가 꼽은 아이템 후보 3개를 각각 확인했다:

| 후보 | 정체 | 판단 |
|---|---|---|
| 고대의 배(유닛, `h05Y`) | `ANCIENT_SHIP_SPEC_2026-09-06.md`(05번, 사장님 확정) — "로스터가 아니라 특수 유닛"으로 이미 구현 대상이 확정됨(시전→40%→해적선 생성→자기소모). **아이템이 아니라 유닛이 맞다.** | ⓑ 아이템 풀과 무관 |
| 고대의 배(아이템, 추정 `I00S`) | `MISSING_SYSTEMS.md` 76행 — 스토리9(어인섬) 클리어 보상, 판매 시 특성포인트1+선택위습. `ANCIENT_SHIP_SPEC_2026-09-06.md`가 이미 지적: **이 아이템(`I00S`)이 유닛 `h05Y`와 같은 것인지 다른 것인지가 원작 조사에서 확정되지 않았다**("두 단서가 서로 다른 것을 가리킬 수 있다... 확정 전까지 한쪽으로 굳히지 말 것"). 판매 절차 자체도 미기재 | ⓒ 정체 불명 — 리서치 대기, 짓지 않음 |
| 레일리 히든 아이템 | `MISSING_SYSTEMS.md` 77행 — 판매 시 특성포인트1. "아이템 정체 자체가 불명" | ⓒ 정체 불명 — 짓지 않음 |
| 항법(route) 선택 시스템 | `MISSING_SYSTEMS.md` 78행 — "무엇을 고르는 시스템인지조차 불명" | ⓒ 정체 불명 — 짓지 않음 |

**결론**: 셋 다(고대의 배 아이템·레일리·항법) 원작 정체 자체가 안 밝혀져 있어 **지어내면 추측 배선이
된다.** 아이템 풀 시스템(`ItemData`/`ItemInventory`)은 그대로 두되(코드는 정상, `CombineSystem`이
이미 안전하게 no-op 처리함) 지금 채울 콘텐츠가 없다는 것으로 닫는다 — `ItemData.cs`에 그 취지의
주석을 남겼다(커밋 참고). **되돌릴 조건**: `I00S`/`h05Y` 관계와 판매 절차가 리서치담당 조사로
확정되면, 그 결과에 맞는 `ItemData` 에셋을 만들고 `CombineRecipe`에 `SpecificItem` 재료로 연결한다
(레일리·항법도 각자 정체가 밝혀지면 같은 경로).

## 9. Unity 배치테스트 — "인프라가 없다"가 아니라 "아무도 안 찾아봤다" (2026-09-06, 정정)

⚠️ **정정(2026-09-06 저녁)**: 이 절 첫 판이 "Unity 배치테스트 인프라가 없다"고 적었는데
**틀렸다.** 실제로 확인해보니 **인프라는 처음부터 있었다**:
```
Unity 6000.0.82f1 (설치 확인, ProjectVersion.txt와 일치)
com.unity.test-framework 1.6.0 (Packages/manifest.json에 이미 있음 — 추가 설치 불필요)
라이선스 활성 흔적 확인(~/Library/Application Support/Unity/config)
```
**오늘 대상측 3축(Aegr·AIsr·A11S)이 "런타임 미검증"으로 끝난 건 인프라 부재가 아니라
아무도 확인해보지 않았기 때문이다.** 지금 막고 있는 건 일시적인 것뿐이다 — **사장님
에디터가 이 프로젝트를 잡고 있는 동안**(`Library/EditorInstance.json`으로 확인)은
배치모드를 못 돌린다. 에디터가 닫히면 배치모드 EditMode 테스트는 될 가능성이 높다.

⚠️ **"없다"와 "안 찾아봤다"는 다르다 — 오늘 같은 실수가 세 번째다**(뿌리 ㉜·㉓ 네
번째와 같은 가족): `GachaTable.weight`(죽은 필드를 결측 근거로 삼음) · `AIsr` 29~36
구간(objectId 접두사 검색이 못 잡은 걸 "데이터 없음"으로 읽음) · 이번(인프라가 없다고
적었는데 처음부터 있었음). **결론 내리기 전에 "진짜 없는지 확인했는가"를 먼저 물을 것.**

**Unity 밖 순수 C# 테스트 경로도 막혀 있다는 점은 그대로다** — 이 머신에 `dotnet`/
`mono`가 둘 다 없다. 오늘 만든 스택 메서드들(`AddAegrStack` 등)은 Unity API를 안 써서
원리적으로는 유니티 밖에서도 테스트 가능하지만, 지금은 그 경로가 도구 부재로 막혀
있다 — 다음 사람의 선택지에서 이 경로를 빼야 한다.

**지금까지 쓴 방법**: `EnemyDummy.cs`의 정확한 산술식을 Python으로 그대로 재현해 숫자를
대조했다(스택0→기존값과 정확히 일치, 스택10→곱셈 결합 확인, float 32비트 반올림
버그 1건 실제로 발견). **이건 "그 식이 맞게 짜였는가"는 확인하지만 "런타임이 실제로
그 코드 경로를 그대로 부르는가"는 확인하지 못한다** — 예를 들어 `UnitAttacker.cs`의
호출부가 잘못된 파라미터를 넘기거나, 다른 경로가 이 프로퍼티를 안 거치고 별도로
계산하는 실수가 있어도 이 방법으로는 안 잡힌다.

**적용 범위**: 이번 스택 3축뿐 아니라, **런타임에서만 값이 정해지는 축은 전부 같은
구멍을 갖는다** — `armorShred`·`magicArmorShred`·버프 시스템(`UnitAttacker.AddBuff`)·
연구소 배율 등, 코드 리뷰·정적 대조·Python 재구현으로는 검증했지만 **실제 Unity
플레이 모드에서 값이 그대로 나오는지는 아직 아무것도 확인 안 됐다.**

**진행 중(2026-09-06 저녁)**: 스택 3축을 실제 피해 계산 경로로 태우는 최소 EditMode
테스트 하네스를 `Assets/` 밖 스크래치패드에 준비 중이다(에디터가 열려 있어 `Assets/`
아래에 파일을 두면 임포트/리컴파일이 걸려 위험) — 에디터가 닫힌 게 확인되면 옮겨서
실행한다. 검증 대상은 정확히 "런타임이 그 코드 경로를 실제로 부르는가"이지 산술
재확인이 아니다(그건 Python이 이미 했다).

**되돌릴 조건**: 위 하네스가 실행돼 통과하면 — 그 뒤로는 "숫자 대조(Python)"가 아니라
"런타임 실측"으로 격상된다.

## 10. RRD순번 다중효과 버그 수정 — 확정 2건 (2026-09-06, PM 지시)

02번(스킬 밀도) ①②확인 작업 중 발견 — "게이트별 배정...능력 N개를 이 SkillData
하나로 묶었다" 문구가 있는 118개 파일 중 62개가 levels[0]에 효과 2개 이상, 전부
`chance:1`(항상 같이 발동)이었다. ⚠️ **이건 밀도 집계 문제가 아니라 지금 살아있는
밸런스 버그다** — `RRD_SEQUENCE_DUPLICATE_AUDIT.md`(리서치담당, JASS 원문 기계추적)가
이미 확정한 두 사례가 우리 자산에 정확히 대응돼 있었다. **같은 "RRD순번 여러 개"
겉모습이었지만 원인이 서로 달라 고치는 방법도 달랐다** — 하나로 뭉뚱그리면 안 된다.

### 사례1 — 확률 배타분기 (`SkillData_게이트_초월_강주혁_AP_5c20b80f.asset`, Nami_Skill_2/H08V)

원작: `if GetRandomInt(1,100)<33` — #1(80만)이 33%, #2(40만)가 67%로 갈리고(절대 같이
안 나감), #3(40만)은 조건과 무관하게 항상 발동. 우리는 셋 다 `chance:1`로 매번 다
나가 **100% 160만 확정타**였다(원작 대비 최대 약 4배 과다).

**수정**: 값(80만/40만/40만)은 그대로 두고 `chance`만 고쳤다 — #1=0.33, #2=0.67,
#3=1(변경 없음).

```
수정 전 기대값   1,600,000  (매 트리거 100% 확정)
수정 후 기대값     932,000  (0.33×80만 + 0.67×40만 + 40만)
```

⚠️ **근사 한계**: `SkillEffect.chance`는 효과별 독립 판정이다(`UnitAttacker.
CastSkillLevel`의 `foreach` + 개별 `Random.value`, 효과 사이에 "배타 그룹" 개념이
코드에 없다). 그래서 원작처럼 "둘 중 정확히 하나만"을 구조적으로 강제하지 못하고
**기대값만 정확히 맞춘 근사**다 — #1·#2가 우연히 같이 뜨는 경우(22.11%, 이땐 오히려
수정 전과 같은 160만)나 둘 다 안 뜨는 경우(22.11%, 40만만 나감)가 원작에는 없는데
이 근사에서만 생긴다. 진짜 배타를 구현하려면 "효과 그룹" 같은 새 필드/코드가
필요한데, 이번 지시는 "chance만 고치라"였으므로 여기서 멈췄다 — 다음에 같은 패턴을
또 만나면 이 한계를 그대로 물려받는다.

### 사례2 — 타겟팅 분기 코드중복, 확률 아님 (`SkillData_게이트_초월_김민준_AP_2a646778.asset`, Tasigi_03/H05N)

⚠️ **PM이 "확정 2건"으로 같이 지시했지만, 실제로는 사례1과 메커니즘이 다르다** — 확률
분기가 아니라 원작 JASS가 타겟팅 방식(`Func003Func001C`, 지정대상 vs 그룹 무작위픽)에
따라 if/else로 완전히 같은 코드를 통째로 복사해놓은 것뿐이다(`RRD_SEQUENCE_
DUPLICATE_AUDIT.md` 사례1). 원작 실제 판정은 타겟팅 방식과 무관하게 **항상** "40만
확정타 1개 + 조건부(대상 포인트값<200, 우리 축 밖이라 이미 드롭됨) 최대체력×0.1
1개" = 2개뿐이다. 우리는 4개(40만×2 + 최대체력×0.1×2) 전부 `chance:1`로 **정확히
2배 과다**였다.

**수정**: `chance`를 건드릴 문제가 아니다(원작에도 랜덤이 없다, 항상 100%) — 중복
사본 2개(#3, #4)를 삭제해 40만×1 + 최대체력×0.1×1만 남겼다. 남은 두 효과의 값은
그대로다.

```
수정 전   확정타 800,000 + 대상최대체력×0.2   (매 트리거 100%)
수정 후   확정타 400,000 + 대상최대체력×0.1   (매 트리거 100%, 확률 변경 없음)
```

### 남은 미검증 — 60건

118개 중 나머지 60개(사례1·2 제외)는 **똑같이 "2효과 이상, 전부 chance:1"** 패턴이지만
어느 쪽(사례1형 확률배타/사례2형 코드중복/셋째 유형)인지 개별 확인이 안 됐다. 우리가
값 비교나 표본 추측으로 판정하면 오늘 이미 한 번 걸렸던 "값이 다르면 다른 효과다"
오류(뿌리 ㊵)를 반복하는 것이라, JASS 원문을 직접 보는 리서치담당의 기계추적을
기다린다(2026-09-06 저녁 기준 리서치담당에게 118개 트리거명 목록 전달 예정). 그때까지
나머지 60개는 **여전히 과다 발동 상태**다 — 밀도 집계(02번)에도, 밸런스에도 동시에
영향을 준다.

**검증 도구**: `Tools/check_required_fields.py`, `Tools/check_assignment_invariants.py`
둘 다 이번 수정 후 재실행, exit 0, 두 파일 모두 위반 목록에 없음(2026-09-06). 컴파일
불필요(순수 `.asset` 편집, C# 무변경).

## 11. 대상조건 배타 쌍의 반쪽 결측 — 3건 (2026-09-06, PM 지시)

`CHANCE1_FIX_INSTRUCTIONS.md`(§요약: 배타 25건 중 24건이 대상 포인트값/버프 기준
조건부, 118개 파일에 매핑) 작업 중 발견 — **배타 쌍(`<200`/`ELSE`) 중 한쪽만 118개
어느 파일에도 없는 경우가 3건이다.** 조건 배정 문제가 아니라 **값 자체가 없는
결측**이라 여기로 옮긴다. 방향(있는 쪽/없는 쪽)과 원작 값을 명시한다.

🔴 **정정(2026-09-06, PM·구현담당2가 원문 CSV 행 번호로 재대조, 리서치담당 원문
재확인 완료)** — 아래 최초 표는 **`Ryougi_Shiki_R2`·`kikoyou_hp` 둘 다 틀렸다.**
어느 값이 이미 있고 어느 값이 진짜 결측인지를 착각했다 — 원인은 내가 이 두 건을
"포인트값 배타 쌍" 프레임에 억지로 끼워 맞췄기 때문이다. 원문(`Trig_Ryougi_Shiki_R2_
Actions`/`Trig_kikoyou_hp_Actions`)을 직접 다시 열어 정정한다.

| 항목 | 최초(틀림) | 정정(원문 재확인) |
|---|---|---|
| `Ryougi_Shiki_R2` | "`#1`(0.45, `<200`)이 결측, `ELSE`(0.03)만 있다" | **`0.45`/`0.03`은 애초에 `<200`/`ELSE` 배타 쌍이 맞고 둘 다 이미 파일에 있다**(구현담당2가 직접 배선 완료). **진짜 결측은 CSV 124행 — `5,000,000` Flat, `Enemies`그룹 AoE(`ForGroupBJ`→`Trig_Ryougi_Shiki_R2_Func017A`류 아님, 원문은 별도 무조건 AoE 호출), 포인트값과 무관한 완전히 별개의 무조건 효과다.** |
| `kikoyou_hp` | "`#1`(현재체력×0.40)·`#2`(1,000,000)가 결측" | **이 둘(CSV 150~151행)은 이미 파일에 있다.** 진짜 결측은 CSV 148~149행 — `Trig_kikoyou_hp_Func034A`(`ForGroupBJ`로 `Enemies`그룹 순회) 안의 **무조건 RRD 2개, 각 1,500,000**(Hero/AD 하나, Normal/Universal 하나) — 포인트값 분기 **바깥**에 있는 그룹형 AoE라 배타 쌍과 무관하다. |

**공통 원인**: 두 건 다 진짜 결측이 **`Enemies`그룹 AoE 효과**였는데, 내가 "배타
쌍의 반쪽"이라는 가설에 꽂혀서 **원문을 다시 열어보지 않고 CSV 행 순서만으로
추정**했다 — 오늘 이미 여러 번 걸린 "값으로 대조 안 하고 모양으로 단정" 함정의
변형이다. **다른 결측 판정에도 같은 오류(AoE 성분을 배타 쌍으로 오분류)가 있을
가능성을 배제 못 한다 — 전수 재확인은 아직 안 했다.**

| 진짜 결측 | 원작 값(원문) | 조건 | 비고 |
|---|---|---|---|
| `Ryougi_Shiki_R2`(CSV124행) | `5,000,000` Flat | 무조건(스킬 발동 시 항상), `Enemies`그룹 AoE | 파일 `SkillData_게이트_랜덤_카마도_탄지로_3c5f8bd9.asset`에 없음 |
| `kikoyou_hp`(CSV148~149행) | `1,500,000`×2(Hero/AD, Normal/Universal) | 무조건, `Enemies`그룹 AoE(`Func034A`) | 파일 `SkillData_게이트_랜덤_이즈미_신이치_351a0f00.asset`에 없음 |

`vivi_Skill_4_Mana#1`/`#3`은 이 정정과 무관하다 — 포인트값이 아니라 **캐스터(비비)
자신의 강화 스택(`A0LZ`)** 결측이라는 판정은 그대로 유효하다(아래 그대로 유지).

**되돌릴 조건**: `Ryougi_Shiki_R2`·`kikoyou_hp` 둘 다 **새 게이트 파일이 필요하다**
(무조건·AoE 효과라 기존 파일에 조건부로 얹을 게 아니라 별도 effect로 추가). `vivi_
Skill_4_Mana#1`/`#3`은 `A0LZ` 스택 시스템(목재2+재료3기 소모, 성공확률
(90−레벨×9)%)을 구현하기 전까진 되돌릴 수 없다 — 별도 트랙(01번 인접 문제)으로
본다.

⚠️ **`A0LZ` 스택 축 구현 완료(2026-09-06, `fe6cec7`) — 레벨10 상한 정정**: 처음
구현(`fe6cec7`)엔 `selfUpgradeLevel>=maxLevel`이면 시전 자체를 막는 안전장치가
있었다(자원 낭비 방지). PM 정정: **원작은 레벨10에서도 시전이 되고 성공확률만
공식(`(90−레벨×9)%`)으로 0%가 될 뿐, 자원 낭비 자체를 막지 않는다** — 그게 원작
밸런스의 일부다. "전부 원작대로" 기준상 이런 "더 친절한" 가드는 창작 허용 범위
밖이라 뺐다 — 막았으면 위습이 원작보다 여유로워졌을 것이다. 자원 확인·차감
로직은 그대로 두고(레벨10에서도 목재2+위습3 소모), 성공확률 계산의 `Mathf.Clamp`가
자연히 0%를 만드는 것만으로 상한을 표현한다.

⚠️ **`Legend13_2`에 `>=300` 결측은 없다 — 원문에 그 갈래 자체가 없다(정정
2026-09-06)**: PM이 "`>=300`에 붙일 5번째 효과가 지시서에 없다"고 지적해 원문
(`Trig_Legend13_2_Actions`)을 다시 열었다. 실제 구조는 `if <200 then ... elseif
==200 then ... endif`로 **딱 2단뿐이고 `else`(`>=300` 포함 그 외 전부)가 없다** —
Gaban_Skill_mana(진짜 3단 `<200`/`==200`/`>=300`)와 헷갈리면 안 된다. 즉 원작
자체가 "점값 200 이상 300 미만이거나 300 이상이면 이 두 효과 중 아무것도 안
나가고, 무조건 효과(AoE 120,000 + 단일 750,000) 둘만 나간다"로 설계돼 있다 —
**5번째 효과가 빠진 게 아니라 처음부터 없다.** `CHANCE1_FIX_INSTRUCTIONS.md`의
기존 매핑(effect_index0/3=무조건, 1=`<200`, 2=`==200`)은 정정 불필요, 그대로 맞다.

## 12. 대상조건(포인트값/버프) 배선 완료 — 18개 파일, 39건 (2026-09-06, PM 지시)

`POINTVALUE_CENSUS.md`(원작 `upoi` 전수, 200=라운드보스/301=신세계 사이드보스/그
외=로스터 등급 전체 `<200`)와 `CHANCE1_FIX_INSTRUCTIONS.md`(21개 파일 매핑)를 받아
①`EnemyData.pointValue` 전수 채움 → ②21개 파일 조건 배선 순서로 진행했다.

### ① `EnemyData.pointValue` 전수 채움 (95개 전부)

우리 적 로스터는 등급 사다리가 없다(라운드 번호 기반 임시 더미, `Tools/
enemy_roster.py` 참고) — 그래서 원작처럼 등급별 세분값(70/80/90/101…)을 그대로
옮길 축이 없다. **기능적으로 의미 있는 건 `<200`/`==200`/`>=300` 경계뿐**이라(21개
파일 중 어느 것도 `<200` 구간 내부를 더 세분하지 않는다), 다음 규칙으로 채웠다:

```
Enemy_SideBoss{62,66,71}_*.asset (3개)     → 301  (신세계 사이드보스, SideBossManager.cs
                                                     주석이 이미 "upoi=301" 확정해둔 값과 일치)
Enemy_Story{01~13}_*.asset (13개, isBoss=1) → 300  (스토리/구역 마커 — armorShredBuildingOnly
                                                     로 이미 알려진 그 13개와 정확히 일치)
Enemy_R{10,20,30,40,50,60,65,70,75}_*.asset (9개, isBoss=1) → 200  (라운드보스)
그 외 70개(isBoss=0, Creep 포함)            → 100  (일반 몹 대표값 — 등급 세분 없음,
                                                     `<200` 경계만 보장하는 자리값)
```

⚠️ **일반 몹의 "100"은 등급을 재현한 값이 아니다** — 우리 적 로스터에 등급 축
자체가 없어서 원작의 세분(흔함70~랜덤전용180)을 옮길 수 없다. 21개 파일 중 어느
조건도 `<200` 구간을 더 쪼개 쓰지 않으므로 지금은 회귀 없이 안전하지만, **나중에
`<200` 내부를 가르는 조건이 나오면 이 "100" 하나로는 부족하다** — 그때는 적 로스터에
등급 축을 먼저 만들어야 한다.

⚠️ **Story01~13(300)에 대한 판단 근거**: `POINTVALUE_CENSUS.md` 자체는 "300=스토리
마커, 우리 쪽엔 대응 유닛 없음"이라고 적었는데 **틀렸다** — 우리에게 정확히 13개
대응 유닛이 있다(`armorShredBuildingOnly` 필드가 이미 이 13개를 "스토리 건물"로
써왔다). 리서치가 우리 자산 구조를 모르고 낸 결론이라 판단 근거를 직접 검증하고
300으로 채웠다 — `isBoss=1`이라고 200을 주면 `Gaban_Skill_mana`/`Legend13_2`의
`>=300` 분기가 이 장식용 마커에도 잘못 걸릴 수 있었다.

검증: `grep -L "pointValue:" Assets/Data/Enemies/*.asset` 0건(전수 채움 확인),
`check_required_fields.py` #20(targetCondition 쓰는데 pointValue 전부 0) 클린.

### ② 조건 배선 — 18개 파일 39건 targetCondition/버프ID

21개 대상 중 **18개**를 배선했다(3개는 아래 이유로 보류/제외). `SkillEffectTargetCondition`
(`LessThan`/`Equal`/`AtLeast`, `EnemyData.pointValue`와 비교)과 기존
`SkillEffect.requiredTargetBuffId`/`forbiddenTargetBuffId`(06번①-2 기존 필드,
새로 안 만듦)를 조합해 배선했다 — **삭제가 아니라 조건 부여**(PM 원칙 그대로),
값(80만/40만 등)은 전부 불변.

| 파일 | 조건 | 이전(항상 발동, 확정타) | 이후 일반(pointValue&lt;200) | 이후 보스(=200) | 이후 사이드보스(≥300) |
|---|---|---:|---:|---:|---:|
| 회수_불멸_이승우(Garp_AttackDamage) | `<200`/`ELSE` | 2.1000(계수) | 1.1 | 1.0 | 1.0 |
| 게이트_랜덤_이타도리_유지(Minato_E) | `<200`/`ELSE` | 0.16(계수) | 0.15 | 0.01 | 0.01 |
| 게이트_초월_김민준_AP(Tasigi_03) | `<200`(#2만) | 400,000.10 | 400,000.10 | 400,000 | 400,000 |
| 게이트_제한_박성호_874ea782(Marco_S1) | `<200`/`ELSE` | 150,000.20(계수+고정 혼재) | 0.2(계수) | 150,000 | 150,000 |
| 게이트_불멸_신지우(Gaban_Skill_mana) | `<200`/`==200`/`>=300` | 0.63(계수) | 0.5 | 0.1 | 0.03 |
| 게이트_초월_조성진_AD(Usop_Skill_2) | `<300`/`ELSE(>=300)` | 875,000 | 612,500 | 612,500 | 612,500 |
| 게이트_랜덤_이즈미_신이치(kikoyou_skill_1) | `<200`/`ELSE` | 700,000.31(계수+고정) | 700,000.01 | 350,000.31 | 350,000.31 |
| 게이트_제한_이충민_18aa2343(Ain_Skill_1) | B06B유/무 | 4,840,000 | 4,520,000(무버프 기준) | 〃 | 〃 |
| 게이트_전설적인_정준영(Legend13_2) | `<200`/`==200` | 2,370,000.12(계수+고정) | 2,370,000 | 1,620,000.12 | 1,620,000 |
| 게이트_랜덤_카마도_탄지로_3c5f8bd9(Ryougi_Shiki_R2) | `<200`/`ELSE`(재해석, 아래 참고) | 1,440,000.48(계수+고정) | 1,440,000.45 | 1,440,000.03 | 1,440,000.03 |
| 게이트_초월_임채민_AP(Tichi_skill_1) | `<300` AND B06B / 근사ELSE | 7,000,000 | 3,000,000(무버프 기준) | 〃 | 〃 |
| 게이트_전설적인_이현주(Legend_han_1) | `<200`/`ELSE` | 262,500.05(계수+고정) | 262,500 | 112,500.05 | 112,500.05 |
| 게이트_전설적인_홍인창(Legend26_tichi_crows) | `<200` AND B06B / 근사ELSE | 4,600,000 | 600,000(무버프 기준) | 〃 | 〃 |
| 게이트_제한_이충민_8f2b5872(Ain_Skill_2) | B06B유/무 | 5,270,000 | 5,030,000(무버프 기준) | 〃 | 〃 |
| 게이트_제한_임준성(King_skill_2_tr) | `<200`/`ELSE` | 12,225,000 | 2,225,000 | 10,950,000 | 10,950,000 |
| 게이트_제한_박성호_3a13fd3d(Marco_S2) | `<200`/`ELSE` | 862,500.30(계수+고정) | 862,500 | 112,500.30 | 112,500.30 |
| 게이트_랜덤_한마_바키(byakuya_Chan) | `<200`/`ELSE` | 5,500,000.02(계수+고정) | 5,000,000.02 | 5,500,000 | 5,500,000 |
| 게이트_랜덤_카마도_탄지로_d370bb23(Ryougi_Shiki_2) | `<200`/`ELSE` | 10,740,000.33(계수+고정) | 10,740,000.03 | 9,590,000.33 | 9,590,000.33 |
| 게이트_랜덤_카마도_탄지로_ff8bed43(Ryougi_Skill_3×3) | `<200`/`ELSE`×3 | 15,520,000.45(계수+고정) | 15,520,000.03 | 4,320,000.45 | 4,320,000.45 |

"이전" 열은 62파일 버그 그대로(전 효과 `chance:1`, 전부 항상 같이 발동)였을 때의
합계다 — "계수" 항목(0.x)은 `대상최대체력/현재체력×배율` 형태라 실제 값은 대상
체력에 따라 달라지므로 이 표에서는 배율 자체를 그대로 뒀다(대상 100/보스HP를
대입하면 실제 원 단위가 나온다). Python으로 targetCondition/버프ID 판정 로직을
`UnitAttacker.CastSkillLevel`/`ApplyToEnemy`와 동일하게(효과별 독립 `chance` 판정
후 AND 조건) 재구현해 pointValue 100(일반)/200(보스)/301(사이드보스) 세 대입으로
검산했다 — **⚠️ Python 재구현 — 런타임 미검증**(§9).

⚠️ **엔진 한계 — `(A AND B)`의 정확한 ELSE를 못 담는다**: `Tichi_skill_1`·
`Legend26_tichi_crows` 둘 다 원작이 "`<300`(또는 `<200`) AND 버프B06B보유"의
ELSE를 요구하는데, 우리 엔진은 효과당 조건 하나(`targetCondition`)+버프 하나
(`required`/`forbiddenTargetBuffId`)의 AND 조합만 표현하고 OR/드모르간 부정은
못 담는다. **De Morgan의 두 항(`pointValue>=경계` OR `B06B없음`) 중 "B06B없음"쪽만
근사로 담았다** — B06B는 R62·66·71 광폭화 소환체 전용 태그라 "point값이 경계 이상
*이면서* B06B도 가진" 조합이 실전에 사실상 없어(사이드보스 자신은 소환체가 아니라
B06B가 없다) 근사 오차는 이론상으로만 존재한다.

### 보류/제외 3건

- **`kikoyou_hp`**(게이트_랜덤_이즈미_신이치_351a0f00.asset) — **배선 안 함.** 장부
  §11이 "`<200`=현재체력×0.40, `ELSE`=고정100만"을 결측이라 했는데, 원문 CSV
  (`ORIGINAL_UNLISTED_SKILL_EFFECTS.csv` 148-151행)를 직접 대조하니 그 두 값은
  **RRD순번3·4로 이미 이 파일에 들어있다**(효과0=0.40, 효과1=1,000,000, 둘 다
  현재 무조건). 진짜 결측은 RRD순번1·2(둘 다 1,500,000 Flat, `Enemies`그룹 AoE,
  각각 Hero/AD·Spells/AP)인데 **포인트값 조건과 무관한, 완전히 별개의 AoE 효과
  쌍으로 보인다**(같은 값, 다른 damageType — `<200`/`ELSE` 배타 쌍의 형태가 아니다).
  §11의 "#1/#2" 라벨이 원문 RRD순번과 다른 값을 가리키고 있어 **그대로 적용하면
  이미 있는 효과를 중복으로 다시 만드는 사고**가 난다 — 리서치 재확인 요청함
  (PM에 보고, 2026-09-06).
- **`vivi_Skill_4_Mana`** — 배선 안 함(§11에서 이미 A0LZ 캐스터 스택 축 결측으로
  재분류됨, 포인트값 문제 아님).
- **`Legend13_2`의 `>=300` 갈래** — 파일에 이미 있는 4효과(#1~#4)를 전부 `<200`/
  `==200`/무조건으로 다 배정했고 `>=300`에 대응하는 5번째 효과가 CHANCE1_FIX_
  INSTRUCTIONS.md에 없다. PM이 "Gaban_Skill_mana·Legend13_2 둘 다 `>=300`
  갈래가 있다"고 전달했으나 **Legend13_2 쪽은 현재 매핑에 그 갈래가 안 보인다** —
  리서치 재확인 요청함.

### 재해석 1건 — `Ryougi_Shiki_R2`

`CHANCE1_FIX_INSTRUCTIONS.md`는 이 파일의 효과0(#2=0.45)을 `ELSE`(단독, 짝인
`<200`은 없음)로, 효과1(#3=0.03)을 "무조건 별개"로 분류했다. 그런데 원문 CSV
(`ORIGINAL_UNLISTED_SKILL_EFFECTS.csv` 125-126행)를 직접 보면 **#2(0.45)·#3(0.03)이
완전히 같은 형태**(`SingleTarget`/`Hero`/`AD`, `GetUnitStateSwap(MAX_LIFE)` 곱)에
배율만 다르다 — 전형적인 "약한 버전/강한 버전" 포인트값 쌍의 모양이다. **이걸
전형적인 `<200`(0.45)/`ELSE`(0.03) 쌍으로 재해석해 배선했다** — 리서치 문서를
그대로 따르지 않고 직접 원문을 재대조해 판단을 바꾼 것이라 명시한다. 그 대신
CHANCE1_FIX_INSTRUCTIONS.md/장부§11이 "결측"이라 했던 RRD순번1(원문 CSV상
5,000,000 Flat, `Enemies`그룹 AoE, `Hero`/`AD` — **0.45가 아니다**)은 여전히 결측
상태로 남는다 — §11의 "missing=0.45" 라벨은 원문과 안 맞으므로 폐기하고 이 절로
대체한다. 리서치 재확인 요청함.

**검증 도구**: `Tools/check_required_fields.py`(#20/#21 클린), `Tools/
check_assignment_invariants.py`(클린) 둘 다 exit 0. `grep -c "targetCondition: [1-3]"`
로 36건(효과 단위) 직접 재확인 — 파일 단위 집계(17개)와 다른 건 한 파일에 여러
효과가 걸린 경우(예: Ryougi_Skill_3 계열 1파일에 6건)가 있어서다.

## 6. 재생성 방법

이 장부는 스크립트로 다시 만들 수 있다(수치가 안 바뀌었으면 그대로, 바뀌었으면
카테고리·건수만 다시 세면 된다) — `Assets/Data/UnitSkills/*.asset`의 `description`을
`[게이트 미확인]`/`[미확인]`/"축밖·축 밖·축이 없·축 없음·우리 축에 없·우리에 없"
패턴으로 훑고, §1의 9개 정규식으로 분류한다. 이 세션에서 임시로 짠 스크립트는
저장 안 했다 — 다음에 이 장부를 갱신할 사람은 위 패턴 그대로 새로 짜면 된다(로직은
간단하다, §0 참고).
