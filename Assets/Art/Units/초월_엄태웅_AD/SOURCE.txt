초월_엄태웅_AD(죠죠 니지무라 오쿠야스) — blender 세션 **재출력**(2026-09-24, PM 요청 A군). gen_scan_rig.py.

■ 무엇이 잘못돼 있었나 — **손·아래팔 뼈가 팔 끝보다 47cm 바깥 허공에**
```
커밋본:  mixamorig:LeftHand   x +1.35 · RightHand x −1.35
         mixamorig:L/RForeArm x ±0.99
         메시 경계             x −0.88 ~ +0.88
```
살이 붙은 뼈가 몸 밖이면 클립을 먹일 때 그 먼 지점을 축으로 팔이 돌아간다.
📷 `why/pin_초월_엄태웅_AD.png` — 빨간 공이 뼈 자리다. 팔 바깥 허공에 떠 있는 게 보인다.

■ 🔴 고침 — **설정은 안 고쳤다. 지금 파이프라인으로 다시 뽑기만 했다**
커밋본이 **옛 산출물**이었다. 같은 설정으로 오늘 다시 돌리니 뼈가 전부 메시 안으로 들어왔다.
(노바라 때와 같다 — 「문제 보고를 받으면 먼저 커밋본을 그대로 다시 뽑아 재 보라」.)

■ 곁가지: `fit_arms` 옵션을 만들었다가 **기본은 꺼 뒀다**
팔 뼈가 메시 밖이면 그 높이 메시 경계 안쪽(85%)으로 당기는 옵션을 gen_scan_rig.py에 넣었다.
그런데 끝까지 파 보니 **쓸 데가 없었다**: 이 유닛은 다시 뽑기만 해도 들어왔고, 같이 걸렸던
전설적인_김민규·전설적인_이일중은 결함이 아니라 **허깨비 팔**이었다(아래). 그래서 opt-in으로 뒀다 —
안 쓰는 기계를 기본으로 돌리지 않는다. 필요한 유닛이 나오면 그때 켠다.

■ 검증(고치기 전 ↔ 고친 뒤)
```
뼈가 메시 밖   26%    →  **없음** ✅
clip_spread     —     →  최댓값 0.99 (기준 3.0 · 대조군 0.97)
```

■ 산출물(스크래치만, Assets 미반영)
초월_엄태웅_AD.fbx · Textures/
