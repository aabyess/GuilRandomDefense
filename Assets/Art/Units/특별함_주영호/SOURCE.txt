출처: ~/Downloads/stylised_orc_model.glb(원본 배포처 미상 — 사장님 다운로드분)
원본 파일: stylised_orc_model.glb · 8,692,476 bytes(8.69MB) · sha256 c47f87c26b15de2a407df780e91eab3ffe74e9c8172a2679d77972625fe1bb97
받은 날: 2026-09-14 · 유닛: 특별함_주영호(별명 마마보이)

glb: 뼈·스킨 0 · 애니메이션 0 · 메시 66개(조각 이름으로 봐서 몸통 하나 + 갑옷·소품 60여 개가
  전부 따로 — 정점 예산으로 쪼갠 게 아니라 원래 킷배시 조립품) · 삼각형 합 69,576(PM 사전조사와
  일치) · 재질 4개: Body_Texture(베이스 image0·노멀 image2) · Eye_Texture(베이스 image3, 노멀
  없음) · Armour_Texture(베이스 image5·노멀 image8) · Default_Material(이미지 없음, glTF
  baseColorFactor만 있는 회색 계열 — Chest_Belt_Detail·Belt_Loop_Detail류 3쌍 6조각).
  image1·4·6·7은 안 씀(PM 사전조사와 일치, 금속/거칠기 맵으로 추정).

원본 배치(리깅 전 확인): Z-up · 키(z) 0.021422(작은 단위, glTF 노드에 0.01 스케일이 이미
  걸려 있음 — 절대 크기는 무관, 스크립트가 height=1.8로 다시 맞춘다) · 팔이 이미 좌우로
  넓게 벌어져 있어(도끼 자세 확산, X축) 전체 가로폭이 세로높이의 81%나 됨 · 정면 판정 렌더
  (fromNegY.png, 스크래치)로 얼굴이 −Y를 본다는 것과 회전 불필요(rotate_z=0)를 확인 —
  01~03(전부 rotate_z 필요)과 다른 첫 사례. 팔은 어깨에서 약 30~40° 처져 있어(A자에 가까움,
  판정 렌더 팔 처짐 42.2°로 측정) level_arms=True로 편다.

🔴 조각 66개 처리 — 재질 하나(Armour_Texture)를 갑옷·소품 40여 개가 같이 써서 기존 방식
  (재질→뼈 rigid dict)으로는 못 가른다. 이번 건에서 gen_skin_rig.py에 새 기능 둘을 추가했다:
  1) rigid_by_name — 조각 "이름"의 부분 문자열로 rigid 뼈를 정하고(예: "Wrist"→그 조각
     최종 x부호로 Left/RightForeArm), "{side}" 틀은 조각의 평균 x로 좌우를 그때그때
     판정한다(재질이 아니라 개별 오브젝트 단위라 좌우 비대칭 디자인도 맞게 갈린다).
  2) decimate_rules/decimate_default — 조각 이름별로 다른 감량 비율(아래 감량 항목 참고).
  두 기능 다 조각별 최종 좌표를 미리 기록해 뒀다가 합친 뒤 KD-트리 위치 매칭으로 실제
  정점에 되찾아 붙인다(자동가중치 되옮김과 같은 방식 — join 순서에 기대지 않는다).

  분류(첫 매치 우선): Eye→Head · Jaw(Lower/Upper)→Head(마마보이엔 턱뼈가 없어 고정) ·
  Belt·Tooth·Loop 계열(허리 장신구)→Hips · Wrist 계열→그 팔뚝(ForeArm) · Shoulder·Horn·Ring
  (어깨 갑옷·장식뿔, |x|<0.06m면 중앙 망토로 보고 Spine2, 아니면 그 팔 Arm)→Arm 또는 Spine2.
  나머지(Body_Low·Jaw 자체·Leg_Wrap 4개·Trouser·Boot 5종×2)는 rigid 없이 자동가중치 —
  Trouser가 양다리를 잇는 통짜 통짜 메시(x 대칭, z분율 0.09~0.53)라 UpLeg·Leg·Foot 히트가
  자연스럽게 닿고, Boot도 자동가중치로 둬야 Foot·ToeBase 둘 다 가중치를 받는다(처음에 Boot를
  통째로 Foot에 rigid 고정했더니 ToeBase가 가중치 0으로 남아 assert가 막았다 — 발이 통째로
  부츠 속이라 맨발 메시가 따로 없다는 뜻, 부츠 자체를 발목~발끝 잇는 자동가중치 표면으로
  써야 ToeBase까지 커버된다).

  ⚠️ 정말 큰 함정 하나 — 처음엔 이름 기준 rigid로 뺀 조각(눈·턱·허리장신구·손목·어깨 20여 개)을
  재질 기준 rigid(옛 코드, 빈 딕셔너리)와 똑같이 자동가중치 사본에서 안 빼고 그대로 뒀다.
  결과: Bone Heat Weighting이 "failed to find solution for one or more bones" 경고를 내고
  가중치 있는 정점이 통째로 0개가 됐다(서로 안 이어진 섬 20여 개가 히트 확산 자체를
  깨뜨렸다). gen_skin_rig.py의 자동가중치 사본 준비 단계(rigid_idx 필터)를 재질 기준뿐
  아니라 이름 기준 rigid_vert까지 같이 거르게 고쳐서 해결 — 이 수정은 공용 코드라 앞선
  세 유닛에는 영향 없다(그쪽은 이름 기준 rigid를 안 써서 그 갈래를 안 탄다).

  또 하나 — ToeBase 관절을 처음 y=−0.10(발끝 쪽 극단)에 뒀더니 부츠 메시의 가장자리
  가장자리라 히트가 못 붙었다(가중치 0, assert가 잡음). y=−0.06(부츠 안쪽, 밀집한 부분)으로
  당겨서 해결 — 관절은 늘 실제 표면 "안"에, 가장자리에 두지 말 것(교훈으로 남긴다).

  Default_Material(이미지 없음): glTF baseColorFactor를 그대로 16×16 단색 PNG로 합성(신규
  build() 분기 — cfg["textures"] 딕셔너리에 없는 재질은 이 경로로 떨어진다, 이지원 건의
  write_solid_png 재사용).

🔴 감량: 원본 69,576삼각형 중 Shoulder_Fur(19,296) + Belt_Fur_1(7,150) + Belt_Fur_2(7,494) =
  33,940(전체 49%)이 털 가닥을 기하로 흉내낸 조각이라 실루엣 손실 없이 크게 줄인다(비율
  0.22, Decimate 모디파이어 COLLAPSE). Body_Low·Jaw·Eye·Hand는 원본 그대로(PM 지시 "얼굴·손은
  덜 줄이게"). 나머지(허리띠·장화·손목·뿔 등 소품)는 0.72. 조각별로 결정한 이유·이 Blender
  (5.2.1)엔 bmesh.ops.decimate_collapse가 없어(UV 경계 델리밋 기능) 대신 표준 Decimate
  모디파이어를 썼다는 것(UV 이음매를 완벽히는 안 지킴, 털처럼 반복 패턴인 조각 위주로만
  크게 줄여 위험을 낮췄다)은 gen_skin_rig.py 주석에도 적어 뒀다.
  최종: 69,574(집계 오차 −2, 폴리곤 카운트 방식 차이) → 36,249삼각형(export한 FBX에서
  직접 확인) — 목표(~40,000) 안.

리깅: 2026-09-14 blender에서 직접(Mixamo 안 씀) — Tools/blender/gen_skin_rig.py
  「특별함_주영호」로 재현.
  - mixamorig 22뼈 전부 가중치 있음(0건 없음, 스크립트 assert 통과) — 이름기준 rigid
    정점 14,529개(허리·손목·어깨·눈·턱), 나머지는 자동가중치.
  - 관절 위치는 정사영 렌더 + z높이별 정점 단면(P 전체 21분할)으로 몸통 구조를 먼저 파악한
    뒤(예: Body_Low가 z분율 0.40 아래로는 아예 없다는 것 확인) 표준 인체 비율을 이 오크
    체형(넓은 어깨·굵은 팔)에 맞게 조정해 잡았다.
  - level_arms로 팔을 수평으로 편다(굽은 각도: LeftArm/RightArm 42.2°·ForeArm 7.7°·Hand
    14.7°, 좌우 대칭 확인).
  - 가중치: 이음새 붙임 840정점, 자동가중치 실패→가까운 가중치 정점 채움 297개(최대거리
    0.111m — 다른 세 건보다 크다, 조각이 원래 서로 안 붙어 있는 킷배시 소스라 자연스럽다).
  - 키 1.8m(원본 0.0214×84.03) · 발 z 0 · 정면 −Y · 원점 두 다리 사이(center_band
    0.40~0.45 — Body_Low 자체가 몸통·머리·팔만 담아 다리가 없어서, Body_Low 맨 아래
    골반 끝단에서 좌우 중심을 잡았다. 이지원·서아인처럼 발목 높이를 못 쓴다).

⚠️ 어깨 갑옷 관통(PM 사전조사 지적, 실측 확인): T자 렌더에서 어깨 팔뚝 갑옷판 뒤로 어깨
  근육 메시가 살짝 비쳐 보인다 — 원본 모델링 단계의 문제라 리깅으로 못 고친다. 게임
  카메라 거리에서는 눈에 띄지 않지만(판정 렌더로 확인), 클로즈업이 필요하면 모델링
  수정이 별도로 필요하다.

판정 렌더(T자 + 팔 45°·무릎 90°): /tmp/orc_scratch/render_final/특별함_주영호_{T,arms45_knees90}_{front,side,knee,shoulder}.png
  (스크래치 경로 — 저장소에는 없음, 재현하려면 위 명령을 --render로 다시 돌릴 것)
  - T자: 늘어짐 1.00(변형 없음). 눈으로 확인 — 어깨 갑옷·허리띠·부츠 전부 제자리, 원본
    실루엣 그대로.
  - 팔45·무릎90: 상위1% 늘어짐 1.31로 안정적. 렌더로 확인해도 찢어짐·분리 없음 — 갑옷·
    허리띠·부츠 전부 제 위치에서 자연스럽게 따라 움직인다. 다만 최댓값이 1052.9로 매우
    크게 찍힌 자리가 하나 있다(허리 부근, x0.17 y−0.13 z0.84) — 통계상 극단치지만 상위1%
    에는 안 잡히는 정점 하나 수준이라 렌더 클로즈업으로도 안 보인다. 원인은 rigid(허리
    장신구, 100% 고정)와 자동가중치(몸통, 여러 뼈 혼합) 경계가 만나는 이음매로 추정 —
    서아인 건의 목깃 소품 이음매(최대 3.66)와 같은 종류지만 이번엔 rigid·자동가중치가
    직접 맞닿아 배율이 더 크게 나온다. 눈에는 안 보이지만 기록해 둔다.

원본 지우지 않음.
