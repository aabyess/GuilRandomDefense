전설적인_이재윤 — blender 세션 **재출력**(2026-09-24, PM 요청 C군). gen_objrip_skin.py.

■ 무엇이 잘못돼 있었나 — **발끝 뼈가 신발보다 앞에**
쉬는 자세에서 살 붙은 발끝 뼈(ToeBase)가 메시 경계 밖에 있었다(키의 6.5%). 정지 렌더로는 안 보이고,
클립을 먹이면 몸 밖 지점을 축으로 발이 돈다.

■ 🔴 원인은 이 한 종이 아니라 **복붙 한 줄**이었다
제한_강보명·희귀함_유재헌·전설적인_이재윤·희귀함_윤현모·초월_박민석_ADAP **다섯 종의 발끝 좌표가
±0.16, −0.25까지 완전히 같았다.** gen_objrip_skin.py의 joints 표에 ToeBase=(x, −0.14, …)가
그대로 반복돼 있었고 — 신발 앞끝은 모델마다 다른데 — **메시에 맞춰 다시 잰 적이 없었다.**
한 종씩 고치면 여섯 번째가 또 같은 값으로 태어나므로 **표를 고쳤다**(PM 지시).

■ 고침 — 발끝은 표를 믿지 않고 **메시에서 잰다**(fit_toes, 기본 켜짐)
발목 높이 아래·그쪽 발 x 둘레의 정점이 신발이다. 그 앞끝(y 최소)과 뒤끝(y 최대)을 재서
ToeBase는 앞에서 35%, ToeTip은 앞끝 바로 안쪽에 둔다. x·z는 표 그대로 둔다(그쪽은 안 틀렸다).
    표값 y −0.252  →  잰 값 y −0.044

■ 검증(고치기 전 ↔ 고친 뒤)
    뼈가 메시 밖   6.5%  →  **없음** ✅
    clip_spread    최댓값 1.00 (기준 3.0 이하 · 대조군 랜덤_한마_바키 0.97)
모델(메시·텍스처·키 1.8m)은 그대로다. 발끝 뼈 두 개만 제자리로 왔다.

■ 산출물(스크래치만, Assets 미반영)
전설적인_이재윤.fbx · Textures/
