히든_석성례(원피스 알비다, pl_alvida_orig01) — blender 세션 **재출력**(2026-09-24, PM 요청 A군). fix_unit_fbx.py.

■ 🔴 무엇이 잘못돼 있었나 — **골반 뼈가 몸 밖에, 바닥보다 1.17m 아래**
```
커밋본:  mixamorig:Hips  (0.000, 0.000, −1.172)   ← 메시는 z 0~1.8
         나머지 뼈 전부  x ≈ −0.91, y ≈ −1.43     ← 몸과 같이 있다
         그래서 유닛 전체가 원점에서 (−0.91, −1.43)만큼 밀려 있었다
```
쉬는 자세에선 뼈가 안 움직이니 멀쩡해 보이고, 클립을 먹이면 **그 먼 지점을 축으로 몸이 돌아 13cm 떠올랐다.**
사이렌헤드(안흔함_박준희)와 같은 병이다 — 「정지 렌더로는 원리적으로 안 보이는 층」.

■ 🔴 원인은 **원본 리그**다(우리 파이프라인이 아니다)
zip 안 원본 pl_alvida_orig01.fbx를 그대로 열어 재 보니:
```
Body_Pelvis  (+0.0101, +0.0163, −0.0130)      ← 골반
Body_Belly   (−0.0000, +0.0003, +0.0105)      ← 그 자식(배)
LLeg_Thigh   (+0.0021, +0.0004, +0.0068)      ← 그 자식(넓적다리)
메시 경계     (−0.016, −0.015, −0.015)~(0.015, 0.012, 0.020)
```
모델 크기가 0.035밖에 안 되는데 골반이 y로 0.016 떨어져 있다 — **모델 절반만큼**이다. 키 1.8로 키우면 0.8m가 된다.
다른 pl_ 유닛(양재모·임장혁·박민수 등)은 전부 제자리인데 이 하나만 그렇다.

■ 고침 둘(새 옵션 `fit_hips`)
① **골반 뼈를 양 넓적다리 머리의 한가운데로 옮긴다** — 골반은 정의상 거기다. 자식 뼈는 연결돼 있지 않아 안 따라온다(그게 맞다).
② **가운데 맞추기도 골반이 아니라 넓적다리 한가운데로 잡는다** — 그 뼈가 틀렸으니 그걸 기준 삼으면 안 된다.
   이게 없으면 유닛 전체가 원점에서 밀린 채 남는다.
    골반 (0.000, 0.000, −1.172) → **(0.000, 0.000, +0.613)** · 옮긴 거리 2.46

■ ⚠️ 이 항목은 **오늘 돌리면 그 자리에서 멈췄다**
`no_nulls=True`라 Null 노드가 뼈로 안 살아나는데 `drop_bones`에 그 이름 여덟(model_root·HELPER_*·pre/post_flag·eff_muzzle_*)이
적혀 있었다 — 없는 뼈에 assert가 걸린다(「뺄 뼈가 없다 model_root」). 커밋된 FBX는 그 전에 나온 것이고 **설정과 산출물이 어긋나 있었다.**
실제 뼈인 world_joint만 남겼다.

■ ⚠️ `use_rest_pose=True`는 **그대로 둬야 한다** — 껐다가 확인했다
껐더니 뼈 숫자는 좋아졌는데(뼈 넘침 0.447→0, 위 0.82→0.98) **메시가 통째로 찢어졌다**(렌더 확인 — 몸통이 긴 판으로 늘어남).
이 원본은 기본 자세와 결합 자세가 다르고, 결합 자세로 읽어야 살이 제자리에 붙는다. 09-17 주석대로가 맞다.

■ 검증(고치기 전 ↔ 고친 뒤)
```
뼈가 메시 밖      65%      →  **없음** ✅
리타겟 뼈퍼짐     1.50배   →  **0.99** (대조군 랜덤_한마_바키 0.97)
리타겟 공중       +3.7%    →  clip_spread 통과 범위
```
뒷면 컬링 렌더 정상(파란 코트·분홍 셔츠·흰 모자·붉은 구두 전부 제 색, 뚫린 면 없음).

■ 산출물(스크래치만, Assets 미반영)
히든_석성례.fbx · Textures/pl_alvida_orig01_diff.png · cull_full.png · cull_head.png
