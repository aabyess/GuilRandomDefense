히든_호치킨(나루토 이치라쿠 라멘 一楽 포장마차) — blender 세션(2026-09-23).
gen_prop_unit.py UNITS["히든_호치킨"]로 처리. 뼈 없는 **소품 유닛**(특별함_김정래 노트북 선례).

■ 원본
~/Desktop/구랜디스킨모음/05_히든/히든_호치킨.glb (11.4MB, Sketchfab)
 - 뼈 0 · 스킨 0 · 애니 0 · 메시 37 · 재질 4 · 내장 이미지 12
 - 재질 4개마다 이미지 3장씩: 베이스(jpeg) · metallicRoughness(png) · 노멀(png)
   Detail_Objects(0,1,2) · Ground(3,4,5) · Building_Bottom(6,7,8) · Building_Top(9,10,11)

■ 🔴 폴리곤 수(PM 요청) — **감량 불필요**
원본 삼각형 **10,137**, Ground_low를 뺀 뒤 **10,029**. 소품 유닛 치고 아주 가볍다(노트북 8,322면과
같은 수준) → decimate 안 걸었다.

■ 🔴 가장 긴 변·높이(PM 요청)
원본(가져오기 단위, Ground 뺀 뒤): 가로 857.2 · 앞뒤 994.3 · 높이 734.0
「가장 긴 변 0.6m」 관례를 적용한 결과(배율 0.000603):
  **가로 0.5173 · 앞뒤 0.600 · 높이 0.4429 m**, 최저 z 0.
  → 가장 긴 변은 **앞뒤(Y)**다. 가게가 폭보다 깊다 — 지붕 처마와 앞에 매단 등롱·사슬이 정면으로
    많이 튀어나와서다(등롱이 y −538까지, 건물 본체는 −389까지). 게임 안 크기는 PM 판단.

■ 처리
- 🔴 Ground_low.000_Ground_0(돌 받침 슬래브, 원본 z −177~8 · 가로세로 1,262 = 건물보다 큼)를
  지시대로 뺐다 — 맵 바닥 위에 또 깔리면 겹친다. 빼고 나니 바닥이 건물 밑면(원본 z −23.8)이
  되어 그 면이 z 0에 놓인다. 남는 Ground 재질도 함께 사라진다(그 메시 전용이었다).
- 정면은 이미 −Y다(ラーメン 一楽 간판·의자 쪽) → rotate_z 불필요. raw_negY.png로 직접 확인.
- 원점은 전체 bbox 가운데(center="all"). 노트북식 「정점 가장 많은 조각을 프레임으로」는 건물엔
  안 맞는다(가장 많은 조각이 미닫이문 935정점이라 문 가운데가 원점이 될 뻔했다).
- 🔴 베이스 텍스처가 **jpeg**다(메탈릭·노멀만 png) — 확장자를 glb의 mimeType대로 .jpg로 뺀다.
  .png로 적으면 유니티가 확장자로 형식을 골라 깨진다.
- metallicRoughness·노멀 8장은 안 쓴다(우리 재질은 베이스+발광만 쓴다 — 노트북과 같은 처리).

■ 🔸 gen_prop_unit.py 보편 수정(이번에 넣음, 소품 3건에 같이 쓰려고)
 ① 압축 원본 지원 — member로 zip 안 경로를 준다. **zip 안 rar**도 되게 했다(파이썬 표준 라이브러리로는
    rar를 못 푼다 → 시스템 bsdtar를 쓴다. macOS 기본 탑재, rar 읽기 됨. 이삭토스트·감탄떡볶이용).
 ② FBX 입력 — src가 .glb/.gltf가 아니면 import_scene.fbx로 읽는다.
 ③ 바깥 텍스처 파일 — materials 값이 정수면 glb 내장 이미지 번호, 문자열이면 압축 안 textures/의
    파일 이름(FBX 원본은 텍스처가 바깥에 있다).
 ④ drop_meshes · center="all" · 베이스 텍스처 확장자 mimeType 추종.

■ 검증
- 배치 리포트: 크기 (0.5173, 0.6, 0.4429)m · 최저 z 0 · 삼각형 10,029.
- 재수입 검증: 메시 1개 · 가로 0.5173 · 높이 0.4429 · 앞뒤 0.6 · 최저 z 0 ·
  오브젝트 원점거리 0 · **Model Lcl Rotation (0,0,0)**(뼈 없는 단일 메시가 유니티에서 눕는 사고 방지).
- 판정 렌더 앞·옆·위 — 받침 없이 건물이 바닥에 딱 놓이고, 간판·등롱·물탱크 텍스처 정상.

■ 산출물(스크래치만, Assets 미반영)
히든_호치킨.fbx · Textures/{Detail_Objects,Building_Bottom,Building_Top}_baseColor.jpg ·
히든_호치킨_front.png · _side.png · _top.png
