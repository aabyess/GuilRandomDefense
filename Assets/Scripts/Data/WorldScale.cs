/// <summary>
/// 맵 배율 — 원작(워크3) 1단위 = 우리 <see cref="Value"/>분의 1. 정본 배율 4.167
/// (근거: 충돌 크기 기준과 "레인 폭 ÷ 유닛 지름" 기준이 둘 다 4.167로 나온다.
///  속도 기준 23.73은 쓰지 않는다 — PM 지시 2026-09-23 "맵·속도·사거리를 원작 비율로").
///
/// ⚠️ 이 값이 **유일한 정본**이다. 맵 생성 쪽 <c>MapLayout.Scale</c>도 여기를 가져다 쓴다.
/// 에디터 어셈블리(Assets/Editor)는 런타임 어셈블리를 볼 수 있지만 반대는 안 되기 때문에,
/// 양쪽이 같은 값을 보려면 상수가 런타임 쪽에 있어야 한다 — 그래서 MapLayout이 아니라
/// 여기에 둔다. (ArtBinder.UnitHeight를 public으로 연 것과 같은 이유다.)
///
/// ⚠️ 유닛 크기는 이 배율을 **타지 않는다**(사장님/PM 확정: "유닛 기준 키 20이 기준자").
/// 맵이 커지고 유닛은 그대로다. 그래서 유닛 크기에 묶인 값(흙길 폭 12 등)도 안 탄다 —
/// 어떤 값이 "세계 좌표"이고 어떤 값이 "유닛 기준"인지 구분해서 곱할 것.
/// </summary>
public static class WorldScale
{
    public const float Value = 4.167f;
}
